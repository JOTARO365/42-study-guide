/* A-Maze-ing — เนื้อหาสร้างจาก skill a-maze-ing-generator ซึ่งอ่านจากโจทย์จริง */
window.TEACHING_DATA = window.TEACHING_DATA || [];
window.TEACHING_EN = window.TEACHING_EN || {};

window.TEACHING_DATA.push({
  id: "amaze",
  name: "A-Maze-ing",
  tag: {
    th: "สร้างเขาวงกตด้วย Python จากไฟล์ตั้งค่า เขียนออกเป็นเลขฐานสิบหกหนึ่งหลักต่อหนึ่งช่อง พร้อมทางออกที่สั้นที่สุด ลาย 42 กฎห้ามพื้นที่โล่ง 3x3 และโมดูลที่แพ็กเป็น wheel ให้โปรเจกต์อื่นใช้ต่อได้",
    en: "Generate mazes in Python from a config file, write them as one hexadecimal digit per cell with the shortest path, draw a 42 in sealed cells, keep every open area under 3x3, and ship the generator as an importable wheel"
  },
  accent: "#ec4899",
  sections: {
    principle: [
      { h: "โจทย์นี้ต้องการอะไร" },
      { p: "เขียนโปรแกรม Python ที่อ่าน **ไฟล์ตั้งค่าหนึ่งไฟล์** สร้างเขาวงกต แล้วเขียนผลลงไฟล์ในรูปแบบเลขฐานสิบหก พร้อมแสดงผลให้ดูด้วยตาได้ และแยกส่วนอัลกอริทึมออกมาเป็นโมดูลที่โปรเจกต์อื่น import ไปใช้ต่อได้" },
      { code: String.raw`python3 a_maze_ing.py config.txt`,
        cap: "ชื่อไฟล์หลักต้องเป็น a_maze_ing.py และรับอาร์กิวเมนต์เดียวคือไฟล์ตั้งค่า", lang: "bash" },
      { note: "**อาร์กิวเมนต์มีตัวเดียว** — ทุกอย่างที่ปรับได้อยู่ในไฟล์ตั้งค่า ไม่ใช่ในบรรทัดคำสั่ง อย่าออกแบบให้รับ `--width` แล้วมาเถียงกับไฟล์ทีหลัง" },
      { h: "ข้อกำหนดทั้งหมด — ต้องอ่านพร้อมกัน" },
      { p: "โจทย์ดูเหมือนรายการข้อกำหนดที่ไม่เกี่ยวกัน แต่จริง ๆ แล้ว **สามข้อในนั้นคือข้อเดียวกันในเสื้อผ้าคนละชุด** เห็นตรงนี้แล้วงานหายไปเกินครึ่ง" },
      { table: { head: ["ข้อกำหนด", "สาระ"], rows: [
        ["ไฟล์ตั้งค่า", "หนึ่งบรรทัดหนึ่งคู่ `KEY=VALUE` บรรทัดที่ขึ้นต้นด้วย `#` คือคอมเมนต์ ต้องข้าม"],
        ["ทำซ้ำได้", "สุ่มก็จริง แต่ต้องมี seed ที่ทำให้ได้เขาวงกตเดิม"],
        ["ผนังต้องสอดคล้อง", "ช่องที่ติดกันต้องเห็นผนังร่วมตรงกันทั้งสองฝั่ง"],
        ["ไม่มีช่องโดดเดี่ยว", "ทุกช่อง (ยกเว้นลาย 42) ต้องเดินถึงได้"],
        ["ขอบนอกต้องมีผนัง", "ทางเข้ากับทางออกเป็นช่องเฉพาะ ขอบที่เหลือต้องปิด"],
        ["ห้ามพื้นที่โล่ง 3x3", "ทางเดินกว้างได้ไม่เกิน 2 ช่อง — 2x3 ได้ 3x3 ไม่ได้"],
        ["ลาย 42", "ต้องมองเห็นเลข 42 ที่วาดด้วยช่องซึ่งปิดผนังครบสี่ด้าน"],
        ["`PERFECT=True`", "มีทางเดียวเท่านั้นระหว่างทางเข้ากับทางออก"],
        ["ไฟล์ผลลัพธ์", "ฐานสิบหกหนึ่งหลักต่อช่อง แล้วตามด้วยพิกัดเข้า พิกัดออก และเส้นทางสั้นสุด"],
        ["แสดงผลด้วยตา", "ASCII ในเทอร์มินัล หรือ MiniLibX พร้อมปุ่มโต้ตอบ"],
        ["นำกลับมาใช้ได้", "อัลกอริทึมต้องเป็นคลาสเดียวในโมดูลเดี่ยวที่ import ได้"]
      ]}},
      { h: "ไฟล์ตั้งค่า" },
      { code: String.raw`# config.txt — บรรทัดที่ขึ้นต้นด้วย # ถูกข้าม
WIDTH=20
HEIGHT=15
ENTRY=0,0
EXIT=19,14
OUTPUT_FILE=maze.txt
PERFECT=True

# คีย์เพิ่มเองได้ ถ้ามีประโยชน์
SEED=42`,
        cap: "หกคีย์แรกเป็นข้อบังคับ และต้องมีไฟล์ตั้งค่าตัวอย่างอยู่ใน repo", lang: "ini" },
      { note: "โจทย์ให้ **สคริปต์ตรวจไฟล์ผลลัพธ์** มาด้วย และบอกว่า Moulinette อาจใช้มันตรวจอัตโนมัติ — รันสคริปต์นั้นกับผลลัพธ์ของตัวเองก่อนส่งเสมอ" },
      { h: "ทุก error ต้องถูกจัดการ" },
      { p: "ไฟล์ตั้งค่าไม่ถูกต้อง หาไฟล์ไม่เจอ ไวยากรณ์ผิด ค่าที่สร้างเขาวงกตไม่ได้ — ทั้งหมดต้องจบด้วยข้อความที่อ่านรู้เรื่อง **ห้ามมี traceback หลุดออกมาเด็ดขาด** นี่คือกฎร่วมของทุกโปรเจกต์ Python ในส่วนนี้ของหลักสูตร ดูรายละเอียดที่หน้า **Python Modules 00–04**" }
    ],

    theory: [
      { h: "การตัดสินใจข้อเดียวที่จบทุกอย่าง" },
      { p: "`PERFECT=True` แปลว่า **มีทางเดียวระหว่างทางเข้ากับทางออก** เขาวงกตมีคุณสมบัตินี้ก็ต่อเมื่อทางเดินของมันเป็น **spanning tree** ของกราฟตาราง คือถึงทุกช่องและไม่มีวงรอบ" },
      { p: "ดังนั้นถ้าเลือกอัลกอริทึมที่สร้าง spanning tree ออกมาตรง ๆ ความสมบูรณ์แบบจะไม่ใช่สิ่งที่ต้องไปตรวจทีหลังอีกเลย" },
      { table: { head: ["อัลกอริทึม", "ลักษณะที่ได้", "หมายเหตุ"], rows: [
        ["**recursive backtracker** (DFS สุ่ม)", "ทางเดินยาวคดเคี้ยว ทางตันน้อย", "ใช้ stack ตัวเดียว อ่านง่ายที่สุด"],
        ["randomised Prim", "ทางตันสั้น ๆ เยอะ ดูหนามเตี้ย", "ต้องมีเซตขอบเขต"],
        ["randomised Kruskal", "สม่ำเสมอ ดูเป็นเขาวงกตมาก", "ต้องมี union-find"]
      ]}},
      { p: "ทั้งสามตัวให้ spanning tree เหมือนกัน จึงสมบูรณ์แบบเหมือนกัน DFS อ่านบนจอง่ายที่สุดและเขียนสั้นที่สุด" },
      { h: "เขียนแบบวนลูป ไม่ใช่เรียกซ้ำ" },
      { p: "ขนาดในโจทย์ไม่ใหญ่ แต่เทสต์ที่ดันขอบไม่ได้เล็กตาม — 400x300 คือ 120,000 ช่อง และ DFS แบบเรียกซ้ำจะชน recursion limit ของ Python ก่อนถึงตรงนั้นนานมาก stack ที่จับต้องได้คืออัลกอริทึมเดียวกันโดยไม่มีความเสี่ยงนั้น" },
      { code: String.raw`stack = [start]
visited = {start}
while stack:
    current = stack[-1]
    options = [(d, n) for d, n in neighbours(current)
               if n not in visited and n not in reserved]
    if not options:
        stack.pop()
        continue
    direction, neighbour = rng.choice(options)
    knock_down(current, direction)
    visited.add(neighbour)
    stack.append(neighbour)`,
        cap: "reserved คือช่องของลาย 42 ที่ห้ามเดินเข้า", lang: "python" },
      { h: "ทำไมกฎห้ามโล่ง 3x3 แทบไม่ต้องทำอะไรเพิ่ม" },
      { p: "spanning tree ไม่มีวงรอบ และบล็อก **2x2** ที่เปิดผนังในครบทั้งสี่ด้าน **คือวงรอบ** ดังนั้นเขาวงกตสมบูรณ์แบบจึงมีแม้แต่บล็อกโล่ง 2x2 ไม่ได้ ไม่ต้องพูดถึง 3x3" },
      { p: "กฎนี้จึงถูกละเมิดได้ที่เดียวเท่านั้น คือตอน **เปิดผนังเพิ่ม** เมื่อ `PERFECT=False` เอาการตรวจไปวางตรงนั้น และตรวจ **ก่อน** ที่จะเปิดจริง ไม่ใช่ไปตามเก็บทีหลัง" },
      { code: String.raw`def would_open_3x3(cell, direction) -> bool:
    knock_down(cell, direction)       # ลองเปิด
    bad = has_open_block(3)
    put_back(cell, direction)         # แล้วปิดคืน
    return bad`,
        cap: "ลองแล้วถอย ถูกกว่าการไล่ซ่อมทีหลัง", lang: "python" },
      { note: "การทำ pass ตอนท้ายแบบ 'ไล่หาทางเดินกว้างแล้วแบ่งครึ่ง' เป็นรูปทรงที่ผิด — มันแก้ผนังหลังจากที่การันตีความเชื่อมต่อไปแล้ว จึงทำลายสิ่งที่เพิ่งการันตีได้" },
      { h: "การเข้ารหัสผนัง กับสิ่งเดียวที่ตัวตรวจสนใจ" },
      { table: { head: ["บิต", "ค่า", "ทิศ"], rows: [
        ["0", "1", "North"], ["1", "2", "East"], ["2", "4", "South"], ["3", "8", "West"]
      ]}},
      { p: "**บิตที่ติดแปลว่าผนังปิด** `0xF` คือปิดสนิททั้งสี่ด้าน `0x0` คือเปิดหมด และ `0x5` (N+S) คือทางเดินแนวตะวันออก-ตะวันตก" },
      { p: "สคริปต์ตรวจที่โจทย์ให้มาสนใจอยู่เรื่องเดียว: **ช่องที่ติดกันต้องเห็นผนังร่วมตรงกัน** บิต East ของช่อง (x,y) ต้องเท่ากับบิต West ของช่อง (x+1,y) และในทำนองเดียวกันทุกทิศ" },
      { code: String.raw`def knock_down(self, cell, direction):
    x, y = cell
    dx, dy = STEP[direction]
    self.grid[y][x] &= ~direction
    self.grid[y + dy][x + dx] &= ~OPPOSITE[direction]`,
        cap: "ให้ทุกการเปลี่ยนผนังในโปรแกรมผ่านฟังก์ชันนี้ฟังก์ชันเดียว", lang: "python" },
      { p: "ทำแบบนี้แล้วข้อความ 'Wrong encoding for (x,y)' จะกลายเป็นสิ่งที่ **เป็นไปไม่ได้** แทนที่จะเป็นสิ่งที่ 'ไม่น่าจะเกิด'" }
    ],

    architecture: [
      { h: "ลาย 42 — จองก่อนขุด ห้ามปิดทีหลัง" },
      { p: "ลายคือช่องที่ปิดผนังครบสี่ด้าน (`0xF`) เรียงกันเป็นเลข 42" },
      { note: "**จองช่องพวกนั้นก่อนเริ่มค้นหา** วิธีที่นึกออกก่อน — สร้างเขาวงกตให้เสร็จแล้วค่อยไปปิดช่องลาย — จะตัดขาดทุกอย่างที่เดินถึงได้ผ่านช่องพวกนั้นเท่านั้น ซึ่งโจทย์ห้ามไว้ การจองก่อนทำให้การค้นหาไม่เคยเดินเข้าไป และความเชื่อมต่อถูกรักษาไว้โดยโครงสร้าง" },
      { ul: [
        "**ทางเข้ากับทางออกสำคัญกว่าลวดลาย** — เอาสองช่องนั้นออกจากเซตที่จอง ไม่งั้นลายที่วางกลางตารางจะขังผู้ใช้ไว้ในวันที่ทางเข้าดันไปอยู่ตรงกลางพอดี",
        "**ตัวอักษรกั้นช่องได้** — ออกแบบให้ช่องว่างในตัวอักษรเปิดออกด้านนอก แล้วยังต้องตรวจซ้ำอยู่ดี: หลังขุดเสร็จให้ไล่ช่องที่ไม่ได้จอง ถ้าเจอช่องที่การค้นหาไม่เคยไปถึง ให้เปิดผนังหนึ่งด้านเข้าหาเขาวงกต เป็นประกันราคาถูกสำหรับกฎที่จะพังหนึ่งครั้งในห้าสิบขนาด"
      ]},
      { p: "ตัวอักษรขนาด 5x3 ที่มีคอลัมน์คั่นต้องใช้ 7 คอลัมน์ 5 แถว บวกขอบอีกสองช่อง — ดังนั้น **11x9 คือตารางเล็กที่สุดที่ใส่เลข 42 ลงได้** เล็กกว่านั้นโจทย์บอกให้พิมพ์ข้อความแจ้งแล้วทำงานต่อ ซึ่งคือคำเตือนบน stderr ไม่ใช่การล้มเลิก" },
      { h: "รูปแบบไฟล์ผลลัพธ์ เป๊ะตามนี้" },
      { code: String.raw`<แถวเลขฐานสิบหก หนึ่งบรรทัดต่อหนึ่งแถวของช่อง>
<บรรทัดว่าง>
<พิกัดทางเข้า x,y>
<พิกัดทางออก x,y>
<เส้นทางสั้นที่สุดด้วยตัวอักษร N/E/S/W>`,
        cap: "ทุกบรรทัดลงท้ายด้วย \\n รวมบรรทัดสุดท้าย", lang: "text" },
      { code: String.raw`9c5c5c5c6
3a3a3a38a
...

0,0
19,14
EESSESSEE`,
        cap: "ตัวอย่างย่อ — หนึ่งหลักต่อหนึ่งช่อง", lang: "text" },
      { p: "เส้นทางที่สั้นที่สุดมาจาก **BFS** — นั่นคือสิ่งที่ทำให้มันสั้นที่สุด เพราะครั้งแรกที่ BFS ไปถึงทางออก มันไปถึงด้วยจำนวนก้าวที่น้อยที่สุดแล้ว" },
      { note: "ทดสอบเส้นทางด้วยการ **เดินตามมันจริง ๆ** ไม่ใช่ด้วยการเชื่อตัวแก้: เริ่มที่ทางเข้า ตรวจบิตผนังก่อนก้าวทุกครั้ง แล้วยืนยันว่าลงเอยที่ทางออก วิธีนี้จับบั๊กเรื่องเครื่องหมายทิศและ off-by-one ได้ทั้งกลุ่ม ซึ่งการเช็กแค่ความยาวจับไม่ได้เลย" },
      { h: "โครงไฟล์" },
      { code: String.raw`a_maze_ing.py          # อ่าน argv, อ่าน config, ประกอบทุกอย่าง
render.py              # ตาราง -> ข้อความ + ปุ่มโต้ตอบ
mazegen/
|-- __init__.py        # เปิด MazeGenerator กับค่าคงที่ทิศ
'-- generator.py       # ทั้งอัลกอริทึม ไม่รู้จักการวาดและไม่รู้จัก config
config.txt             # ไฟล์ตั้งค่าตัวอย่างที่ต้องมีใน repo
pyproject.toml         # แพ็ก mazegen เป็น wheel
Makefile               # install / run / debug / clean / lint`,
        cap: "generator ไม่เคยเห็นตัวอักษร render ไม่เคยเห็นบิต", lang: "text" },
      { p: "การแยกนี้ไม่ใช่ความสวยงาม — มันคือสิ่งที่ทำให้ **ทดสอบอัลกอริทึมได้โดยไม่ต้อง parse ข้อความ** เทสต์เรียกตัวสร้างแล้วตรวจกราฟตรง ๆ และเป็นข้อกำหนดของโจทย์ด้วยว่าส่วนสร้างต้องนำไปใช้ต่อได้" }
    ],

    dataflow: [
      { h: "เส้นทางของข้อมูลทั้งโปรแกรม" },
      { table: { head: ["ขั้น", "อินพุต", "เอาต์พุต"], rows: [
        ["อ่าน config", "พาธไฟล์จาก `sys.argv[1]`", "คู่ `KEY=VALUE` ที่แกะแล้ว"],
        ["ตรวจค่า", "ค่าที่อาจเป็นอะไรก็ได้", "ค่าที่ใช้ได้ หรือข้อความ error หนึ่งบรรทัด"],
        ["จองลาย 42", "ขนาดตาราง", "เซตของช่องที่ห้ามแตะ"],
        ["ขุด", "ขนาด, seed, เซตที่จอง", "ตารางของ int บิตผนัง"],
        ["เปิดผนังเพิ่ม", "ตาราง (เมื่อ `PERFECT=False`)", "ตารางที่มีวงรอบ แต่ยังไม่มีบล็อกโล่ง 3x3"],
        ["แก้", "ตาราง, ทางเข้า, ทางออก", "เส้นทางสั้นสุดเป็น N/E/S/W"],
        ["เขียนไฟล์", "ทุกอย่างข้างบน", "ไฟล์ตามรูปแบบเป๊ะ"],
        ["แสดงผล", "ตาราง + เส้นทาง", "ASCII หรือหน้าต่าง MLX"]
      ]}},
      { h: "แกะไฟล์ตั้งค่า" },
      { code: String.raw`def read_config(path: str) -> dict[str, str]:
    """อ่านคู่ KEY=VALUE ข้ามคอมเมนต์และบรรทัดว่าง"""
    values: dict[str, str] = {}
    with open(path, "r", encoding="utf-8") as handle:
        for number, line in enumerate(handle, start=1):
            text = line.strip()
            if not text or text.startswith("#"):
                continue
            if "=" not in text:
                raise MazeError(f"{path}:{number}: expected KEY=VALUE")
            key, _, value = text.partition("=")
            values[key.strip().upper()] = value.strip()
    return values`,
        cap: "บอกเลขบรรทัดใน error เสมอ — คนแก้ไฟล์ตั้งค่าจะได้ไม่ต้องเดา", lang: "python" },
      { table: { head: ["อินพุต", "ต้องเกิดอะไร"], rows: [
        ["ไม่ได้ส่งไฟล์ตั้งค่ามา", "บอกวิธีใช้ แล้วออกด้วยรหัสไม่เท่าศูนย์"],
        ["ไฟล์ไม่มีอยู่", "ข้อความหนึ่งบรรทัด ไม่ใช่ traceback"],
        ["ขาดคีย์บังคับ", "บอกว่าขาดคีย์ไหน"],
        ["`WIDTH=abc`", "บอกว่าคีย์ไหนค่าไหนผิด"],
        ["`WIDTH=0` หรือค่าติดลบ", "ปฏิเสธ ไม่ใช่สร้างตารางว่าง"],
        ["`ENTRY` เท่ากับ `EXIT`", "ปฏิเสธ — โจทย์บอกว่าต้องต่างกัน"],
        ["`EXIT` อยู่นอกตาราง", "ปฏิเสธพร้อมบอกขอบเขตที่ยอมรับ"],
        ["ตารางเล็กเกินใส่ลาย 42", "เตือนบน stderr แล้วสร้างต่อโดยไม่มีลาย"],
        ["ตารางใหญ่มาก", "ต้องไม่ชน recursion limit"]
      ]}},
      { h: "เขียนไฟล์ผลลัพธ์" },
      { code: String.raw`def to_output(self) -> str:
    """ประกอบเนื้อไฟล์ทั้งก้อนเป็นสตริงเดียว"""
    rows = ["".join(f"{cell:x}" for cell in row) for row in self.grid]
    entry_x, entry_y = self.entry
    exit_x, exit_y = self.exit
    return "\n".join(rows + [
        "",
        f"{entry_x},{entry_y}",
        f"{exit_x},{exit_y}",
        "".join(self.solve()),
    ]) + "\n"


def write(self, filename: str) -> None:
    """เขียนลงไฟล์ด้วย context manager"""
    with open(filename, "w", encoding="utf-8") as handle:
        handle.write(self.to_output())`,
        cap: "ประกอบเป็นสตริงก่อน แล้วเขียนครั้งเดียว — เทสต์ตรวจสตริงได้โดยไม่ต้องแตะดิสก์", lang: "python" },
      { h: "แสดงผลและปุ่มโต้ตอบ" },
      { p: "โจทย์ยอมรับทั้ง ASCII ในเทอร์มินัลและหน้าต่าง MiniLibX แต่ **ต้องมีปุ่มโต้ตอบ** อย่างน้อยตามนี้" },
      { table: { head: ["ต้องทำได้", "หมายเหตุ"], rows: [
        ["สร้างเขาวงกตใหม่แล้วแสดง", "เปลี่ยน seed แล้วเรียกตัวสร้างซ้ำ"],
        ["ซ่อน/แสดงเส้นทางสั้นสุด", "เก็บเส้นทางไว้แล้วสลับการวาด ไม่ต้องคำนวณใหม่"],
        ["เปลี่ยนสีผนัง", "ในเทอร์มินัลใช้รหัสสี ANSI ได้"],
        ["(ตัวเลือก) ให้ลาย 42 มีสีของตัวเอง", "ต้องรู้ว่าช่องไหนคือช่องที่จองไว้"]
      ]}},
      { p: "การแสดงผลต้องเห็น **ผนัง ทางเข้า ทางออก และเส้นทาง** ได้ชัด — เก็บเซตช่องที่จองไว้เป็นข้อมูลสาธารณะของตัวสร้าง ไม่งั้นตัววาดจะระบายสีลาย 42 ไม่ได้" },
      { code: String.raw`+---+---+---+
| E         |     E = ทางเข้า  X = ทางออก
+---+   +---+     . = เส้นทางสั้นสุด
|   | . . X |
+   +---+---+`,
        cap: "หนึ่งช่องวาดขอบบนกับซ้ายของตัวเอง แล้วปิดท้ายด้วยขอบขวาและขอบล่าง", lang: "text" }
    ],

    implementation: [
      { h: "โจทย์บังคับว่าส่วนสร้างต้องเป็นคลาส" },
      { p: "ข้อกำหนดเขียนไว้ตรง ๆ ว่าให้ทำ **คลาสเดียว** เช่น `MazeGenerator` ในโมดูลเดี่ยวที่ import ไปใช้ในโปรเจกต์ถัดไปได้ พร้อมเอกสารสั้น ๆ บอกวิธีสร้างอินสแตนซ์ ส่งพารามิเตอร์ และอ่านทั้งโครงสร้างกับคำตอบ" },
      { code: String.raw`from mazegen import MazeGenerator

gen = MazeGenerator(width=20, height=15, seed=7, perfect=True)
gen.generate()

print(gen.grid[0][0])          # int ที่มีบิตผนังสี่บิต
print("".join(gen.solve()))    # เส้นทางสั้นสุดเป็น N/E/S/W
print(gen.is_perfect())        # ตรวจตัวเองได้
gen.write("maze.txt")          # เขียนตามรูปแบบของโจทย์`,
        cap: "หน้าตาที่เอกสารของโมดูลต้องอธิบายได้", lang: "python" },
      { p: "สถานะอยู่ในอินสแตนซ์ ไม่ใช่ในตัวแปรระดับโมดูล — สองเขาวงกตจึงอยู่ในโปรแกรมเดียวกันได้ ซึ่งเป็นสิ่งที่ปุ่ม 'สร้างใหม่' ต้องใช้" },
      { h: "แพ็กเป็น wheel" },
      { code: String.raw`[project]
name = "mazegen"
version = "1.0.0"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["mazegen"]`,
        cap: "pyproject.toml", lang: "toml" },
      { code: String.raw`python3 -m pip install build
python3 -m build --wheel
python3 -m pip install dist/mazegen-1.0.0-py3-none-any.whl

cd /tmp && python3 -c "from mazegen import MazeGenerator; print(MazeGenerator(5, 5, seed=1))"`,
        cap: "บรรทัดสุดท้ายคือการพิสูจน์จริง — import ได้จากไดเรกทอรีอื่น", lang: "bash" },
      { note: "โจทย์บอกว่าไฟล์ wheel หรือ sdist ต้องอยู่ที่ **รากของ repository** พร้อมเอกสารการใช้งาน ไม่ใช่แค่โฟลเดอร์ที่บังเอิญ import ได้เพราะอยู่ข้าง ๆ" },
      { h: "เทสต์ที่พิสูจน์ perfect maze ด้วยคณิตศาสตร์" },
      { p: "ตารางที่มี n ช่อง เป็นต้นไม้ก็ต่อเมื่อมี edge เท่ากับ `n − 1` และเชื่อมถึงกันหมด สองข้อนี้รวมกันคือคำนิยาม จึงไม่ต้องตรวจอะไรเพิ่มอีก" },
      { code: String.raw`def edges_removed(grid: list[list[int]]) -> int:
    """นับผนังที่ถูกทุบ นับคู่ละครั้ง"""
    total = 0
    for y, row in enumerate(grid):
        for x, cell in enumerate(row):
            if not cell & EAST and x + 1 < len(row):
                total += 1
            if not cell & SOUTH and y + 1 < len(grid):
                total += 1
    return total


for width, height in ((1, 1), (1, 9), (9, 1), (13, 7), (40, 40)):
    gen = MazeGenerator(width, height, seed=7, perfect=True)
    gen.generate()
    open_cells = width * height - len(gen.pattern_cells())
    assert gen.count_reachable() == open_cells      # เชื่อมถึงกันหมด
    assert gen.is_perfect()                          # และไม่มีวงรอบ`,
        cap: "ช่องที่จองไว้ไม่นับ เพราะโจทย์ยกเว้นลาย 42 ไว้", lang: "python" },
      { h: "เทสต์ที่จับบั๊กได้จริงมากที่สุด" },
      { code: String.raw`# 1) ผนังต้องตรงกันทั้งสองฝั่ง — สิ่งเดียวที่ตัวตรวจของโจทย์ดู
for y in range(height):
    for x in range(width - 1):
        assert bool(grid[y][x] & EAST) == bool(grid[y][x + 1] & WEST)

# 2) เดินตามเส้นทางจริง แทนที่จะเชื่อตัวแก้
x, y = entry
for step in solution:
    direction = {"N": NORTH, "E": EAST, "S": SOUTH, "W": WEST}[step]
    assert not grid[y][x] & direction          # ผนังต้องเปิดก่อนก้าว
    dx, dy = STEP[direction]
    x, y = x + dx, y + dy
assert (x, y) == exit_cell

# 3) seed เดิมได้ผลเดิม seed ต่างได้ผลต่าง
assert build(20, 20, seed=42) == build(20, 20, seed=42)
assert build(20, 20, seed=42) != build(20, 20, seed=43)

# 4) ไม่มีบล็อกโล่ง 3x3 แม้ตอน PERFECT=False
assert not has_open_block(grid, 3)`,
        cap: "สี่ข้อนี้ครอบคลุมข้อกำหนดที่ผู้ตรวจจะลองจริง", lang: "python" },
      { table: { head: ["ชั้น", "ตรวจอะไร"], rows: [
        ["**NORMAL**", "ขนาดปกติสร้างได้ ไฟล์ผลลัพธ์ถูกรูปแบบ สคริปต์ตรวจของโจทย์ผ่าน ทุกเป้าหมายใน Makefile ทำงาน"],
        ["**EXTREME**", "`1x1`, แถวเดียว, คอลัมน์เดียว, ค่าศูนย์และติดลบ, ทางเข้าเท่าทางออก, พิกัดนอกตาราง, ไฟล์ตั้งค่าหาย, ค่าที่ไม่ใช่ตัวเลข, ตารางเล็กกว่า 11x9"],
        ["**HARDCORE**", "spanning tree ทุกขนาด, ผนังตรงกันสองฝั่ง, ไม่มีบล็อกโล่ง 3x3 ทั้งสองโหมด, เดินตามเส้นทางแล้วถึงจริง, ลาย 42 ปิดครบและไม่ตัดใครออก, `200x200` ไม่ชน recursion limit, wheel ติดตั้งแล้ว import ได้จากที่อื่น, flake8 กับ `mypy --strict`"]
      ]}}
    ],

    tricks: [
      { h: "อาการ → สาเหตุ" },
      { table: { head: ["อาการ", "สาเหตุ"], rows: [
        ["สคริปต์ตรวจบอก 'Wrong encoding for (x,y)'", "มีที่ไหนสักแห่งเปลี่ยนบิตผนังข้างเดียว รวมทุกการเปลี่ยนไว้ในฟังก์ชันเดียว"],
        ["`RecursionError` ตอนตารางใหญ่", "DFS เขียนแบบเรียกซ้ำ ใช้ stack ที่จับต้องได้แทน"],
        ["เขาวงกตมีวงรอบทั้งที่ `PERFECT=True`", "ทุบผนังไปหาช่องที่ `visited` แล้ว เช็กก่อนเลือกเพื่อนบ้าน"],
        ["มีช่องเดินไปไม่ถึง", "ลาย 42 ถูกปิดหลังขุด แทนที่จะจองก่อนขุด"],
        ["ลาย 42 ขังผู้ใช้", "ทางเข้าหรือทางออกอยู่ในเซตที่จอง — เอาออกก่อน"],
        ["มีบล็อกโล่ง 3x3", "เปิดผนังเพิ่มโดยไม่ตรวจก่อน — ตรวจตอนลอง ไม่ใช่ตอนจบ"],
        ["เส้นทางไม่ใช่เส้นที่สั้นที่สุด", "ใช้ DFS แก้แทน BFS"],
        ["เส้นทางเดินทะลุผนัง", "ตอนแก้ไม่ได้ตรวจบิตผนังก่อนก้าว"],
        ["seed เดิมได้ผลไม่เหมือนเดิม", "ใช้ `random.seed()` ระดับโมดูล ใช้ `random.Random(seed)` ของตัวเอง"],
        ["ทุกแถวเหมือนกันหมด", "`[[15] * w] * h` — ทุกแถวเป็นลิสต์เดียวกัน ใช้ list comprehension"],
        ["`import mazegen` ไม่ได้หลังติดตั้ง", "`pyproject.toml` ไม่ได้ระบุ package หรือลืม `__init__.py`"],
        ["`make` ฟ้อง `missing separator`", "บรรทัดคำสั่งใช้ space ไม่ใช่ tab"]
      ]}},
      { h: "ทริคที่ควรจำ" },
      { ul: [
        "`[[0] * w for _ in range(h)]` เท่านั้น — `[[0] * w] * h` สร้างแถวเดียวที่ถูกอ้างถึง h ครั้ง เป็นบั๊กที่ตรวจยากเพราะพิมพ์ออกมาแล้วดูถูก",
        "เก็บ `visited` และเซตที่จองเป็น set ของ tuple — การเช็กสมาชิกเป็น O(1)",
        "เขียน `OPPOSITE` กับ `STEP` เป็น dict แทนการคำนวณด้วยเลขคณิตบิต อ่านง่ายกว่าและผิดยากกว่า",
        "ให้ `to_output()` คืนสตริง แล้วให้ `write()` เป็นคนเขียนไฟล์ — เทสต์ตรวจรูปแบบได้โดยไม่ต้องแตะดิสก์",
        "ตรวจอินพุตที่ขอบเดียวคือตอนอ่าน config แล้วส่วนที่เหลือของโปรแกรมไม่ต้องเช็กอะไรอีก",
        "รันสคริปต์ตรวจที่โจทย์ให้มากับผลลัพธ์ของตัวเองทุกครั้งก่อนส่ง — มันคือตัวเดียวกับที่ Moulinette อาจใช้"
      ]}
    ],

    eval: [
      { qa: [
        { q: "perfect maze คืออะไร และเกี่ยวอะไรกับ spanning tree", a: "perfect maze คือเขาวงกตที่มีทางเดียวระหว่างทางเข้ากับทางออก ซึ่งคือคำนิยามของต้นไม้บนกราฟ ช่องคือ node ผนังที่ถูกทุบคือ edge ตารางที่มี n ช่องจึงต้องมี edge เท่ากับ n−1 และการค้นหาครั้งเดียวต้องไปถึงทุกช่อง" },
        { q: "ทำไมกฎห้ามพื้นที่โล่ง 3x3 ถึงแทบไม่ต้องทำอะไรเพิ่มเมื่อ PERFECT=True", a: "เพราะบล็อก 2x2 ที่เปิดผนังในครบสี่ด้านคือวงรอบ และ spanning tree ไม่มีวงรอบ เขาวงกตสมบูรณ์แบบจึงมีแม้แต่บล็อกโล่ง 2x2 ไม่ได้ กฎนี้จึงถูกละเมิดได้เฉพาะตอนเปิดผนังเพิ่มในโหมดไม่สมบูรณ์แบบ" },
        { q: "ทำไมต้องจองช่องลาย 42 ก่อนขุด แทนที่จะปิดทีหลัง", a: "เพราะการปิดทีหลังจะตัดขาดช่องที่เดินถึงได้ผ่านช่องพวกนั้นเท่านั้น ซึ่งโจทย์ห้ามไม่ให้มีช่องโดดเดี่ยว ส่วนการจองก่อนทำให้การค้นหาไม่เคยเดินเข้าไป ความเชื่อมต่อจึงถูกรักษาไว้โดยโครงสร้างไม่ใช่โดยการตรวจ" },
        { q: "ทำไมเลือก DFS แบบวนลูปแทนแบบเรียกซ้ำ", a: "เพราะความลึกของการเรียกเท่ากับความยาวเส้นทางที่ยาวที่สุด ตารางใหญ่จึงชน recursion limit ที่ 1000 ได้ การย้าย stack มาไว้ใน list ทำให้ความลึกถูกจำกัดด้วยหน่วยความจำแทน โดยอัลกอริทึมเหมือนเดิมทุกอย่าง" },
        { q: "ทำไมต้องทุบผนังทั้งสองฝั่ง", a: "เพราะช่องสองช่องเก็บผนังระหว่างกันคนละชุด ถ้าทุบข้างเดียว ตัวตรวจที่โจทย์ให้มาจะฟ้องทันทีว่าการเข้ารหัสไม่สอดคล้อง และตัววาดกับตัวเดินจะเห็นเขาวงกตคนละอัน" },
        { q: "ทำไมเส้นทางต้องหาด้วย BFS", a: "เพราะ BFS ขยายทีละชั้นความลึก ครั้งแรกที่มันไปถึงทางออกจึงเป็นเส้นทางที่ใช้จำนวนก้าวน้อยที่สุด ส่วน DFS หาเส้นทางเจอเหมือนกันแต่ไม่รับประกันว่าสั้นที่สุด" },
        { q: "seed มีไว้ทำไม และทำไมไม่ใช้ `random.seed()`", a: "seed ทำให้ผลลัพธ์ทำซ้ำได้ ซึ่งโจทย์บังคับ และจำเป็นทั้งกับเทสต์และการอธิบายบั๊ก ส่วน `random.seed()` เปลี่ยนสถานะร่วมของทั้งโปรแกรม โค้ดส่วนอื่นที่สุ่มด้วยจะได้ผลเปลี่ยนไปด้วย การใช้ `random.Random(seed)` ทำให้สถานะเป็นของเราคนเดียว" },
        { q: "ทำไมส่วนสร้างต้องเป็นคลาสในโมดูลแยก", a: "เพราะโจทย์บังคับให้นำกลับมาใช้ในโปรเจกต์อื่นได้ คลาสเก็บสถานะไว้ในอินสแตนซ์ จึงมีเขาวงกตสองอันในโปรแกรมเดียวได้ ซึ่งปุ่มสร้างใหม่ต้องใช้ และการติดตั้ง wheel แล้ว import ได้จากไดเรกทอรีอื่นคือหลักฐานว่ามันเป็นโมดูลจริง" },
        { q: "ถ้าตารางเล็กเกินกว่าจะใส่ลาย 42 ต้องทำอย่างไร", a: "โจทย์บอกให้พิมพ์ข้อความแจ้งทางคอนโซลแล้วสร้างเขาวงกตต่อ ไม่ใช่ล้มเลิก ขนาดเล็กที่สุดที่ใส่ได้คือ 11x9 เมื่อใช้ตัวอักษร 5x3 พร้อมคอลัมน์คั่นและขอบสองช่อง" },
        { q: "ไฟล์ผลลัพธ์มีอะไรบ้าง", a: "แถวของเลขฐานสิบหกหนึ่งหลักต่อหนึ่งช่อง หนึ่งบรรทัดต่อหนึ่งแถว ตามด้วยบรรทัดว่าง แล้วสามบรรทัดคือพิกัดทางเข้า พิกัดทางออก และเส้นทางสั้นที่สุดด้วยตัวอักษร N E S W ทุกบรรทัดลงท้ายด้วยขึ้นบรรทัดใหม่" }
      ]}
    ]
  }
});
