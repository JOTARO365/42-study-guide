/* Production ML — serving, monitoring, drift, rollout, cost */
window.TEACHING_DATA = window.TEACHING_DATA || [];
window.TEACHING_EN = window.TEACHING_EN || {};

window.TEACHING_DATA.push({
  id: "ml_prod",
  name: "นำไปใช้จริง — จาก notebook สู่ระบบที่คนใช้งานอยู่",
  nameEn: "Production — From Notebook to a System People Depend On",
  titleShort: { th: "นำไปใช้จริง", en: "Production ML" },
  tag: {
    th: "โมเดลที่คะแนนดีใน notebook กับโมเดลที่ทำเงินได้จริง ต่างกันตรงที่ไม่มีอะไรในเก้าหน้าก่อนหน้าพูดถึง — training-serving skew, ข้อมูลที่เปลี่ยนไปเงียบ ๆ, label ที่มาช้าสามเดือน, และการปล่อยของที่ถอยกลับได้",
    en: "The gap between a model that scores well in a notebook and one that earns its keep is made of things the previous nine pages never mentioned — training-serving skew, data that shifts silently, labels that arrive three months late, and a rollout you can reverse"
  },
  accent: "#f59e0b",
  sections: {
    principle: [
      { h: "ช่องว่างที่เก้าหน้าก่อนหน้าไม่ได้พูดถึง" },
      { table: { head: ["ใน notebook", "ใน production"], rows: [
        ["ข้อมูลนิ่ง อยู่ในไฟล์เดียว", "**ข้อมูลไหลเข้ามาตลอดและเปลี่ยนรูปไปเรื่อย ๆ**"],
        ["รู้คำตอบทันทีจากชุด test", "**label มาช้าเป็นสัปดาห์หรือเป็นเดือน**"],
        ["ทำนายกี่วินาทีก็ได้", "มี p99 latency ที่ต้องไม่เกิน"],
        ["ผิดแล้วรันใหม่", "**ผิดแล้วมีคนได้รับผลกระทบจริง**"],
        ["โค้ดเตรียมข้อมูลอยู่ใน cell", "โค้ดเดียวกันต้องรันได้ทั้งตอนเทรนและตอนเสิร์ฟ"]
      ]}},
      { h: "สาเหตุที่โมเดลพังใน production เรียงตามความถี่" },
      { code: String.raw`1. training-serving skew      โค้ดเตรียมข้อมูลสองชุดไม่ตรงกัน
2. data drift                 input เปลี่ยนไปโดยไม่มีใครสังเกต
3. feature หาย/เปลี่ยนเงียบ ๆ   ต้นน้ำแก้ schema โดยไม่บอก
4. concept drift              ความสัมพันธ์ระหว่าง X กับ y เปลี่ยน
5. โมเดลผิดจริง ๆ              ← อยู่ท้ายสุด และพบน้อยที่สุด`,
        cap: "**ข้อ 5 คือสิ่งที่เก้าหน้าก่อนหน้าสอน ส่วนข้อ 1-4 คือเนื้อหาของหน้านี้ทั้งหมด**", lang: "txt" },
      { h: "คำถามที่ต้องตอบก่อนเขียนโค้ดบรรทัดแรก" },
      { ul: [
        "**การทำนายนี้จะไปเปลี่ยนการตัดสินใจอะไร** — ถ้าตอบไม่ได้ ไม่ต้องทำ",
        "**ผิดแล้วเสียหายเท่าไร** และผิดแบบไหนเจ็บกว่ากัน (false positive เทียบ false negative)",
        "**label ของจริงจะรู้เมื่อไร** — วันเดียว สัปดาห์ หรือสามเดือน",
        "**ต้องตอบใน latency เท่าไร** และปริมาณเท่าไรต่อวินาที",
        "**baseline ที่ไม่ใช้ ML ทำได้แค่ไหน** — กฎง่าย ๆ มักชนะและดูแลถูกกว่ามาก"
      ]},
      { note: "**คำถามข้อสุดท้ายฆ่าโครงการได้จำนวนมาก และควรฆ่า** — ถ้ากฎ if-else สามข้อได้ 90% ของประโยชน์ ML ที่ได้เพิ่ม 3% แต่ต้องมีทีมดูแลตลอดชีพ มักไม่คุ้ม" },
      { h: "หน้านี้จะทำให้ทำได้" },
      { ul: [
        "ระบุและกัน training-serving skew ด้วยโครงสร้าง ไม่ใช่ด้วยวินัย",
        "ตั้ง monitoring ที่จับปัญหาได้ก่อนที่ label จะมาถึง",
        "คำนวณ PSI ด้วยมือและอ่านค่ามันออก",
        "ปล่อยโมเดลใหม่แบบ shadow → canary → A/B พร้อมเงื่อนไข rollback ที่เขียนไว้ล่วงหน้า",
        "คำนวณ latency budget และต้นทุนต่อพันคำขอได้"
      ]}
    ],

    theory: [
      { p: "หมวดนี้ไล่ปัญหาของ production ทีละข้อ พร้อมกลไกที่ใช้แก้" },
      { h: "1) training-serving skew — ปัญหาอันดับหนึ่ง" },
      { code: String.raw`ตอนเทรน (Python + pandas):
   age_bucket = pd.cut(age, [0, 25, 40, 60, 200])

ตอนเสิร์ฟ (Java, เขียนใหม่โดยอีกทีม):
   if (age < 25) bucket = 0;         ← ขอบเขตไม่ตรงกัน!

โมเดลเห็น feature คนละความหมายกับที่เทรนมา`,
        cap: "ไม่มี error ใด ๆ แจ้ง มีแต่ความแม่นยำที่ต่ำกว่าตอนทดสอบโดยอธิบายไม่ถูก", lang: "txt" },
      { table: { head: ["วิธีแก้", "ได้ผลแค่ไหน"], rows: [
        ["ตรวจโค้ดให้ตรงกันด้วยสายตา", "**ไม่ได้ผล** — จะเพี้ยนอีกในสามเดือน"],
        ["ใช้โค้ดเตรียมข้อมูลชุดเดียวกันทั้งสองทาง", "**ได้ผลจริง** — เป็นทางแก้เชิงโครงสร้าง"],
        ["**feature store**", "ได้ผลที่สุด — คำนวณครั้งเดียว ใช้ทั้งเทรนและเสิร์ฟ"],
        ["log feature ที่ใช้จริงตอนเสิร์ฟ แล้วเอามาเทรนรอบถัดไป", "ตัดปัญหาที่ต้นเหตุเลย"]
      ]}},
      { h: "2) online เทียบ batch prediction" },
      { table: { head: ["", "batch", "online"], rows: [
        ["ทำเมื่อไร", "ตามเวลา เช่นทุกคืน", "เมื่อมีคำขอเข้ามา"],
        ["latency", "ไม่สำคัญ", "**สำคัญที่สุด**"],
        ["ความซับซ้อน", "**ต่ำมาก** — แค่ cron กับตาราง", "สูง — ต้องมี service, monitoring, autoscaling"],
        ["เหมาะกับ", "จัดอันดับลูกค้า · แนะนำสินค้าประจำวัน", "ตรวจจับฉ้อโกง · จัดอันดับผลค้นหา"]
      ]}},
      { note: "**เริ่มด้วย batch เสมอถ้าเป็นไปได้** — ถูกกว่าหลายเท่า ดีบักง่ายกว่ามาก และงานจำนวนมากไม่ได้ต้องการ online จริง ๆ แค่คนขอคิดว่าต้องการ" },
      { h: "3) data drift เทียบ concept drift" },
      { code: String.raw`data drift:     P(X) เปลี่ยน — input หน้าตาต่างจากเดิม
                เช่นกลุ่มลูกค้าใหม่เข้ามา ทำให้อายุเฉลี่ยลดลง

concept drift:  P(y|X) เปลี่ยน — ความสัมพันธ์เปลี่ยน
                เช่นพฤติกรรมฉ้อโกงแบบใหม่ที่ feature เดิมจับไม่ได้`,
        cap: "**data drift ตรวจได้ทันทีโดยไม่ต้องรอ label · concept drift ต้องรอ label ถึงจะรู้**", lang: "txt" },
      { h: "4) PSI — วัด data drift โดยไม่ต้องมี label" },
      { code: String.raw`PSI = Σ (actual_i − expected_i) · ln(actual_i / expected_i)

แบ่ง feature เป็นถัง (มักใช้ 10 ถังตาม decile ของชุดเทรน)
แล้วเทียบสัดส่วนในแต่ละถัง

PSI < 0.10   ไม่มีการเปลี่ยนที่มีนัย
0.10 - 0.25  เปลี่ยนปานกลาง — ควรตรวจสอบ
PSI > 0.25   เปลี่ยนมาก — ควรพิจารณาเทรนใหม่`,
        cap: "ใช้ได้ทันทีตั้งแต่วันแรกที่ deploy เพราะไม่ต้องรู้คำตอบจริง", lang: "txt" },
      { h: "5) label ที่มาช้า — ข้อจำกัดที่กำหนดทุกอย่าง" },
      { table: { head: ["งาน", "label มาเมื่อไร", "ผลที่ตามมา"], rows: [
        ["จัดอันดับผลค้นหา", "**ทันที** (คลิกหรือไม่คลิก)", "วัดผลจริงได้เกือบทันที"],
        ["ตรวจจับฉ้อโกง", "หลายวันถึงหลายสัปดาห์", "ต้องพึ่ง proxy metric ระหว่างรอ"],
        ["ทำนายการเลิกใช้บริการ", "**30-90 วัน**", "รู้ว่าโมเดลพังตอนที่สายไปแล้ว"],
        ["อนุมัติสินเชื่อ", "**หลายเดือนถึงหลายปี**", "และเห็นเฉพาะคนที่อนุมัติผ่าน — เป็น feedback loop"]
      ]}},
      { p: "**แถวสุดท้ายคือกับดักที่ร้ายที่สุด**: โมเดลปฏิเสธใครไป ก็ไม่มีวันรู้ว่าคนนั้นจะจ่ายคืนได้หรือไม่ ข้อมูลรอบถัดไปจึงมีแต่คนที่โมเดลเดิมอนุมัติ ทำให้โมเดลยืนยันอคติของตัวเองไปเรื่อย ๆ · ทางแก้คืออนุมัติแบบสุ่มไว้ส่วนน้อยเพื่อเก็บข้อมูลที่ไม่มีอคติ" },
      { h: "6) ลำดับการปล่อยของที่ถอยกลับได้" },
      { code: String.raw`1. shadow    รับ traffic จริงแต่ **ไม่ใช้ผลของมัน** — เทียบกับของเดิมเงียบ ๆ
2. canary    ให้ traffic 1-5% ตัดสินใจจริง พร้อม alert
3. A/B       แบ่งครึ่งวัดผลทางธุรกิจอย่างเป็นระบบ
4. rollout   ค่อย ๆ ขยายเป็น 100%

**ทุกขั้นต้องมีเงื่อนไข rollback เขียนไว้ก่อนเริ่ม**`,
        cap: "shadow จับ training-serving skew กับปัญหา latency ได้โดยไม่มีความเสี่ยงใด ๆ ต่อผู้ใช้เลย", lang: "txt" },
      { h: "7) metric ที่ต้องดูมีสามชั้น" },
      { table: { head: ["ชั้น", "ดูอะไร", "รู้ผลเมื่อไร"], rows: [
        ["**ระบบ**", "latency p50/p99 · error rate · throughput", "**ทันที**"],
        ["**โมเดล**", "การกระจายของ input · การกระจายของคำทำนาย · PSI", "**ทันที**"],
        ["**ธุรกิจ**", "รายได้ · conversion · การร้องเรียน", "ช้า แต่**เป็นตัวเดียวที่สำคัญจริง**"]
      ]}},
      { note: "**การกระจายของคำทำนายคือสัญญาณเตือนที่เร็วที่สุด** — ถ้าโมเดลเคยทำนาย 'เสี่ยง' 2% แล้ววันหนึ่งกลายเป็น 15% แปลว่ามีอะไรผิดแน่นอน โดยที่ยังไม่ต้องมี label สักตัว" },
      { h: "8) เทรนใหม่เมื่อไร" },
      { table: { head: ["เกณฑ์", "ข้อดี", "ข้อเสีย"], rows: [
        ["**ตามเวลา** เช่นทุกสัปดาห์", "คาดเดาได้ ง่ายต่อการวางแผน", "เทรนทั้งที่ไม่จำเป็น หรือช้าเกินไป"],
        ["**ตามการวัด** เช่น PSI เกิน 0.25", "เทรนเมื่อจำเป็นจริง", "ต้องมี monitoring ที่เชื่อถือได้ก่อน"],
        ["**ตามผลที่ตกลง**", "ตรงจุดที่สุด", "**ต้องรอ label ซึ่งอาจสายเกินไป**"]
      ]}},
      { h: "9) versioning สามอย่างที่ต้องผูกกัน" },
      { code: String.raw`code version    +    data version    +    model version
                                          ‾‾‾‾‾‾‾‾‾‾‾‾‾
                            ต้องย้อนกลับไปสร้างใหม่ได้ทุกตัว

ถ้าตอบไม่ได้ว่า "โมเดลที่รันอยู่ตอนนี้เทรนจากข้อมูลชุดไหน ด้วยโค้ดคอมมิตไหน"
แปลว่าย้อนกลับไม่ได้ และดีบักไม่ได้`,
        cap: "นี่คือเหตุผลที่ model registry มีอยู่ ไม่ใช่แค่ที่เก็บไฟล์", lang: "txt" },
      { h: "10) เมื่อไรไม่ควรใช้ ML" },
      { ul: [
        "**เขียนเป็นกฎได้ชัดเจน** — กฎอ่านออก แก้ได้ทันที และไม่ drift",
        "**ข้อมูลน้อยมาก** — ต่ำกว่าหลักพันตัวอย่างมักไม่พอ",
        "**ผิดแล้วรับไม่ได้เลย** — งานที่ต้องถูก 100% ไม่ควรใช้โมเดลความน่าจะเป็น",
        "**ไม่มีใครดูแลต่อ** — โมเดลที่ไม่มีคนดูแลจะเสื่อมลงเงียบ ๆ เสมอ",
        "**ไม่มี label และหามาไม่ได้** — ไม่มีทางวัดได้ว่าดีขึ้นหรือแย่ลง"
      ]}
    ],

    foundations: [
      { p: "หมวดนี้เจาะตัวเลขที่ตัดสินเรื่องการนำไปใช้จริง" },
      { h: "คำนวณ PSI ด้วยมือ" },
      { code: String.raw`feature หนึ่งตัว แบ่งเป็น 5 ถัง

ถัง   ตอนเทรน   ตอนนี้    (a−e)·ln(a/e)
 1      0.10      0.05      0.0347
 2      0.20      0.12      0.0409
 3      0.40      0.35      0.0067
 4      0.20      0.28      0.0269
 5      0.10      0.20      0.0693
                            ‾‾‾‾‾‾
                    PSI =   0.1784`,
        cap: "0.178 อยู่ในช่วง 0.10-0.25 คือเปลี่ยนปานกลาง — ยังไม่ต้องเทรนใหม่ทันที แต่ต้องหาสาเหตุ", lang: "txt" },
      { p: "**สังเกตว่าถังที่ 5 มีส่วนมากที่สุด** (0.0693 จาก 0.1784) เพราะสัดส่วนเพิ่มเป็นสองเท่า · PSI ไม่ได้บอกแค่ว่าเปลี่ยนหรือไม่ แต่บอกด้วยว่าเปลี่ยนตรงไหน ซึ่งเป็นเบาะแสว่าต้องไปดูอะไร" },
      { h: "latency budget ต้องแตกออกมาเป็นชิ้น" },
      { code: String.raw`เป้าหมาย p99 = 200 ms

network เข้า-ออก      20 ms   13%
ดึง feature           60 ms   40%    ← มักเป็นตัวใหญ่ที่สุด ไม่ใช่โมเดล
preprocess            15 ms   10%
model forward         45 ms   30%
postprocess + log     10 ms    7%
                     ‾‾‾‾‾‾
รวม                  150 ms   เหลือ headroom 50 ms`,
        cap: "**คนมักไปปรับโมเดลให้เร็วขึ้นทั้งที่คอขวดอยู่ที่การดึง feature** — วัดก่อนแล้วค่อยแก้", lang: "txt" },
      { note: "**ต้องดู p99 ไม่ใช่ค่าเฉลี่ย** — ค่าเฉลี่ย 50 ms ที่มี p99 เป็น 2 วินาที แปลว่าผู้ใช้หนึ่งในร้อยรอนานมาก และในระบบที่เรียกต่อกันหลายชั้น หางนั้นจะทบกันจนกลายเป็นปัญหาใหญ่" },
      { h: "batching เพิ่ม throughput ได้เท่าไร" },
      { code: String.raw`โมเดลเดียวกันบน GPU ตัวเดียว

batch  1:   45 ms ต่อคำขอ    →   22 req/s
batch  8:   73 ms ต่อชุด     →  110 req/s
batch 32:  169 ms ต่อชุด     →  189 req/s

throughput เพิ่ม 8.6 เท่า แต่ latency ต่อคำขอเพิ่ม 3.8 เท่า`,
        cap: "นี่คือการแลกกันตรง ๆ ระหว่าง latency กับ throughput — dynamic batching คือการหาจุดสมดุลอัตโนมัติ", lang: "txt" },
      { h: "ต้นทุนต่อพันคำขอ" },
      { code: String.raw`GPU ราคา $1.20 ต่อชั่วโมง · ที่ batch 32 ได้ 189 req/s

ต้นทุน = 1.20 / 3600 / 189 × 1000 = $0.0018 ต่อพันคำขอ

10 ล้านคำขอต่อวัน → $18 ต่อวัน → $540 ต่อเดือน`,
        cap: "คำนวณตัวเลขนี้ก่อนเริ่มเสมอ — บ่อยครั้งมันบอกได้เลยว่าควรใช้โมเดลเล็กลงหรือทำแบบ batch แทน", lang: "txt" },
      { h: "A/B test ต้องใช้ผู้ใช้กี่คน" },
      { code: String.raw`conversion เดิม 5% · ต้องการจับความต่าง · α = 0.05 · power = 80%

จับ +0.5 จุดเปอร์เซ็นต์ (5.0% → 5.5%)   → 31,199 คนต่อกลุ่ม
จับ +1.0 จุดเปอร์เซ็นต์ (5.0% → 6.0%)   →  8,149 คนต่อกลุ่ม

ที่ 10,000 คนต่อวันต่อกลุ่ม: 3.1 วัน เทียบกับ 0.8 วัน`,
        cap: "**ยิ่งอยากจับความต่างที่เล็กลง ยิ่งต้องใช้ตัวอย่างมากขึ้นแบบกำลังสอง** — ครึ่งหนึ่งของขนาด ต้องการสี่เท่าของจำนวน", lang: "txt" },
      { p: "**และถ้าคำนวณแล้วต้องใช้เวลาสามเดือนกว่าจะสรุปได้ นั่นคือคำตอบในตัวมันเอง** — ความต่างที่เล็กขนาดนั้นอาจไม่คุ้มกับการดูแลโมเดลที่ซับซ้อนขึ้น" },
      { h: "shadow deployment จับอะไรได้บ้าง" },
      { table: { head: ["จับได้", "จับไม่ได้"], rows: [
        ["**training-serving skew**", "ผลกระทบทางธุรกิจ"],
        ["latency จริงภายใต้ traffic จริง", "**feedback loop** (เพราะไม่มีใครเห็นผล)"],
        ["error และ edge case ที่ไม่เคยเจอ", "การเปลี่ยนพฤติกรรมของผู้ใช้"],
        ["**การกระจายของคำทำนายที่ต่างจากเดิม**", "—"]
      ]}},
      { h: "ความถี่ในการเทรนใหม่กับความเก่าของโมเดล" },
      { code: String.raw`เทรนใหม่ทุก  1 วัน   → โมเดลเก่าเฉลี่ย  0.5 วัน
เทรนใหม่ทุก  7 วัน   → โมเดลเก่าเฉลี่ย  3.5 วัน
เทรนใหม่ทุก 30 วัน   → โมเดลเก่าเฉลี่ย   15 วัน
เทรนใหม่ทุก 90 วัน   → โมเดลเก่าเฉลี่ย   45 วัน

เทียบกับความเร็วที่ข้อมูลเปลี่ยนจริงในโดเมนนั้น`,
        cap: "ในงานฉ้อโกงที่รูปแบบเปลี่ยนทุกสัปดาห์ การเทรนทุกไตรมาสแปลว่าโมเดลตามหลังอยู่เสมอ", lang: "txt" },
      { h: "feedback loop — ปัญหาที่มองไม่เห็นจาก metric" },
      { code: String.raw`ระบบแนะนำสินค้า:
   โมเดลแนะนำ A → คนคลิก A → ข้อมูลบอกว่า A ดี → โมเดลแนะนำ A มากขึ้น
   → สินค้าอื่นไม่เคยถูกแสดง → ไม่มีข้อมูลว่ามันดีหรือไม่

metric ดูดีขึ้นเรื่อย ๆ ทั้งที่ระบบแคบลงเรื่อย ๆ`,
        cap: "ทางแก้คือสอดแทรกการสุ่มไว้ส่วนน้อย เพื่อเก็บข้อมูลที่ไม่ถูกอคติของโมเดลเองปนเปื้อน", lang: "txt" }
    ],

    architecture: [
      { p: "หมวดนี้คือโครงของระบบและลำดับที่ควรสร้าง" },
      { h: "ลำดับที่ควรสร้างจริง" },
      { code: String.raw`1. baseline ที่ไม่ใช้ ML ขึ้น production ก่อน   ← ได้ท่อข้อมูลและการวัดผลครบ
2. โมเดลง่าย ๆ แทนที่ baseline นั้น
3. monitoring ให้ครบสามชั้น
4. ระบบเทรนใหม่อัตโนมัติ
5. ค่อยทำโมเดลให้ซับซ้อนขึ้น`,
        cap: "**ขั้นที่ 1 สำคัญที่สุดและถูกข้ามบ่อยที่สุด** — มันบังคับให้สร้างท่อข้อมูล การ deploy และการวัดผลจริงตั้งแต่ยังไม่มีโมเดล", lang: "txt" },
      { h: "โครงของระบบ online" },
      { code: String.raw`  คำขอ
    ↓
  [API gateway]  ← rate limit, auth
    ↓
  [feature service] ←──── feature store (online) ←── ท่อคำนวณ feature
    ↓                                                  ↓
  [model service]  ←──── model registry            feature store (offline)
    ↓                                                  ↓
  [postprocess + business rules]                   ระบบเทรน
    ↓                                                  ↓
  ตอบกลับ ──────────→ log ทั้ง feature และคำทำนาย ────┘`,
        cap: "**ลูกศรจากล่างขวากลับขึ้นไป** คือสิ่งที่ทำให้ระบบพัฒนาตัวเองได้ — log feature ที่ใช้จริง แล้วเอาไปเทรนรอบถัดไป", lang: "txt" },
      { h: "feature store แก้อะไร" },
      { table: { head: ["ปัญหา", "แก้ยังไง"], rows: [
        ["**training-serving skew**", "นิยาม feature ชุดเดียว ใช้ทั้งสองทาง"],
        ["**ทำซ้ำกัน**", "ทีมอื่นเอา feature เดิมไปใช้ได้เลย"],
        ["**point-in-time correctness**", "ดึงค่าที่ 'รู้ ณ เวลานั้น' เท่านั้น จึงกัน leakage ได้ในตัว"],
        ["latency", "ฝั่ง online เป็น key-value ที่ตอบในหลักมิลลิวินาที"]
      ]}},
      { note: "**point-in-time correctness คือคุณค่าที่สำคัญที่สุดและถูกพูดถึงน้อยที่สุด** — การ join ข้อมูลย้อนหลังโดยไม่ระวังเวลา ทำให้ feature มีข้อมูลที่ตอนนั้นยังไม่มีจริง ซึ่งคือ leakage ที่หน้าที่ 2 พูดถึง แต่เกิดในระดับโครงสร้างพื้นฐาน" },
      { h: "เกณฑ์ rollback ที่ต้องเขียนก่อนปล่อย" },
      { code: String.raw`ถอยกลับทันทีเมื่อ:
   error rate  > 1%
   p99 latency > 300 ms ติดต่อกัน 5 นาที
   การกระจายคำทำนายเปลี่ยนเกิน 3 เท่าของส่วนเบี่ยงเบนปกติ
   metric ธุรกิจตกเกิน 2% ในกลุ่มทดลอง

**เขียนเป็นโค้ดที่ทำงานอัตโนมัติ ไม่ใช่ข้อตกลงที่รอคนตัดสินใจตอนตีสาม**`,
        cap: "เกณฑ์ที่ต้องรอคนตัดสินใจ ในทางปฏิบัติแปลว่าไม่มีเกณฑ์", lang: "txt" },
      { h: "สิ่งที่ต้อง log ทุกคำขอ" },
      { code: String.raw`request_id · timestamp
feature ที่ใช้จริงทุกตัว        ← สำคัญที่สุด ใช้ทั้งดีบักและเทรนรอบหน้า
คำทำนายและความน่าจะเป็น
เวอร์ชันโมเดล
latency แยกตามขั้นตอน
การตัดสินใจสุดท้ายหลังผ่านกฎธุรกิจ`,
        cap: "**ถ้าไม่ log feature ที่ใช้จริง จะไม่มีทางย้อนกลับไปหาสาเหตุของคำทำนายผิดได้เลย**", lang: "txt" },
      { h: "ลด latency ตามลำดับผลตอบแทน" },
      { table: { head: ["วิธี", "ได้เท่าไร", "แลกอะไร"], rows: [
        ["**cache คำทำนายที่ซ้ำ**", "มากที่สุดถ้ามีของซ้ำเยอะ", "ข้อมูลเก่าไปนิดหน่อย"],
        ["**แก้คอขวดการดึง feature**", "มักได้มากที่สุดในทางปฏิบัติ", "ต้องแก้โครงสร้าง"],
        ["**dynamic batching**", "throughput หลายเท่า", "latency ต่อคำขอเพิ่ม"],
        ["**quantization (int8)**", "2-4 เท่า", "คุณภาพลดเล็กน้อย"],
        ["**distillation ไปโมเดลเล็ก**", "หลายเท่า", "ต้องเทรนเพิ่มและคุณภาพลด"],
        ["**ONNX / TensorRT**", "2-5 เท่า", "ขั้นตอน build เพิ่มขึ้น"]
      ]}},
      { h: "ทีมเล็กควรมีอะไรบ้าง" },
      { code: String.raw`ขั้นต่ำที่ยอมรับได้:
   ✓ โค้ดเตรียมข้อมูลชุดเดียวใช้ทั้งเทรนและเสิร์ฟ
   ✓ log feature และคำทำนายทุกคำขอ
   ✓ dashboard สามชั้น (ระบบ · โมเดล · ธุรกิจ)
   ✓ ผูก model version กับ git commit และ data snapshot
   ✓ ปุ่ม rollback ที่กดแล้วกลับไปเวอร์ชันก่อนได้ในนาทีเดียว

ยังไม่ต้องมี: feature store เต็มรูปแบบ · Kubernetes · การเทรนใหม่อัตโนมัติ`,
        cap: "ห้าข้อบนทำได้ด้วยเครื่องมือธรรมดาและได้ประโยชน์เกือบทั้งหมดของ MLOps เต็มรูปแบบ", lang: "txt" }
    ],

    dataflow: [
      { p: "เดินโครงการหนึ่งตั้งแต่คำถามทางธุรกิจจนถึงการเทรนใหม่รอบแรก" },
      { h: "โจทย์" },
      { code: String.raw`ร้านค้าออนไลน์ต้องการลดการคืนสินค้า
   ทำนายว่าคำสั่งซื้อนี้มีโอกาสถูกคืนสูงหรือไม่
   ถ้าสูง → เสนอให้ยืนยันไซซ์อีกครั้งก่อนชำระเงิน

ปริมาณ  50,000 คำสั่งซื้อต่อวัน
latency ต้องตอบใน 200 ms เพราะอยู่ในหน้าชำระเงิน
label   รู้หลัง 30 วัน (ครบกำหนดคืนสินค้า)`,
        cap: "**สังเกตว่า label มาช้า 30 วัน** — ข้อจำกัดนี้กำหนดทุกอย่างที่ตามมาในหน้านี้", lang: "txt" },
      { h: "ขั้นที่ 0 — baseline ที่ไม่ใช้ ML" },
      { code: String.raw`กฎ: ถ้าเป็นเสื้อผ้า และลูกค้าเคยคืนมาก่อน → ถือว่าเสี่ยง

ครอบคลุม 60% ของการคืนจริง · เตือนผิด 25% ของคำสั่งซื้อ
ขึ้น production ในสองวัน`,
        cap: "และตอนนี้ท่อข้อมูล การ deploy การ log และ dashboard มีครบแล้ว ก่อนจะมีโมเดลสักตัว", lang: "txt" },
      { h: "ขั้นที่ 1 — feature ที่ 'รู้ ณ เวลาสั่งซื้อ' เท่านั้น" },
      { code: String.raw`ใช้ได้:
   หมวดสินค้า · ราคา · จำนวนชิ้น
   จำนวนครั้งที่ลูกค้าคนนี้เคยคืน **ก่อนวันนี้**
   อัตราการคืนของสินค้าตัวนี้ **คำนวณจากข้อมูลก่อนวันนี้**

ใช้ไม่ได้ (leakage):
   อัตราการคืนของสินค้าตัวนี้จากข้อมูลทั้งหมด  ← มีอนาคตปนอยู่
   ลูกค้าติดต่อฝ่ายบริการหรือไม่                ← เกิดหลังการสั่งซื้อ`,
        cap: "สองบรรทัดล่างคือ leakage ที่หน้าที่ 2 อธิบาย และเป็นสาเหตุอันดับหนึ่งของ 'ดีใน notebook แต่พังจริง'", lang: "txt" },
      { h: "ขั้นที่ 2 — แบ่งข้อมูลตามเวลา" },
      { code: String.raw`train       มกราคม - กันยายน
validation  ตุลาคม
test        พฤศจิกายน

ห้ามสุ่มแบ่ง — เพราะตอนใช้จริงต้องทำนายอนาคตจากอดีตเสมอ
และต้องเว้นช่วง 30 วันก่อน validation เพราะ label ยังไม่ครบกำหนด`,
        cap: "**ประโยคสุดท้ายถูกลืมบ่อยที่สุด** — คำสั่งซื้อของปลายกันยายนยังไม่รู้ผลตอนตัดข้อมูล จึงต้องตัดออก", lang: "txt" },
      { h: "ขั้นที่ 3 — เทรนและเลือกเกณฑ์ตัดสิน" },
      { code: String.raw`gradient boosting · PR-AUC = 0.34  (baseline สุ่มได้ 0.05 เท่ากับอัตราการคืน)

เลือก threshold จากต้นทุนจริง ไม่ใช่จาก F1:
   เตือนผิด    → รบกวนลูกค้ากลางหน้าชำระเงิน ประเมิน 20 บาท
   พลาดการคืน  → ค่าขนส่งกลับ + รีสต๊อก      ประเมิน 150 บาท

threshold ที่ทำให้ต้นทุนรวมต่ำสุด = C_FP / (C_FP + C_FN)
                              = 20 / 170 = 0.12   ไม่ใช่ 0.5`,
        cap: "**0.5 ไม่เคยเป็นเกณฑ์ที่ถูกต้องโดยอัตโนมัติ** — มันถูกก็ต่อเมื่อความเสียหายสองด้านเท่ากันพอดี ซึ่งแทบไม่เคยเกิด", lang: "txt" },
      { h: "ขั้นที่ 4 — shadow หนึ่งสัปดาห์" },
      { code: String.raw`ผลที่พบ:
   ✗ p99 latency = 340 ms  เกินงบ 200 ms
     → ไล่ดูพบว่าการดึงประวัติลูกค้าใช้ 210 ms
     → แก้ด้วย cache ล่วงหน้า เหลือ 45 ms → p99 = 175 ms

   ✗ 3% ของคำขอมี feature หายเพราะลูกค้าใหม่ไม่มีประวัติ
     → ตอนเทรนใช้ค่าเฉลี่ยเติม แต่ตอนเสิร์ฟส่ง null → **training-serving skew**
     → แก้ให้ใช้โค้ดเติมค่าชุดเดียวกันทั้งสองทาง`,
        cap: "**ทั้งสองข้อไม่มีทางเจอใน notebook เลย** และทั้งคู่ถูกจับได้โดยไม่มีผู้ใช้คนไหนได้รับผลกระทบ", lang: "txt" },
      { h: "ขั้นที่ 5 — canary 5%" },
      { code: String.raw`ปล่อย 5% ของ traffic เป็นเวลา 3 วัน พร้อมเงื่อนไขถอยกลับอัตโนมัติ

เฝ้าดู:
   error rate            0.2%  ปกติ
   p99 latency           178 ms  ผ่าน
   สัดส่วนที่ถูกเตือน      11%  ใกล้เคียงที่คาดไว้ (12%)
   อัตราการละทิ้งตะกร้า   ไม่ต่างจากกลุ่มควบคุมอย่างมีนัย`,
        cap: "แถวสุดท้ายคือสิ่งที่กลัวที่สุด — การเตือนอาจทำให้คนเลิกซื้อ ซึ่งแย่กว่าการคืนสินค้าเสียอีก", lang: "txt" },
      { h: "ขั้นที่ 6 — A/B ที่คำนวณขนาดไว้ล่วงหน้า" },
      { code: String.raw`อัตราการคืนเดิม 5% · ต้องการจับการลดลง 0.5 จุดเปอร์เซ็นต์

ขนาดตัวอย่างที่ต้องใช้ = 31,199 คำสั่งซื้อต่อกลุ่ม
ที่ 25,000 ต่อกลุ่มต่อวัน → 1.3 วันในการสะสมตัวอย่าง
แต่ต้องรออีก 30 วันให้ label ครบกำหนด

→ สรุปผลได้จริงในวันที่ 32`,
        cap: "**บรรทัดรองสุดท้ายคือสิ่งที่คนวางแผนลืมเสมอ** — การสะสมตัวอย่างเสร็จไม่ได้แปลว่าวัดผลได้", lang: "txt" },
      { h: "ขั้นที่ 7 — สองเดือนต่อมา monitoring ส่งสัญญาณ" },
      { code: String.raw`PSI ของ feature "หมวดสินค้า" = 0.31        ← เกิน 0.25

ไล่ดูพบว่า: ร้านเพิ่งเปิดหมวดรองเท้าซึ่งไม่มีในข้อมูลเทรนเลย
คำทำนายในหมวดนั้นแทบสุ่ม แต่ metric รวมยังดูดีเพราะเป็นแค่ 4% ของคำสั่งซื้อ`,
        cap: "**metric รวมกลบปัญหาของกลุ่มย่อยได้เสมอ** — จึงต้องแยกดูตามกลุ่มที่สำคัญด้วย ไม่ใช่ดูแต่ตัวเลขเดียว", lang: "txt" },
      { h: "ขั้นที่ 8 — เทรนใหม่และปล่อยรอบสอง" },
      { code: String.raw`เทรนด้วยข้อมูลที่รวมหมวดใหม่แล้ว
ใช้ feature ที่ log ไว้จากตอนเสิร์ฟจริง ไม่ใช่คำนวณใหม่จาก data warehouse
   → ตัดโอกาสเกิด skew ที่ต้นเหตุ

แล้ววน shadow → canary → A/B เหมือนเดิม
**ทุกรอบใช้ขั้นตอนเดียวกัน ไม่มีข้อยกเว้นสำหรับ "แก้นิดเดียว"**`,
        cap: "ประโยคสุดท้ายคือกฎที่ช่วยไว้ได้มากที่สุด — เหตุขัดข้องส่วนใหญ่มาจากการเปลี่ยนแปลงที่ 'เล็กจนไม่ต้องทดสอบ'", lang: "txt" }
    ],

    implementation: [
      { p: "โค้ดของสิ่งที่หน้านี้พูดถึง เน้นสิ่งที่ทีมเล็กทำได้จริง" },
      { h: "1) โค้ดเตรียมข้อมูลชุดเดียวใช้ทั้งสองทาง" },
      { code: String.raw`# features.py — ไฟล์เดียว ทั้งเทรนและเสิร์ฟ import จากที่นี่
from dataclasses import dataclass

AGE_BINS = [0, 25, 40, 60, 200]
CATEGORY_DEFAULT = "unknown"

def build_features(raw: dict) -> dict:
    """ต้องรับ dict เดียว ทำงานได้ทั้งกับหนึ่งแถวและกับ batch"""
    return {
        "age_bucket": _bucket(raw.get("age"), AGE_BINS),
        "price_log": math.log1p(raw.get("price", 0.0)),
        "category": raw.get("category") or CATEGORY_DEFAULT,
        "prior_returns": raw.get("prior_returns", 0),
    }

# training:  df.apply(build_features, axis=1)
# serving:   build_features(request_payload)`,
        cap: "**ฟังก์ชันเดียว ไฟล์เดียว import ทั้งสองทาง** — นี่คือการกัน skew เชิงโครงสร้าง ไม่ใช่ด้วยวินัย", lang: "python" },
      { h: "2) ทดสอบว่าเทรนกับเสิร์ฟให้ผลตรงกัน" },
      { code: String.raw`def test_train_serve_parity():
    rows = load_sample_rows(200)

    batch = pd.DataFrame([build_features(r) for r in rows])       # เส้นทางเทรน
    single = [build_features(r) for r in rows]                     # เส้นทางเสิร์ฟ

    for i, row in enumerate(single):
        for k, v in row.items():
            assert batch.iloc[i][k] == v, f"skew ที่ {k} แถว {i}"

def test_serving_matches_recorded_predictions():
    """ยิง payload ที่ log ไว้จริงเข้าโมเดลอีกครั้ง ต้องได้ค่าเดิม"""
    for rec in load_production_logs(500):
        assert abs(model.predict_one(rec["features"]) - rec["prediction"]) < 1e-6`,
        cap: "**การทดสอบตัวที่สองมีค่ามากที่สุด** — มันจับได้ทั้ง skew, การเปลี่ยนเวอร์ชันไลบรารี และการ deploy โมเดลผิดตัว", lang: "python" },
      { h: "3) คำนวณ PSI" },
      { code: String.raw`import numpy as np

def psi(expected, actual, bins=10):
    edges = np.percentile(expected, np.linspace(0, 100, bins + 1))
    edges[0], edges[-1] = -np.inf, np.inf

    e = np.histogram(expected, edges)[0] / len(expected)
    a = np.histogram(actual, edges)[0] / len(actual)

    e = np.clip(e, 1e-6, None)         # กันหารด้วยศูนย์
    a = np.clip(a, 1e-6, None)
    return float(np.sum((a - e) * np.log(a / e)))

# psi < 0.10  ปกติ · 0.10-0.25 ควรตรวจสอบ · > 0.25 ควรเทรนใหม่`,
        cap: "**ขอบถังต้องมาจากชุดเทรนและต้องแช่ไว้** — ถ้าคำนวณขอบใหม่จากข้อมูลปัจจุบันทุกครั้ง สัดส่วนปัจจุบันจะเป็น 10% ทุกถังโดยนิยาม และถ้าเทียบกับ 10% ที่เป็นค่าคาดหวังตามชื่อ decile ก็จะได้ PSI = 0 เสมอ ซึ่งเป็นท่อที่พังแบบพบบ่อยที่สุด", lang: "python" },
      { h: "4) log ที่ใช้ดีบักได้จริง" },
      { code: String.raw`import json, time, uuid

def log_prediction(features, pred, model_version, latencies, decision):
    record = {
        "request_id": str(uuid.uuid4()),
        "ts": time.time(),
        "model_version": model_version,
        "features": features,          # ← ค่าที่โมเดลเห็นจริง ไม่ใช่ค่าดิบ
        "prediction": float(pred),
        "decision": decision,          # หลังผ่านกฎธุรกิจแล้ว
        "latency_ms": latencies,       # แยกตามขั้นตอน
    }
    logger.info(json.dumps(record))
    return record["request_id"]`,
        cap: "`features` ต้องเป็นค่าหลังแปลงแล้ว ไม่ใช่ payload ดิบ — ไม่งั้นย้อนกลับไปหาสาเหตุของคำทำนายผิดไม่ได้", lang: "python" },
      { h: "5) ผูก model version กับ code และ data" },
      { code: String.raw`import subprocess, hashlib, json, datetime

def build_model_card(model, X_train, params, metrics):
    return {
        "model_version": datetime.datetime.utcnow().strftime("%Y%m%d_%H%M%S"),
        "git_commit": subprocess.check_output(
            ["git", "rev-parse", "HEAD"]).decode().strip(),
        "git_dirty": bool(subprocess.check_output(["git", "status", "--porcelain"])),
        "data_hash": hashlib.sha256(
            pd.util.hash_pandas_object(X_train).values).hexdigest()[:16],
        "n_rows": len(X_train),
        "params": params,
        "metrics": metrics,
        "feature_names": list(X_train.columns),
    }`,
        cap: "**`git_dirty` สำคัญกว่าที่คิด** — โมเดลที่เทรนจากโค้ดที่ยังไม่ commit คือโมเดลที่สร้างใหม่ไม่ได้", lang: "python" },
      { h: "6) ตรวจ schema ก่อนให้โมเดลทำนาย" },
      { code: String.raw`EXPECTED = {"age_bucket": int, "price_log": float,
            "category": str, "prior_returns": int}

def validate(features: dict):
    missing = set(EXPECTED) - set(features)
    extra = set(features) - set(EXPECTED)
    if missing or extra:
        raise ValueError(f"schema ไม่ตรง: ขาด {missing} เกิน {extra}")

    for k, t in EXPECTED.items():
        if not isinstance(features[k], t):
            raise TypeError(f"{k} ควรเป็น {t.__name__} แต่ได้ {type(features[k]).__name__}")

    if not 0 <= features["prior_returns"] <= 500:
        raise ValueError(f"prior_returns อยู่นอกช่วงที่เคยเห็น: {features['prior_returns']}")`,
        cap: "**ล้มเสียงดังดีกว่าทำนายเงียบ ๆ ด้วย input ที่ผิด** — ปัญหา schema ที่ต้นน้ำเปลี่ยนคือสาเหตุอันดับสามของ production incident", lang: "python" },
      { h: "7) service ที่ตรวจสอบและ log ครบ" },
      { code: String.raw`from fastapi import FastAPI, HTTPException
app = FastAPI()

@app.post("/predict")
def predict(payload: dict):
    t0 = time.perf_counter()
    try:
        feats = build_features(payload)
        validate(feats)
    except (ValueError, TypeError) as e:
        FALLBACK_COUNTER.inc()
        return {"decision": fallback_rule(payload), "reason": str(e)}

    t1 = time.perf_counter()
    p = model.predict_proba(feats)
    t2 = time.perf_counter()

    decision = "warn" if p > THRESHOLD else "ok"
    rid = log_prediction(feats, p, MODEL_VERSION,
                         {"features": (t1-t0)*1000, "model": (t2-t1)*1000}, decision)
    return {"request_id": rid, "decision": decision, "score": round(p, 4)}`,
        cap: "**มี fallback เสมอ** — โมเดลล้มแล้วระบบต้องยังตอบได้ด้วยกฎง่าย ๆ ไม่ใช่คืน error 500 ให้ผู้ใช้", lang: "python" },
      { h: "8) shadow deployment" },
      { code: String.raw`@app.post("/predict")
def predict(payload: dict):
    result = current_model_predict(payload)          # ตัวที่ใช้จริง

    try:
        shadow = candidate_model_predict(payload)     # ตัวใหม่ ไม่ใช้ผล
        log_shadow_comparison(payload, result, shadow)
    except Exception as e:
        logger.warning(f"shadow ล้ม: {e}")            # ← ห้ามกระทบผลจริงเด็ดขาด

    return result`,
        cap: "**`try/except` รอบ shadow เป็นข้อบังคับ** — โมเดลตัวใหม่ล้มแล้วต้องไม่ทำให้คำขอจริงล้มตาม", lang: "python" },
      { h: "9) เกณฑ์ rollback อัตโนมัติ" },
      { code: String.raw`ROLLBACK_RULES = [
    ("error_rate",        lambda m: m["error_rate"] > 0.01),
    ("p99_latency",       lambda m: m["p99_ms"] > 300),
    ("prediction_shift",  lambda m: abs(m["pos_rate"] - BASELINE_RATE) > 3 * BASELINE_STD),
    ("business_metric",   lambda m: m["conversion"] < BASELINE_CONV * 0.98),
]

def check_and_rollback(metrics):
    for name, rule in ROLLBACK_RULES:
        if rule(metrics):
            logger.error(f"rollback: {name}")
            deploy_model_version(PREVIOUS_VERSION)
            alert_oncall(f"ถอยกลับอัตโนมัติเพราะ {name}: {metrics}")
            return True
    return False`,
        cap: "**เกณฑ์ที่ทำงานอัตโนมัติเท่านั้นที่นับว่ามีอยู่จริง** — เกณฑ์ที่รอคนตัดสินใจตอนตีสามคือเกณฑ์ที่ไม่มี", lang: "python" },
      { h: "10) รายงาน monitoring รายวัน" },
      { code: String.raw`def daily_report(today, reference):
    out = {"date": str(today["date"])}

    for col in FEATURE_COLS:                              # data drift
        out[f"psi_{col}"] = psi(reference[col], today[col])

    out["pred_mean"] = today["prediction"].mean()          # prediction drift
    out["pos_rate"] = (today["prediction"] > THRESHOLD).mean()
    out["null_rate"] = today[FEATURE_COLS].isna().mean().to_dict()

    matured = today_labels_matured(today["date"])          # เฉพาะที่ label ครบแล้ว
    if len(matured) > 100:
        out["pr_auc"] = average_precision_score(matured["y"], matured["score"])

    for seg in ["category", "customer_tier"]:              # แยกตามกลุ่ม
        out[f"by_{seg}"] = segment_metrics(matured, seg)
    return out`,
        cap: "**สามบรรทัดสุดท้ายคือสิ่งที่จับปัญหาหมวดสินค้าใหม่ในตัวอย่างข้างต้นได้** — metric รวมอย่างเดียวมองไม่เห็น", lang: "python" }
    ],

    tricks: [
      { h: "ไล่ปัญหาตามอาการ" },
      { table: { head: ["อาการ", "สาเหตุที่พบบ่อยที่สุด", "ตรวจยังไง"], rows: [
        ["**ผลจริงแย่กว่าตอนทดสอบมาก ตั้งแต่วันแรก**", "training-serving skew หรือ leakage", "ยิง log จริงเข้าโมเดลซ้ำแล้วเทียบค่า"],
        ["ผลค่อย ๆ แย่ลงเป็นเดือน ๆ", "drift", "PSI ของทุก feature · การกระจายของคำทำนาย"],
        ["ผลตกฮวบในวันเดียว", "**ต้นน้ำเปลี่ยน schema หรือท่อข้อมูลล่ม**", "null rate · schema validation · การกระจายของ input"],
        ["metric รวมดี แต่ลูกค้าบางกลุ่มบ่น", "ปัญหาอยู่ในกลุ่มย่อยที่ค่าเฉลี่ยกลบไว้", "แยก metric ตามกลุ่มที่สำคัญ"],
        ["latency เกินงบ", "มักเป็นการดึง feature ไม่ใช่โมเดล", "วัด latency แยกตามขั้นตอน"],
        ["metric ดูดีขึ้นเรื่อย ๆ อย่างน่าสงสัย", "**feedback loop**", "สอดแทรกการสุ่มไว้ส่วนน้อยแล้วเทียบ"],
        ["เทรนใหม่แล้วแย่ลง", "ข้อมูลใหม่มีปัญหา หรือ leakage ในรอบเดิม", "เทียบ metric บนชุด test ชุดเดียวกันเสมอ"],
        ["สร้างผลลัพธ์เดิมซ้ำไม่ได้", "ไม่ได้ผูก data version กับ code version", "model card ที่มี git commit และ data hash"]
      ]}},
      { h: "สัญญาณที่ควรรู้สึกไม่ดี" },
      { ul: [
        "**คะแนน offline สูงผิดปกติ** — สงสัย leakage ก่อนเสมอ ไม่ใช่ดีใจ",
        "**ไม่มีใครตอบได้ว่าโมเดลที่รันอยู่เทรนจากข้อมูลชุดไหน** — แปลว่าย้อนกลับไม่ได้",
        "**โค้ดเตรียมข้อมูลมีสองชุด** — skew กำลังจะเกิด ถ้ายังไม่เกิด",
        "**deploy แล้วไม่มี dashboard** — ปัญหาจะถูกค้นพบโดยลูกค้า",
        "**เกณฑ์ตัดสินตั้งไว้ที่ 0.5 โดยไม่มีเหตุผล** — แทบไม่เคยเป็นค่าที่ถูก",
        "**เทรนใหม่ด้วยมือทุกครั้ง** — จะไม่ได้ทำ แล้วโมเดลจะเก่าไปเรื่อย ๆ"
      ]},
      { h: "ลำดับความสำคัญเมื่อมีเวลาจำกัด" },
      { code: String.raw`ถ้ามีเวลาทำได้แค่สามอย่าง:
   1. โค้ดเตรียมข้อมูลชุดเดียว        ← กันสาเหตุอันดับหนึ่ง
   2. log feature และคำทำนายทุกคำขอ   ← ทำให้ดีบักได้เลยเมื่อมีปัญหา
   3. dashboard การกระจายของคำทำนาย   ← จับปัญหาได้ก่อน label มาถึง

สามข้อนี้ให้ประโยชน์เกิน 80% ของ MLOps เต็มรูปแบบ`,
        cap: "ทำสามข้อนี้ให้ครบก่อนจะไปคิดเรื่อง Kubernetes หรือ feature store", lang: "txt" },
      { h: "ตัวเลขที่ควรจำได้" },
      { table: { head: ["เรื่อง", "ค่า"], rows: [
        ["PSI", "< 0.10 ปกติ · 0.10-0.25 ตรวจสอบ · > 0.25 เทรนใหม่"],
        ["latency ที่ต้องดู", "**p99 ไม่ใช่ค่าเฉลี่ย**"],
        ["คอขวดที่พบบ่อยที่สุด", "การดึง feature ไม่ใช่ model forward"],
        ["ขนาด A/B", "ครึ่งหนึ่งของความต่างที่ต้องการจับ = สี่เท่าของจำนวนตัวอย่าง"],
        ["สาเหตุอันดับหนึ่งที่โมเดลพัง", "**training-serving skew**"],
        ["ลำดับการปล่อย", "shadow → canary → A/B → 100%"]
      ]}},
      { note: "**ถ้าจำได้แค่ประโยคเดียวจากทั้งสิบหน้า**: โมเดลที่ดีที่สุดคือโมเดลที่ยังทำงานถูกต้องอยู่ในอีกหกเดือนข้างหน้า และสิ่งที่ตัดสินเรื่องนั้นไม่ใช่สถาปัตยกรรม แต่คือท่อข้อมูล การวัดผล และความสามารถในการถอยกลับ" }
    ],

    eval: [
      { p: "คำถามที่แยกคนที่เคยส่งโมเดลขึ้น production จริง ออกจากคนที่เทรนโมเดลเป็น" },
      { qa: [
        { q: "สาเหตุอันดับหนึ่งที่โมเดลพังใน production คืออะไร",
          a: "training-serving skew — โค้ดเตรียมข้อมูลตอนเทรนกับตอนเสิร์ฟไม่ตรงกัน ทำให้โมเดลเห็น feature คนละความหมายกับที่เทรนมา โดยไม่มี error ใด ๆ แจ้ง มีแต่ความแม่นยำที่ต่ำกว่าที่ทดสอบไว้" },
        { q: "แก้ training-serving skew ยังไงให้ได้ผลจริง",
          a: "ด้วยโครงสร้าง ไม่ใช่วินัย — ใช้โค้ดเตรียมข้อมูลชุดเดียวกันทั้งสองทาง หรือใช้ feature store และที่ดีที่สุดคือ log feature ที่ใช้จริงตอนเสิร์ฟแล้วเอามาเทรนรอบถัดไป ซึ่งตัดปัญหาที่ต้นเหตุ" },
        { q: "data drift ต่างจาก concept drift ยังไง",
          a: "data drift คือ `P(X)` เปลี่ยน คือ input หน้าตาต่างจากเดิม ส่วน concept drift คือ `P(y|X)` เปลี่ยน คือความสัมพันธ์เปลี่ยน · ข้อสำคัญคือ data drift ตรวจได้ทันทีโดยไม่ต้องมี label แต่ concept drift ต้องรอ label" },
        { q: "PSI คำนวณยังไง และอ่านค่าอย่างไร",
          a: "`Σ (actual − expected)·ln(actual/expected)` โดยแบ่งเป็นถังตาม decile ของชุดเทรน · ต่ำกว่า 0.10 คือปกติ, 0.10-0.25 ควรตรวจสอบ, เกิน 0.25 ควรพิจารณาเทรนใหม่" },
        { q: "ทำไมขอบถังของ PSI ต้องมาจากชุดเทรน",
          a: "เพราะถ้าคำนวณขอบใหม่ทุกครั้ง สัดส่วนปัจจุบันจะเป็น 10% ทุกถังโดยนิยาม และเมื่อเทียบกับค่าคาดหวัง 10% ตามชื่อ decile ก็ได้ PSI = 0 เสมอ — ขอบถังต้องแช่ไว้จากชุดเทรน" },
        { q: "ทำไม label ที่มาช้าถึงเปลี่ยนทุกอย่าง",
          a: "เพราะจะรู้ว่าโมเดลพังตอนที่สายไปแล้ว — งานทำนายการเลิกใช้บริการที่ label มาใน 30-90 วัน แปลว่าต้องพึ่งสัญญาณที่ไม่ต้องใช้ label เช่น PSI และการกระจายของคำทำนาย เป็นระบบเตือนหลัก" },
        { q: "การกระจายของคำทำนายบอกอะไร",
          a: "เป็นสัญญาณเตือนที่เร็วที่สุดที่ไม่ต้องใช้ label เลย — ถ้าโมเดลเคยทำนายว่าเสี่ยง 2% แล้ววันหนึ่งกลายเป็น 15% แปลว่ามีอะไรผิดแน่นอน ไม่ว่าจะเป็นข้อมูลหรือโมเดล" },
        { q: "feedback loop ในระบบแนะนำสินค้าคืออะไร และแก้ยังไง",
          a: "โมเดลแนะนำ A คนคลิก A ข้อมูลจึงยืนยันว่า A ดี ทำให้แนะนำ A มากขึ้น ส่วนสินค้าอื่นไม่เคยถูกแสดงจึงไม่มีข้อมูล · metric ดูดีขึ้นทั้งที่ระบบแคบลง แก้ด้วยการสอดแทรกการสุ่มไว้ส่วนน้อยเพื่อเก็บข้อมูลที่ไม่มีอคติ" },
        { q: "ลำดับการปล่อยโมเดลใหม่ที่ปลอดภัยคืออะไร",
          a: "shadow ซึ่งรับ traffic จริงแต่ไม่ใช้ผล แล้ว canary ที่ 1-5% แล้ว A/B วัดผลทางธุรกิจ แล้วค่อยขยายเป็น 100% โดยทุกขั้นต้องมีเงื่อนไข rollback เขียนไว้ก่อนเริ่ม" },
        { q: "shadow deployment จับอะไรได้และจับอะไรไม่ได้",
          a: "จับได้ทั้ง training-serving skew, latency จริง, error และการกระจายของคำทำนายที่ต่างจากเดิม โดยไม่มีความเสี่ยงต่อผู้ใช้เลย · แต่จับผลกระทบทางธุรกิจและ feedback loop ไม่ได้ เพราะไม่มีใครเห็นผลของมัน" },
        { q: "ทำไมต้องดู p99 latency ไม่ใช่ค่าเฉลี่ย",
          a: "เพราะค่าเฉลี่ย 50 ms ที่มี p99 เป็น 2 วินาที แปลว่าผู้ใช้หนึ่งในร้อยรอนานมาก และในระบบที่เรียกต่อกันหลายชั้น หางนั้นจะทบกันจนกลายเป็นปัญหาใหญ่" },
        { q: "คอขวดของ latency มักอยู่ที่ไหน",
          a: "ที่การดึง feature ไม่ใช่ที่ model forward — ในตัวอย่างงบ 200 ms การดึง feature กิน 60 ms หรือ 40% ขณะที่โมเดลกิน 45 ms คนจึงมักไปปรับโมเดลทั้งที่ควรแก้ที่การดึงข้อมูล" },
        { q: "batching แลกอะไรกับอะไร",
          a: "แลก latency กับ throughput — จาก batch 1 ไป 32 ได้ throughput เพิ่ม 8.6 เท่า แต่ latency ต่อคำขอเพิ่ม 3.8 เท่า · dynamic batching คือการหาจุดสมดุลอัตโนมัติภายใต้เพดาน latency ที่ตั้งไว้" },
        { q: "จะจับความต่างที่เล็กลงครึ่งหนึ่งใน A/B test ต้องใช้ตัวอย่างเพิ่มเท่าไร",
          a: "สี่เท่า เพราะขนาดตัวอย่างแปรผกผันกับกำลังสองของความต่าง — จับ +1.0 จุดเปอร์เซ็นต์ใช้ 8,149 ต่อกลุ่ม ส่วน +0.5 จุดใช้ 31,199" },
        { q: "อะไรที่คนวางแผน A/B ลืมบ่อยที่สุด",
          a: "ว่าการสะสมตัวอย่างครบไม่ได้แปลว่าวัดผลได้ — ถ้า label มาใน 30 วัน ต้องรออีก 30 วันหลังสะสมครบถึงจะสรุปได้จริง" },
        { q: "ควรเลือกเกณฑ์ตัดสินอย่างไร",
          a: "จากต้นทุนจริงของความผิดสองแบบ ไม่ใช่จาก F1 หรือค่าเริ่มต้น 0.5 · เกณฑ์ที่ทำให้ต้นทุนรวมต่ำสุดคือ `C_FP / (C_FP + C_FN)` — ที่ 20 กับ 150 จะได้ 0.12 ซึ่งต่ำกว่า 0.5 มาก" },
        { q: "ควรเริ่มด้วย batch หรือ online prediction",
          a: "batch เสมอถ้าเป็นไปได้ เพราะถูกกว่าหลายเท่าและดีบักง่ายกว่ามาก · งานจำนวนมากไม่ได้ต้องการ online จริง แค่คนขอคิดว่าต้องการ" },
        { q: "feature store แก้อะไรที่สำคัญที่สุด",
          a: "point-in-time correctness — การดึงเฉพาะค่าที่รู้ ณ เวลานั้นจริง ๆ ซึ่งกัน leakage ได้ในระดับโครงสร้างพื้นฐาน นอกเหนือจากการกัน skew และการใช้ feature ซ้ำข้ามทีม" },
        { q: "ต้อง log อะไรบ้างในทุกคำขอ",
          a: "request id, เวลา, **feature ที่ใช้จริงหลังแปลงแล้ว**, คำทำนาย, เวอร์ชันโมเดล, latency แยกตามขั้นตอน และการตัดสินใจสุดท้ายหลังผ่านกฎธุรกิจ · ขาด feature ที่ใช้จริงแล้วจะย้อนกลับไปหาสาเหตุไม่ได้เลย" },
        { q: "ทำไมต้องผูก model version กับ git commit และ data hash",
          a: "เพราะถ้าตอบไม่ได้ว่าโมเดลที่รันอยู่เทรนจากข้อมูลชุดไหนด้วยโค้ดคอมมิตไหน ก็สร้างใหม่ไม่ได้ ย้อนกลับไม่ได้ และดีบักไม่ได้ · ควรบันทึกด้วยว่าตอนเทรนโค้ดยังไม่ commit หรือไม่" },
        { q: "ทำไม metric รวมถึงอันตราย",
          a: "เพราะมันกลบปัญหาของกลุ่มย่อยได้เสมอ — หมวดสินค้าใหม่ที่คิดเป็น 4% ของคำสั่งซื้ออาจทำนายแทบสุ่มโดยที่ metric รวมแทบไม่ขยับ จึงต้องแยกดูตามกลุ่มที่สำคัญด้วย" },
        { q: "เมื่อไรไม่ควรใช้ ML เลย",
          a: "เมื่อเขียนเป็นกฎได้ชัดเจน เมื่อข้อมูลน้อยมาก เมื่อผิดแล้วรับไม่ได้เลย เมื่อไม่มีใครดูแลต่อ และเมื่อไม่มี label และหามาไม่ได้ · ถ้ากฎสามข้อได้ 90% ของประโยชน์ ML ที่ได้เพิ่ม 3% แต่ต้องดูแลตลอดชีพมักไม่คุ้ม" },
        { q: "ถ้ามีเวลาทำได้แค่สามอย่าง ควรทำอะไร",
          a: "โค้ดเตรียมข้อมูลชุดเดียวใช้ทั้งสองทาง, log feature และคำทำนายทุกคำขอ, และ dashboard การกระจายของคำทำนาย · สามข้อนี้ให้ประโยชน์เกิน 80% ของ MLOps เต็มรูปแบบ และควรทำก่อนคิดเรื่อง Kubernetes หรือ feature store" }
      ]},
      { h: "อ่านเพิ่ม" },
      { links: [
        { label: "Rules of Machine Learning (Google)", url: "https://developers.google.com/machine-learning/guides/rules-of-ml", note: "43 กฎจากประสบการณ์จริง อ่านครั้งเดียวได้ประโยชน์ทั้งอาชีพ" },
        { label: "Hidden Technical Debt in Machine Learning Systems", url: "https://papers.nips.cc/paper/2015/hash/86df7dcfd896fcaf2674f757a2463eba-Abstract.html", note: "เปเปอร์ที่ชี้ว่าโค้ดโมเดลเป็นส่วนเล็กมากของระบบจริง" },
        { label: "Machine Learning Design Patterns — code and notebooks", url: "https://github.com/GoogleCloudPlatform/ml-design-patterns", note: "โค้ดประกอบหนังสือ Machine Learning Design Patterns — รูปแบบที่ใช้ซ้ำได้สำหรับการเสิร์ฟ การเทรนใหม่ และการจัดการ feature" },
        { label: "Machine Learning Systems Design (Chip Huyen)", url: "https://huyenchip.com/machine-learning-systems-design/toc.html", note: "หนังสือ Machine Learning Systems Design ฉบับเปิดอ่านฟรี ครอบคลุมตั้งแต่การวางโจทย์จนถึงการเฝ้าระวัง" },
        { label: "Monitoring Machine Learning Models in Production", url: "https://christophergs.com/machine%20learning/2020/03/14/how-to-monitor-machine-learning-models/", note: "อธิบายสามชั้นของ metric และวิธีตั้ง alert อย่างละเอียด" },
        { label: "Evidently — Data Drift documentation", url: "https://docs.evidentlyai.com/", note: "เครื่องมือโอเพนซอร์สสำหรับ drift พร้อมนิยามของ PSI และตัวชี้วัดอื่น" },
        { label: "MLflow — Model Registry", url: "https://mlflow.org/docs/latest/model-registry.html", note: "การจัดการเวอร์ชันโมเดลและการผูกกับ run ที่สร้างมัน" },
        { label: "Feast — Feature Store", url: "https://docs.feast.dev/", note: "feature store โอเพนซอร์ส พร้อมคำอธิบาย point-in-time correctness" },
        { label: "Trustworthy Online Controlled Experiments", url: "https://experimentguide.com/", note: "หนังสืออ้างอิงของการทำ A/B test ให้ถูกต้อง รวมกับดักที่พบบ่อย" }
      ]}
    ]
  }
});

window.TEACHING_EN = window.TEACHING_EN || {};
window.TEACHING_EN["ml_prod"] = {
  principle: [
    { h: "The gap the previous nine pages never mentioned" },
    { table: { head: ["In a notebook", "In production"], rows: [
      ["The data is static, in one file", "**Data arrives continuously and keeps changing shape**"],
      ["The answer is known immediately from the test set", "**Labels arrive weeks or months later**"],
      ["Prediction can take as long as it likes", "There is a p99 latency that must not be exceeded"],
      ["A mistake means rerunning the cell", "**A mistake reaches real people**"],
      ["Preprocessing lives in a cell", "The same code must run in training and in serving"]
    ]}},
    { h: "Why models fail in production, by frequency" },
    { code: String.raw`1. training-serving skew      two preprocessing paths that do not agree
2. data drift                 the input changed and nobody noticed
3. a feature silently changed  an upstream team altered the schema
4. concept drift              the relationship between X and y moved
5. the model is genuinely wrong  <- last, and the least common`,
      cap: "**Item 5 is what the previous nine pages taught; items 1-4 are the whole of this page**", lang: "txt" },
    { h: "Questions to answer before the first line of code" },
    { ul: [
      "**Which decision will this prediction change** — if there is no answer, do not build it",
      "**What does a mistake cost**, and which kind of mistake hurts more (false positive against false negative)",
      "**When will the true label be known** — a day, a week, or three months",
      "**What latency must it answer in**, and at what volume per second",
      "**What does a non-ML baseline achieve** — simple rules often win and are far cheaper to maintain"
    ]},
    { note: "**That last question kills a great many projects, and should** — if three if-else rules capture 90% of the value, an ML model that adds 3% but needs a team maintaining it forever usually is not worth it." },
    { h: "What this page will let you do" },
    { ul: [
      "Identify and prevent training-serving skew structurally rather than by discipline",
      "Set up monitoring that catches problems before the labels arrive",
      "Compute PSI by hand and read the value correctly",
      "Roll out a model as shadow → canary → A/B with rollback conditions written in advance",
      "Compute a latency budget and a cost per thousand requests"
    ]}
  ],

  theory: [
    { p: "This section works through production's problems one at a time, with the mechanism that fixes each." },
    { h: "1) Training-serving skew — the number one problem" },
    { code: String.raw`in training (Python + pandas):
   age_bucket = pd.cut(age, [0, 25, 40, 60, 200])

in serving (Java, rewritten by a different team):
   if (age < 25) bucket = 0;         <- the boundaries do not match!

the model now sees a feature that means something different from what it was trained on`,
      cap: "No error is raised — only accuracy that is inexplicably lower than in testing", lang: "txt" },
    { table: { head: ["Fix", "How well it works"], rows: [
      ["Review both code paths by eye", "**It does not work** — they will diverge again in three months"],
      ["Use one preprocessing implementation in both paths", "**It genuinely works** — a structural fix"],
      ["**A feature store**", "The best of all — computed once, used for both training and serving"],
      ["Log the features actually used at serving time and train on those", "It removes the cause entirely"]
    ]}},
    { h: "2) Online against batch prediction" },
    { table: { head: ["", "Batch", "Online"], rows: [
      ["When it runs", "On a schedule, such as nightly", "When a request arrives"],
      ["Latency", "Irrelevant", "**The dominant constraint**"],
      ["Complexity", "**Very low** — a cron job and a table", "High — a service, monitoring, autoscaling"],
      ["Suits", "Customer scoring · daily recommendations", "Fraud detection · search ranking"]
    ]}},
    { note: "**Always start with batch when it is possible** — it is several times cheaper and far easier to debug, and a great many tasks do not actually need online, the requester merely assumes they do." },
    { h: "3) Data drift against concept drift" },
    { code: String.raw`data drift:     P(X) changes — the inputs look different
                for instance a new customer segment lowers the average age

concept drift:  P(y|X) changes — the relationship itself moves
                for instance a new kind of fraud the old features cannot see`,
      cap: "**Data drift is detectable immediately without labels · concept drift cannot be seen until the labels arrive**", lang: "txt" },
    { h: "4) PSI — measuring data drift without labels" },
    { code: String.raw`PSI = Σ (actual_i − expected_i) · ln(actual_i / expected_i)

bucket the feature (usually 10 buckets on the training set's deciles)
and compare the proportion falling in each

PSI < 0.10   no meaningful change
0.10 - 0.25  moderate change — investigate
PSI > 0.25   substantial change — consider retraining`,
      cap: "Usable from the first day of deployment, because it needs no ground truth", lang: "txt" },
    { h: "5) Late labels — the constraint that shapes everything" },
    { table: { head: ["Task", "When the label arrives", "Consequence"], rows: [
      ["Search ranking", "**Immediately** (clicked or not)", "Real performance is measurable almost at once"],
      ["Fraud detection", "Days to weeks", "Proxy metrics are needed while waiting"],
      ["Churn prediction", "**30-90 days**", "You learn the model broke long after it did"],
      ["Loan approval", "**Months to years**", "And only for approved applicants — a feedback loop"]
    ]}},
    { p: "**That last row is the nastiest trap**: whoever the model rejects never reveals whether they would have repaid, so the next round of data contains only those the previous model approved, and the model keeps confirming its own bias. The remedy is approving a small random fraction to collect unbiased data." },
    { h: "6) A rollout you can reverse" },
    { code: String.raw`1. shadow    takes real traffic but **its output is not used** — compared quietly
2. canary    1-5% of traffic making real decisions, with alerts
3. A/B       a split test measuring the business outcome properly
4. rollout   expand gradually to 100%

**every stage needs its rollback conditions written before it starts**`,
      cap: "Shadow catches training-serving skew and latency problems with no risk whatsoever to users", lang: "txt" },
    { h: "7) Metrics come in three layers" },
    { table: { head: ["Layer", "What it watches", "When you know"], rows: [
      ["**System**", "p50/p99 latency · error rate · throughput", "**Immediately**"],
      ["**Model**", "Input distributions · prediction distribution · PSI", "**Immediately**"],
      ["**Business**", "Revenue · conversion · complaints", "Slowly, but **it is the only one that truly matters**"]
    ]}},
    { note: "**The prediction distribution is the fastest warning available** — if a model used to flag 2% as risky and suddenly flags 15%, something is certainly wrong, and not one label is needed to know it." },
    { h: "8) When to retrain" },
    { table: { head: ["Trigger", "Advantage", "Drawback"], rows: [
      ["**On a schedule**, say weekly", "Predictable and easy to plan", "Retrains when unnecessary, or too late when it matters"],
      ["**On a measurement**, say PSI above 0.25", "Retrains only when genuinely needed", "Requires trustworthy monitoring first"],
      ["**On measured degradation**", "The most precise trigger", "**It waits for labels, which may be too late**"]
    ]}},
    { h: "9) Three versions that must be tied together" },
    { code: String.raw`code version    +    data version    +    model version
                                          ‾‾‾‾‾‾‾‾‾‾‾‾‾
                            all three must be reconstructible

if you cannot answer "which data and which commit produced the model running now",
you cannot roll back and you cannot debug`,
      cap: "This is what a model registry is for — not merely a place to keep files", lang: "txt" },
    { h: "10) When not to use ML at all" },
    { ul: [
      "**It can be written as clear rules** — rules are readable, instantly fixable, and never drift",
      "**There is very little data** — below a few thousand examples is usually not enough",
      "**Mistakes are unacceptable** — a task requiring 100% correctness should not use a probabilistic model",
      "**Nobody will maintain it** — an unmaintained model degrades quietly, always",
      "**There are no labels and none can be obtained** — improvement cannot be measured at all"
    ]}
  ],

  foundations: [
    { p: "This section digs into the numbers that decide real deployments." },
    { h: "Computing PSI by hand" },
    { code: String.raw`one feature, five buckets

bucket   at training   now      (a−e)·ln(a/e)
  1         0.10       0.05        0.0347
  2         0.20       0.12        0.0409
  3         0.40       0.35        0.0067
  4         0.20       0.28        0.0269
  5         0.10       0.20        0.0693
                                   ‾‾‾‾‾‾
                          PSI =     0.1784`,
      cap: "0.178 falls in the 0.10-0.25 band, a moderate change — no immediate retrain, but the cause must be found", lang: "txt" },
    { p: "**Note that bucket 5 contributes the most** (0.0693 of 0.1784) because its share doubled. PSI does not merely say whether something changed; it says where, which is the clue to what to go and look at." },
    { h: "A latency budget has to be broken into pieces" },
    { code: String.raw`target p99 = 200 ms

network in and out    20 ms   13%
feature fetch         60 ms   40%    <- usually the largest, not the model
preprocess            15 ms   10%
model forward         45 ms   30%
postprocess + log     10 ms    7%
                     ‾‾‾‾‾‾
total                150 ms   leaving 50 ms of headroom`,
      cap: "**People habitually optimise the model when the bottleneck is the feature fetch** — measure first, then fix", lang: "txt" },
    { note: "**Watch p99, not the mean** — a 50 ms mean with a 2-second p99 means one user in a hundred waits a very long time, and in a system of chained calls those tails compound into a serious problem." },
    { h: "How much throughput batching buys" },
    { code: String.raw`the same model on one GPU

batch  1:   45 ms per request   ->   22 req/s
batch  8:   73 ms per batch     ->  110 req/s
batch 32:  169 ms per batch     ->  189 req/s

throughput up 8.6×, per-request latency up 3.8×`,
      cap: "A direct trade between latency and throughput — dynamic batching is finding that balance automatically", lang: "txt" },
    { h: "Cost per thousand requests" },
    { code: String.raw`a GPU at $1.20 per hour · 189 req/s at batch 32

cost = 1.20 / 3600 / 189 × 1000 = $0.0018 per thousand requests

10 million requests a day -> $18 a day -> $540 a month`,
      cap: "Compute this before starting — it often says immediately that a smaller model, or batch prediction, is the right answer", lang: "txt" },
    { h: "How many users an A/B test needs" },
    { code: String.raw`baseline conversion 5% · α = 0.05 · power = 80%

to detect +0.5 percentage points (5.0% -> 5.5%)   -> 31,199 per arm
to detect +1.0 percentage points (5.0% -> 6.0%)   ->  8,149 per arm

at 10,000 users per arm per day: 3.1 days against 0.8 days`,
      cap: "**Halving the effect you want to detect quadruples the sample required**, because it scales with the square", lang: "txt" },
    { p: "**And if the calculation says three months to reach a conclusion, that is itself the answer** — an effect that small may not justify maintaining a more complicated model." },
    { h: "What shadow deployment can and cannot catch" },
    { table: { head: ["It catches", "It cannot catch"], rows: [
      ["**Training-serving skew**", "The business impact"],
      ["Real latency under real traffic", "**Feedback loops** (nobody sees the output)"],
      ["Errors and edge cases never seen before", "Changes in user behaviour"],
      ["**A prediction distribution that differs from the current model's**", "—"]
    ]}},
    { h: "Retraining cadence against model staleness" },
    { code: String.raw`retrain every  1 day   -> average model age  0.5 days
retrain every  7 days  -> average model age  3.5 days
retrain every 30 days  -> average model age   15 days
retrain every 90 days  -> average model age   45 days

compare against how fast the domain's data actually moves`,
      cap: "In fraud, where patterns shift weekly, retraining quarterly means the model is permanently behind", lang: "txt" },
    { h: "Feedback loops — the problem metrics cannot see" },
    { code: String.raw`a recommender:
   the model suggests A -> users click A -> the data says A is good -> it suggests A more
   -> other items are never shown -> there is no data on whether they are good

the metric keeps improving while the system keeps narrowing`,
      cap: "The remedy is injecting a small amount of randomness, to collect data uncontaminated by the model's own bias", lang: "txt" }
  ],

  architecture: [
    { p: "This section is the system's shape and the order it should be built in." },
    { h: "The order to actually build in" },
    { code: String.raw`1. ship a non-ML baseline to production first   <- it forces the pipeline and the measurement
2. replace that baseline with a simple model
3. monitoring across all three layers
4. automated retraining
5. only then, a more sophisticated model`,
      cap: "**Step 1 matters most and is skipped most often** — it forces the data pipeline, the deployment and real measurement into existence before any model exists", lang: "txt" },
    { h: "The shape of an online system" },
    { code: String.raw`  request
    |
  [API gateway]  <- rate limiting, auth
    |
  [feature service] <------ feature store (online) <-- feature pipeline
    |                                                    |
  [model service]  <------ model registry            feature store (offline)
    |                                                    |
  [postprocess + business rules]                     training system
    |                                                    |
  response ------------> log both features and predictions -'`,
      cap: "**The arrow returning from the bottom right** is what lets the system improve itself — log the features actually used and train on them next time", lang: "txt" },
    { h: "What a feature store fixes" },
    { table: { head: ["Problem", "How it fixes it"], rows: [
      ["**Training-serving skew**", "One feature definition, used by both paths"],
      ["**Duplication**", "Other teams reuse existing features directly"],
      ["**Point-in-time correctness**", "It returns only what was known at that moment, preventing leakage by construction"],
      ["Latency", "The online side is a key-value store answering in milliseconds"]
    ]}},
    { note: "**Point-in-time correctness is the most valuable property and the least discussed** — joining historical data without care about time gives features containing information that did not exist yet, which is the leakage of page 2 occurring at the infrastructure level." },
    { h: "Rollback criteria, written before release" },
    { code: String.raw`roll back immediately when:
   error rate  > 1%
   p99 latency > 300 ms for 5 consecutive minutes
   the prediction distribution moves more than 3 standard deviations
   the business metric falls more than 2% in the treatment group

**write them as code that acts automatically, not as an agreement awaiting a human at 3am**`,
      cap: "A criterion that waits for a human decision is, in practice, not a criterion at all", lang: "txt" },
    { h: "What every request must log" },
    { code: String.raw`request_id · timestamp
every feature actually used        <- the most important, for both debugging and next training
the prediction and its probability
the model version
latency broken down by stage
the final decision after the business rules`,
      cap: "**Without the features actually used, there is no way to trace back why a prediction was wrong**", lang: "txt" },
    { h: "Reducing latency, in order of return" },
    { table: { head: ["Method", "Gain", "Cost"], rows: [
      ["**Cache repeated predictions**", "The largest, when repetition is common", "Slightly stale results"],
      ["**Fix the feature-fetch bottleneck**", "Usually the biggest real-world win", "Structural change"],
      ["**Dynamic batching**", "Several times the throughput", "Higher per-request latency"],
      ["**Quantisation (int8)**", "2-4×", "A small quality loss"],
      ["**Distil into a smaller model**", "Several times", "Extra training and some quality loss"],
      ["**ONNX / TensorRT**", "2-5×", "An extra build step"]
    ]}},
    { h: "What a small team actually needs" },
    { code: String.raw`the acceptable minimum:
   ✓ one preprocessing implementation used by both training and serving
   ✓ log the features and predictions of every request
   ✓ dashboards across the three layers (system · model · business)
   ✓ model versions tied to a git commit and a data snapshot
   ✓ a rollback button that returns to the previous version within a minute

not yet needed: a full feature store · Kubernetes · automated retraining`,
      cap: "Those five are achievable with ordinary tools and deliver nearly all of the value of full MLOps", lang: "txt" }
  ],

  dataflow: [
    { p: "One project walked from the business question through to the first retraining." },
    { h: "The task" },
    { code: String.raw`an online shop wants to reduce returns
   predict whether an order is likely to be returned
   if it is -> prompt the customer to confirm the size before paying

volume   50,000 orders a day
latency  must answer within 200 ms, because it sits in the checkout page
labels   known after 30 days (when the return window closes)`,
      cap: "**Note that labels arrive 30 days late** — that constraint shapes everything that follows on this page", lang: "txt" },
    { h: "Step 0 — a non-ML baseline" },
    { code: String.raw`rule: if it is clothing and this customer has returned before -> treat as risky

catches 60% of real returns · falsely warns on 25% of orders
in production in two days`,
      cap: "And now the pipeline, the deployment, the logging and the dashboards all exist, before there is any model", lang: "txt" },
    { h: "Step 1 — only features known at order time" },
    { code: String.raw`allowed:
   product category · price · quantity
   how many times this customer has returned **before today**
   this product's return rate **computed from data before today**

not allowed (leakage):
   this product's return rate over all data   <- it contains the future
   whether the customer contacted support      <- that happens after the order`,
      cap: "Those last two lines are the leakage of page 2, and the leading cause of \"great in the notebook, broken in production\"", lang: "txt" },
    { h: "Step 2 — split by time" },
    { code: String.raw`train       January - September
validation  October
test        November

never split randomly — in production you always predict the future from the past
and leave a 30-day gap before validation, because those labels have not matured`,
      cap: "**That last line is forgotten most often** — late-September orders have no known outcome at the cut-off, so they must be excluded", lang: "txt" },
    { h: "Step 3 — train, and choose the threshold" },
    { code: String.raw`gradient boosting · PR-AUC = 0.34  (the random baseline is 0.05, the return rate)

choose the threshold from real costs, not from F1:
   a false warning  -> interrupting a customer mid-checkout, valued at 20
   a missed return  -> return shipping + restock,             valued at 150

the total-cost-minimising threshold = C_FP / (C_FP + C_FN)
                                    = 20 / 170 = 0.12, not 0.5`,
      cap: "**0.5 is never automatically the right threshold** — it is right only when both errors cost the same, which is almost never", lang: "txt" },
    { h: "Step 4 — one week in shadow" },
    { code: String.raw`what it found:
   ✗ p99 latency = 340 ms, over the 200 ms budget
     -> tracing showed the customer-history lookup took 210 ms
     -> precomputing it into a cache brought it to 45 ms -> p99 = 175 ms

   ✗ 3% of requests had missing features, because new customers have no history
     -> training filled them with the mean, serving sent null -> **training-serving skew**
     -> fixed by using one imputation implementation in both paths`,
      cap: "**Neither of these could ever be found in a notebook**, and both were caught without a single user being affected", lang: "txt" },
    { h: "Step 5 — a 5% canary" },
    { code: String.raw`5% of traffic for 3 days, with automatic rollback conditions armed

watching:
   error rate            0.2%  normal
   p99 latency           178 ms  within budget
   share of orders warned  11%  close to the expected 12%
   cart abandonment      no significant difference from control`,
      cap: "That last row is the real fear — a warning might drive people away entirely, which would be worse than the returns", lang: "txt" },
    { h: "Step 6 — an A/B test sized in advance" },
    { code: String.raw`baseline return rate 5% · to detect a 0.5 percentage point reduction

required sample = 31,199 orders per arm
at 25,000 per arm per day -> 1.3 days to accumulate
but another 30 days before the labels mature

-> a real conclusion on day 32`,
      cap: "**The second-to-last line is what planners always forget** — accumulating the sample is not the same as being able to measure it", lang: "txt" },
    { h: "Step 7 — two months later, monitoring fires" },
    { code: String.raw`PSI on the "product category" feature = 0.31        <- above 0.25

investigation: the shop has just launched a footwear category, absent from training
predictions there are close to random, but the overall metric still looks fine
because footwear is only 4% of orders`,
      cap: "**An aggregate metric will always hide a subgroup's problem** — segment metrics are required, not a single number", lang: "txt" },
    { h: "Step 8 — retrain and release again" },
    { code: String.raw`retrain on data that now includes the new category
using the features logged at serving time, not recomputed from the warehouse
   -> which removes the possibility of skew at its source

then repeat shadow -> canary -> A/B as before
**every release uses the same process, with no exception for "a tiny change"**`,
      cap: "That last sentence saves more incidents than anything else — most outages come from changes deemed too small to test", lang: "txt" }
  ],

  implementation: [
    { p: "Code for what this page describes, focused on what a small team can actually do." },
    { h: "1) One preprocessing implementation for both paths" },
    { code: String.raw`# features.py — one file, imported by both training and serving
from dataclasses import dataclass

AGE_BINS = [0, 25, 40, 60, 200]
CATEGORY_DEFAULT = "unknown"

def build_features(raw: dict) -> dict:
    """takes one dict; must work for a single row and inside a batch"""
    return {
        "age_bucket": _bucket(raw.get("age"), AGE_BINS),
        "price_log": math.log1p(raw.get("price", 0.0)),
        "category": raw.get("category") or CATEGORY_DEFAULT,
        "prior_returns": raw.get("prior_returns", 0),
    }

# training:  df.apply(build_features, axis=1)
# serving:   build_features(request_payload)`,
      cap: "**One function, one file, imported by both** — this prevents skew structurally rather than by discipline", lang: "python" },
    { h: "2) Testing that training and serving agree" },
    { code: String.raw`def test_train_serve_parity():
    rows = load_sample_rows(200)

    batch = pd.DataFrame([build_features(r) for r in rows])       # training path
    single = [build_features(r) for r in rows]                     # serving path

    for i, row in enumerate(single):
        for k, v in row.items():
            assert batch.iloc[i][k] == v, f"skew in {k} at row {i}"

def test_serving_matches_recorded_predictions():
    """replay logged production payloads and require identical outputs"""
    for rec in load_production_logs(500):
        assert abs(model.predict_one(rec["features"]) - rec["prediction"]) < 1e-6`,
      cap: "**The second test is the more valuable** — it catches skew, library version changes, and deploying the wrong model", lang: "python" },
    { h: "3) Computing PSI" },
    { code: String.raw`import numpy as np

def psi(expected, actual, bins=10):
    edges = np.percentile(expected, np.linspace(0, 100, bins + 1))
    edges[0], edges[-1] = -np.inf, np.inf

    e = np.histogram(expected, edges)[0] / len(expected)
    a = np.histogram(actual, edges)[0] / len(actual)

    e = np.clip(e, 1e-6, None)         # guard against division by zero
    a = np.clip(a, 1e-6, None)
    return float(np.sum((a - e) * np.log(a / e)))

# psi < 0.10 normal · 0.10-0.25 investigate · > 0.25 consider retraining`,
      cap: "**The bucket edges must come from the training set and stay frozen** — recompute them each time and the current shares are 10% per bucket by definition, so comparing against a nominal 10% expectation gives PSI = 0 forever, which is the most common broken pipeline", lang: "python" },
    { h: "4) Logging you can actually debug with" },
    { code: String.raw`import json, time, uuid

def log_prediction(features, pred, model_version, latencies, decision):
    record = {
        "request_id": str(uuid.uuid4()),
        "ts": time.time(),
        "model_version": model_version,
        "features": features,          # <- what the model actually saw, not the raw payload
        "prediction": float(pred),
        "decision": decision,          # after the business rules
        "latency_ms": latencies,       # broken down by stage
    }
    logger.info(json.dumps(record))
    return record["request_id"]`,
      cap: "`features` must be the transformed values, not the raw payload — otherwise a wrong prediction cannot be traced", lang: "python" },
    { h: "5) Tying model version to code and data" },
    { code: String.raw`import subprocess, hashlib, json, datetime

def build_model_card(model, X_train, params, metrics):
    return {
        "model_version": datetime.datetime.utcnow().strftime("%Y%m%d_%H%M%S"),
        "git_commit": subprocess.check_output(
            ["git", "rev-parse", "HEAD"]).decode().strip(),
        "git_dirty": bool(subprocess.check_output(["git", "status", "--porcelain"])),
        "data_hash": hashlib.sha256(
            pd.util.hash_pandas_object(X_train).values).hexdigest()[:16],
        "n_rows": len(X_train),
        "params": params,
        "metrics": metrics,
        "feature_names": list(X_train.columns),
    }`,
      cap: "**`git_dirty` matters more than it looks** — a model trained from uncommitted code is a model that cannot be rebuilt", lang: "python" },
    { h: "6) Validating the schema before predicting" },
    { code: String.raw`EXPECTED = {"age_bucket": int, "price_log": float,
            "category": str, "prior_returns": int}

def validate(features: dict):
    missing = set(EXPECTED) - set(features)
    extra = set(features) - set(EXPECTED)
    if missing or extra:
        raise ValueError(f"schema mismatch: missing {missing} extra {extra}")

    for k, t in EXPECTED.items():
        if not isinstance(features[k], t):
            raise TypeError(f"{k} should be {t.__name__}, got {type(features[k]).__name__}")

    if not 0 <= features["prior_returns"] <= 500:
        raise ValueError(f"prior_returns outside the observed range: {features['prior_returns']}")`,
      cap: "**Fail loudly rather than predict quietly on wrong input** — an upstream schema change is the third most common cause of production incidents", lang: "python" },
    { h: "7) A service that validates and logs properly" },
    { code: String.raw`from fastapi import FastAPI, HTTPException
app = FastAPI()

@app.post("/predict")
def predict(payload: dict):
    t0 = time.perf_counter()
    try:
        feats = build_features(payload)
        validate(feats)
    except (ValueError, TypeError) as e:
        FALLBACK_COUNTER.inc()
        return {"decision": fallback_rule(payload), "reason": str(e)}

    t1 = time.perf_counter()
    p = model.predict_proba(feats)
    t2 = time.perf_counter()

    decision = "warn" if p > THRESHOLD else "ok"
    rid = log_prediction(feats, p, MODEL_VERSION,
                         {"features": (t1-t0)*1000, "model": (t2-t1)*1000}, decision)
    return {"request_id": rid, "decision": decision, "score": round(p, 4)}`,
      cap: "**Always have a fallback** — when the model fails the system must still answer with a simple rule, not return a 500 to the user", lang: "python" },
    { h: "8) Shadow deployment" },
    { code: String.raw`@app.post("/predict")
def predict(payload: dict):
    result = current_model_predict(payload)          # the live model

    try:
        shadow = candidate_model_predict(payload)     # the candidate, output unused
        log_shadow_comparison(payload, result, shadow)
    except Exception as e:
        logger.warning(f"shadow failed: {e}")         # <- must never affect the real result

    return result`,
      cap: "**The `try/except` around the shadow is mandatory** — a failing candidate must not take the real request down with it", lang: "python" },
    { h: "9) Automatic rollback criteria" },
    { code: String.raw`ROLLBACK_RULES = [
    ("error_rate",        lambda m: m["error_rate"] > 0.01),
    ("p99_latency",       lambda m: m["p99_ms"] > 300),
    ("prediction_shift",  lambda m: abs(m["pos_rate"] - BASELINE_RATE) > 3 * BASELINE_STD),
    ("business_metric",   lambda m: m["conversion"] < BASELINE_CONV * 0.98),
]

def check_and_rollback(metrics):
    for name, rule in ROLLBACK_RULES:
        if rule(metrics):
            logger.error(f"rollback: {name}")
            deploy_model_version(PREVIOUS_VERSION)
            alert_oncall(f"automatic rollback due to {name}: {metrics}")
            return True
    return False`,
      cap: "**Only criteria that act automatically really exist** — one that waits for a human at 3am is not a criterion", lang: "python" },
    { h: "10) The daily monitoring report" },
    { code: String.raw`def daily_report(today, reference):
    out = {"date": str(today["date"])}

    for col in FEATURE_COLS:                              # data drift
        out[f"psi_{col}"] = psi(reference[col], today[col])

    out["pred_mean"] = today["prediction"].mean()          # prediction drift
    out["pos_rate"] = (today["prediction"] > THRESHOLD).mean()
    out["null_rate"] = today[FEATURE_COLS].isna().mean().to_dict()

    matured = today_labels_matured(today["date"])          # only matured labels
    if len(matured) > 100:
        out["pr_auc"] = average_precision_score(matured["y"], matured["score"])

    for seg in ["category", "customer_tier"]:              # by segment
        out[f"by_{seg}"] = segment_metrics(matured, seg)
    return out`,
      cap: "**The last three lines are what caught the new-category problem in the walkthrough above** — an aggregate metric cannot see it", lang: "python" }
  ],

  tricks: [
    { h: "Diagnosing by symptom" },
    { table: { head: ["Symptom", "The most common cause", "How to check"], rows: [
      ["**Production is far worse than testing, from day one**", "Training-serving skew, or leakage", "Replay logged requests through the model and compare"],
      ["Performance degrades slowly over months", "Drift", "PSI on every feature · the prediction distribution"],
      ["Performance collapses in a single day", "**An upstream schema change or a broken pipeline**", "Null rates · schema validation · input distributions"],
      ["Aggregate metrics fine, some customers complaining", "A subgroup problem hidden by the average", "Break the metrics out by important segments"],
      ["Latency over budget", "Usually the feature fetch, not the model", "Measure latency per stage"],
      ["Metrics improving suspiciously steadily", "**A feedback loop**", "Inject a small random fraction and compare"],
      ["Retraining made it worse", "Bad new data, or leakage in the previous round", "Always compare on the same held-out test set"],
      ["Results cannot be reproduced", "Data version not tied to code version", "A model card carrying the git commit and a data hash"]
    ]}},
    { h: "Signals that should make you uneasy" },
    { ul: [
      "**An unusually high offline score** — suspect leakage first, rather than celebrating",
      "**Nobody can say which data trained the running model** — it cannot be rolled back",
      "**Two preprocessing implementations exist** — skew is coming, if it has not already arrived",
      "**Deployed with no dashboard** — problems will be discovered by customers",
      "**A threshold of 0.5 chosen for no reason** — it is almost never the right value",
      "**Retraining is done manually every time** — which means it will stop happening, and the model will age"
    ]},
    { h: "Priorities when time is short" },
    { code: String.raw`if there is only time for three things:
   1. one preprocessing implementation        <- prevents the number one cause
   2. log features and predictions per request <- makes debugging possible at all
   3. a prediction-distribution dashboard      <- catches problems before labels arrive

these three deliver over 80% of the value of full MLOps`,
      cap: "Finish all three before thinking about Kubernetes or a feature store", lang: "txt" },
    { h: "Numbers worth remembering" },
    { table: { head: ["Thing", "Value"], rows: [
      ["PSI", "< 0.10 normal · 0.10-0.25 investigate · > 0.25 retrain"],
      ["The latency figure that matters", "**p99, not the mean**"],
      ["The most common bottleneck", "The feature fetch, not the model forward"],
      ["A/B sizing", "Halving the detectable effect quadruples the sample"],
      ["The leading cause of production failure", "**Training-serving skew**"],
      ["The rollout order", "shadow → canary → A/B → 100%"]
    ]}},
    { note: "**If only one sentence survives from all ten pages**: the best model is the one still working correctly six months from now, and what decides that is not the architecture but the data pipeline, the measurement, and the ability to roll back." }
  ],

  eval: [
    { p: "The questions that separate having shipped a model from being able to train one." },
    { qa: [
      { q: "What is the leading cause of models failing in production?",
        a: "Training-serving skew — the preprocessing at training time and at serving time disagree, so the model sees features meaning something different from what it learned, with no error raised and only unexplained accuracy loss." },
      { q: "How do you actually fix training-serving skew?",
        a: "Structurally, not by discipline — use one preprocessing implementation for both paths, or a feature store, and best of all log the features actually used at serving time and train the next model on those, which removes the cause entirely." },
      { q: "How does data drift differ from concept drift?",
        a: "Data drift is `P(X)` changing, meaning the inputs look different, while concept drift is `P(y|X)` changing, meaning the relationship itself moved. Crucially, data drift is detectable immediately without labels while concept drift is not." },
      { q: "How is PSI computed and how is it read?",
        a: "`Σ (actual − expected)·ln(actual/expected)` over buckets defined by the training set's deciles. Below 0.10 is normal, 0.10-0.25 warrants investigation, and above 0.25 suggests retraining." },
      { q: "Why must PSI's bucket edges come from the training set?",
        a: "Because recomputing them each time makes the current shares 10% per bucket by definition, and comparing that against a nominal 10% expectation yields PSI = 0 forever — the edges must stay frozen from the training set." },
      { q: "Why do late labels change everything?",
        a: "Because you learn the model broke long after it did — a churn model with 30-90 day labels must rely on label-free signals such as PSI and the prediction distribution as its primary alarm." },
      { q: "What does the prediction distribution tell you?",
        a: "It is the fastest warning available that needs no labels at all — if the model used to flag 2% as risky and now flags 15%, something is certainly wrong, whether in the data or the model." },
      { q: "What is a feedback loop in a recommender, and how is it addressed?",
        a: "The model suggests A, users click A, the data confirms A is good, so it suggests A more, while other items are never shown and never gather evidence. The metric improves while the system narrows. The remedy is a small randomised fraction to collect unbiased data." },
      { q: "What is the safe order for releasing a new model?",
        a: "Shadow, which takes real traffic without using its output; then a 1-5% canary; then an A/B test measuring the business outcome; then a gradual rollout to 100% — with rollback conditions written before each stage begins." },
      { q: "What can and cannot shadow deployment catch?",
        a: "It catches training-serving skew, real latency, errors, and a prediction distribution that differs from the current model's, all with no risk to users. It cannot catch business impact or feedback loops, because nothing acts on its output." },
      { q: "Why watch p99 latency rather than the mean?",
        a: "Because a 50 ms mean with a 2-second p99 means one user in a hundred waits a very long time, and in a system of chained calls those tails compound into a serious problem." },
      { q: "Where is the latency bottleneck usually?",
        a: "In the feature fetch, not the model forward — in the worked 200 ms budget the feature fetch took 60 ms, 40% of the total, against the model's 45 ms, so people optimise the model when they should fix the data access." },
      { q: "What does batching trade away?",
        a: "Latency for throughput — moving from batch 1 to 32 gave 8.6× the throughput at 3.8× the per-request latency. Dynamic batching finds that balance automatically under a latency ceiling." },
      { q: "To detect an effect half as large in an A/B test, how much more sample is needed?",
        a: "Four times as much, because the required sample scales with the inverse square of the effect — detecting +1.0 percentage points needed 8,149 per arm while +0.5 needed 31,199." },
      { q: "What do A/B planners forget most often?",
        a: "That accumulating the sample is not the same as being able to measure it — with 30-day labels, another 30 days must pass after the sample is complete before any conclusion is possible." },
      { q: "How should a decision threshold be chosen?",
        a: "From the real costs of the two error types, not from F1 or a default of 0.5 — the total-cost-minimising threshold is `C_FP / (C_FP + C_FN)`, which at 20 against 150 gives 0.12, far below 0.5." },
      { q: "Should you start with batch or online prediction?",
        a: "Batch whenever possible, because it is several times cheaper and far easier to debug, and many tasks do not genuinely need online — the requester merely assumes they do." },
      { q: "What is a feature store's most important property?",
        a: "Point-in-time correctness — returning only what was genuinely known at that moment, which prevents leakage at the infrastructure level, on top of preventing skew and enabling reuse across teams." },
      { q: "What must be logged on every request?",
        a: "The request id, the timestamp, **the features actually used after transformation**, the prediction, the model version, latency per stage, and the final decision after business rules. Without the real features, a wrong prediction cannot be traced at all." },
      { q: "Why tie the model version to a git commit and a data hash?",
        a: "Because if you cannot say which data and which commit produced the running model, you cannot rebuild it, roll back, or debug it — and it is worth recording whether the training code was uncommitted at the time." },
      { q: "Why are aggregate metrics dangerous?",
        a: "Because they always hide subgroup problems — a new product category at 4% of orders can be predicted almost randomly while the overall metric barely moves, which is why segment metrics are required." },
      { q: "When should ML not be used at all?",
        a: "When the logic can be written as clear rules, when there is very little data, when mistakes are unacceptable, when nobody will maintain it, and when no labels exist or can be obtained. If three rules capture 90% of the value, a model adding 3% but requiring permanent maintenance is usually not worth it." },
      { q: "If there is only time for three things, what should they be?",
        a: "One preprocessing implementation for both paths, logging features and predictions on every request, and a prediction-distribution dashboard. Those three deliver over 80% of full MLOps and should come before Kubernetes or a feature store." }
    ]},
    { h: "Further reading" },
    { links: [
      { label: "Rules of Machine Learning (Google)", url: "https://developers.google.com/machine-learning/guides/rules-of-ml", note: "43 rules from real experience; worth reading once for a whole career" },
      { label: "Hidden Technical Debt in Machine Learning Systems", url: "https://papers.nips.cc/paper/2015/hash/86df7dcfd896fcaf2674f757a2463eba-Abstract.html", note: "The paper showing model code is a tiny fraction of a real system" },
      { label: "Machine Learning Design Patterns — code and notebooks", url: "https://github.com/GoogleCloudPlatform/ml-design-patterns", note: "The notebooks accompanying Machine Learning Design Patterns — reusable patterns for serving, retraining and feature management" },
      { label: "Machine Learning Systems Design (Chip Huyen)", url: "https://huyenchip.com/machine-learning-systems-design/toc.html", note: "The free Machine Learning Systems Design book, from framing the problem through to monitoring" },
      { label: "Monitoring Machine Learning Models in Production", url: "https://christophergs.com/machine%20learning/2020/03/14/how-to-monitor-machine-learning-models/", note: "The three metric layers and how to set alerts, in detail" },
      { label: "Evidently — Data Drift documentation", url: "https://docs.evidentlyai.com/", note: "An open-source drift tool, with definitions of PSI and other measures" },
      { label: "MLflow — Model Registry", url: "https://mlflow.org/docs/latest/model-registry.html", note: "Versioning models and tying them to the run that produced them" },
      { label: "Feast — Feature Store", url: "https://docs.feast.dev/", note: "An open-source feature store, with point-in-time correctness explained" },
      { label: "Trustworthy Online Controlled Experiments", url: "https://experimentguide.com/", note: "The reference on running A/B tests correctly, including the common traps" }
    ]}
  ]
};
