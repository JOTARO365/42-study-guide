/* ============================================================
   data.subjects.js — แท็บ "โจทย์ต้นฉบับ"

   ข้อกำหนดของแต่ละโจทย์ในรูปที่อ่านแล้วลงมือทำได้: สิ่งที่ต้องส่ง,
   ฟังก์ชันที่อนุญาต, ข้อบังคับของ mandatory และ bonus, และเกณฑ์ตรวจ
   เรียบเรียงใหม่จาก subject ต้นฉบับ ไม่ใช่การคัดลอก

   ต้องโหลดไฟล์นี้ "หลัง" data ทุกตัว เพราะมันเติม section ให้หน้าที่มีอยู่แล้ว
   ============================================================ */
(function () {
  var S = {};

  /* ============================== libft ============================== */
  S.libft = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียนไลบรารี C ของตัวเองชื่อ `libft.a` ที่รวมฟังก์ชันอเนกประสงค์ไว้ใช้ต่อในทุกโปรเจกต์หลังจากนี้. แบ่งเป็นสามส่วน: เขียน libc ขึ้นมาใหม่, ฟังก์ชันเสริมที่ libc ไม่มี, และ (bonus) ชุดจัดการ linked list" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`libft.a`"],
        ["Files to Submit", "`Makefile`, `libft.h`, `ft_*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (+ `bonus` เมื่อทำส่วน bonus)"],
        ["External Function", "เฉพาะที่ระบุในแต่ละส่วน — ห้ามเรียกฟังก์ชันภายนอกอื่น"],
        ["Libft authorized", "n/a (นี่คือตัว libft เอง)"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "**Part 1 — libc (23 ตัว)**: `isalpha` `isdigit` `isalnum` `isascii` `isprint` `strlen` `memset` `bzero` `memcpy` `memmove` `strlcpy` `strlcat` `toupper` `tolower` `strchr` `strrchr` `strncmp` `memchr` `memcmp` `strnstr` `atoi` `calloc` `strdup` — พฤติกรรมและ prototype ต้องตรง man page เป๊ะ เปลี่ยนแค่ชื่อเป็น `ft_`",
        "**Part 2 — เพิ่มเติม (11 ตัว)**: `ft_substr` `ft_strjoin` `ft_strtrim` `ft_split` `ft_itoa` `ft_strmapi` `ft_striteri` `ft_putchar_fd` `ft_putstr_fd` `ft_putendl_fd` `ft_putnbr_fd`",
        "ฟังก์ชันตระกูล is* ต้องคืน **1 เมื่อตรง และ 0 เมื่อไม่ตรง** เท่านั้น",
        "`ft_calloc` ต้องคืน pointer ที่ free ได้เสมอแม้ `nmemb` หรือ `size` เป็น 0",
        "ห้ามใส่คีย์เวิร์ด `restrict` ใน prototype และห้ามคอมไพล์ด้วย `-std=c99`",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้ามประกาศ global variable** เด็ดขาด",
        "ฟังก์ชันช่วยต้องเป็น `static` เพื่อจำกัดขอบเขตไว้ในไฟล์นั้น",
        "ไฟล์ทั้งหมดอยู่ที่ **root ของ repository** และห้ามส่งไฟล์ที่ไม่ได้ใช้",
        "ทุก `.c` ต้องคอมไพล์ผ่านด้วย `-Wall -Wextra -Werror`",
        "ต้องสร้าง archive ด้วยคำสั่ง `ar` เท่านั้น — **ห้ามใช้ `libtool`**",
        "`libft.a` ต้องถูกสร้างที่ root ของ repository",
      ]},
      { h: "Bonus" },
      { ul: [
        "**Part 3 — linked list (9 ตัว)**: `ft_lstnew` `ft_lstadd_front` `ft_lstsize` `ft_lstlast` `ft_lstadd_back` `ft_lstdelone` `ft_lstclear` `ft_lstiter` `ft_lstmap`",
        "ต้องใช้ struct `t_list` ที่โจทย์กำหนด (`void *content` + `struct s_list *next`)",
        "ไฟล์ bonus ต้องลงท้ายด้วย `_bonus.c` และมี rule `bonus` แยกใน Makefile",
        "bonus จะถูกตรวจก็ต่อเมื่อ mandatory **สมบูรณ์แบบ** เท่านั้น",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "`make` สองครั้งติดกัน ครั้งที่สองต้องขึ้น Nothing to be done (ไม่ relink)",
        "`norminette *.c *.h` ไม่มี error",
        "เทียบค่าคืนกับของจริงทุกตัว ไม่ใช่แค่ผลลัพธ์ — `strlcpy` กับ `strlcat` พังที่ค่าคืนเป็นหลัก",
        "valgrind ไม่มี leak โดยเฉพาะเส้นทางที่ malloc ล้มกลางคัน (`ft_split`, `ft_lstmap`)",
        "README.md ตามข้อกำหนดของ subject (บรรทัดแรกเป็นตัวเอียงระบุ login)",
      ]},
      { note: "อ้างอิง: Libft subject v19.2" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Write your own C library, `libft.a`, holding general-purpose functions you will reuse in every later project. Three parts: reimplement libc, add functions libc does not have, and (bonus) a linked-list toolkit." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`libft.a`"],
        ["Files to Submit", "`Makefile`, `libft.h`, `ft_*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (plus `bonus` when you do part 3)"],
        ["External Function", "only those listed per part — no other external calls"],
        ["Libft authorized", "n/a (this is libft)"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "**Part 1 — libc (23)**: `isalpha` `isdigit` `isalnum` `isascii` `isprint` `strlen` `memset` `bzero` `memcpy` `memmove` `strlcpy` `strlcat` `toupper` `tolower` `strchr` `strrchr` `strncmp` `memchr` `memcmp` `strnstr` `atoi` `calloc` `strdup` — prototypes and behaviour must match the man pages exactly; only the `ft_` prefix differs",
        "**Part 2 — additional (11)**: `ft_substr` `ft_strjoin` `ft_strtrim` `ft_split` `ft_itoa` `ft_strmapi` `ft_striteri` `ft_putchar_fd` `ft_putstr_fd` `ft_putendl_fd` `ft_putnbr_fd`",
        "The is* family must return **exactly 1 on a match and 0 otherwise**",
        "`ft_calloc` must return a free-able pointer even when `nmemb` or `size` is 0",
        "The `restrict` keyword is forbidden in your prototypes, and so is compiling with `-std=c99`",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**Global variables are strictly forbidden**",
        "Helper functions must be `static`, scoped to their own file",
        "Every file lives at the **root of the repository**, and unused files must not be submitted",
        "Every `.c` must compile under `-Wall -Wextra -Werror`",
        "The archive must be built with `ar` — **`libtool` is forbidden**",
        "`libft.a` must be produced at the repository root",
      ]},
      { h: "Bonus" },
      { ul: [
        "**Part 3 — linked list (9)**: `ft_lstnew` `ft_lstadd_front` `ft_lstsize` `ft_lstlast` `ft_lstadd_back` `ft_lstdelone` `ft_lstclear` `ft_lstiter` `ft_lstmap`",
        "Use the `t_list` struct the subject defines (`void *content` plus `struct s_list *next`)",
        "Bonus files must end in `_bonus.c`, with a separate `bonus` rule in the Makefile",
        "The bonus is graded only if the mandatory part is **perfect**",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Run `make` twice; the second must print Nothing to be done (no relinking)",
        "`norminette *.c *.h` reports zero errors",
        "Compare return values against the originals, not just output — `strlcpy` and `strlcat` fail on the return value first",
        "Valgrind clean, especially the paths where a malloc fails part-way (`ft_split`, `ft_lstmap`)",
        "A README.md per the subject (first line italic, naming your login)",
      ]},
      { note: "Source: Libft subject v19.2" },
    ],
  };

  /* ============================= ft_printf ============================= */
  S.ft_printf = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียน `ft_printf()` ขึ้นมาใหม่ให้เลียนแบบ `printf()` ของ libc แล้วแพ็กเป็นไลบรารี `libftprintf.a`. หัวใจคือ **variadic function** — ฟังก์ชันที่รับอาร์กิวเมนต์ไม่จำกัดจำนวนและไม่รู้ชนิดล่วงหน้า" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program name", "`libftprintf.a`"],
        ["Turn in files", "`Makefile`, `*.h`, `*/*.h`, `*.c`, `*/*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Prototype", "`int ft_printf(const char *, ...);`"],
        ["External functs.", "`malloc` `free` `write` `va_start` `va_arg` `va_copy` `va_end`"],
        ["Libft authorized", "**Yes** — ใส่โฟลเดอร์ libft ไว้ใน repo แล้วให้ Makefile คอมไพล์ก่อนได้"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "รองรับ conversion: `%c` `%s` `%p` `%d` `%i` `%u` `%x` `%X` `%%`",
        "**`%p`** พิมพ์ address ในรูป `0x` ตามด้วยเลขฐาน 16 ตัวพิมพ์เล็ก",
        "**`%x` / `%X`** คือเลขฐาน 16 แบบ unsigned ตัวพิมพ์เล็ก/ใหญ่",
        "**ค่าที่คืนต้องเท่ากับจำนวนตัวอักษรที่พิมพ์จริง** — ตรงนี้คือสิ่งที่ tester จับก่อนอย่างอื่น",
        "ห้ามทำ buffer management แบบที่ `printf()` ตัวจริงทำ",
        "ห้ามใช้ฟังก์ชันตระกูล `printf()` ของ libc ทุกตัว",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้าม global variable**",
        "ต้องคอมไพล์ผ่าน `-Wall -Wextra -Werror` และ archive ด้วย `ar` (ห้าม `libtool`)",
        "`libftprintf.a` ต้องอยู่ที่ root ของ repository",
        "ถ้าใช้ libft: ต้องก๊อปทั้งโฟลเดอร์ `libft/` เข้ามาพร้อม Makefile ของมัน แล้วให้ Makefile หลักคอมไพล์ libft ก่อน และต้องเอา `libft.a` มารวมเป็น `libftprintf.a` ให้ครบ",
      ]},
      { h: "Bonus (ไม่ต้องทำครบทุกข้อ)" },
      { ul: [
        "รองรับ flag ผสมกันได้ของ `-` `0` `.` และ **field minimum width** บนทุก conversion",
        "รองรับ flag `#`, `+` และ **ช่องว่าง** (ใช่ หนึ่งในนั้นคือตัวอักขระเว้นวรรค)",
        "โจทย์เตือนเองว่า: ถ้าคิดจะทำ bonus **ให้ออกแบบเผื่อไว้ตั้งแต่ต้น** ไม่งั้นจะติดกับดักของวิธีที่คิดง่ายเกินไป",
        "bonus ตรวจก็ต่อเมื่อ mandatory **สมบูรณ์แบบ**",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "เทียบกับ `printf()` จริงในโปรแกรมเดียวกัน โดยเทียบ **ทั้ง byte ที่พิมพ์และค่าที่คืน**",
        "เคสขอบที่ต้องผ่าน: `%s` กับ NULL, `%p` กับ NULL, `%d` กับ INT_MIN, `%u` กับ -1, `%c` กับค่า 0, format ว่าง, `%` ตัวสุดท้ายของ string",
        "`make` สองครั้ง ไม่ relink; norminette ศูนย์ error; valgrind สะอาด",
      ]},
      { note: "อ้างอิง: ft_printf subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Reimplement `printf()` as `ft_printf()` and ship it as `libftprintf.a`. The real lesson is **variadic functions** — a function taking an unknown number of arguments of unknown types." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program name", "`libftprintf.a`"],
        ["Turn in files", "`Makefile`, `*.h`, `*/*.h`, `*.c`, `*/*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Prototype", "`int ft_printf(const char *, ...);`"],
        ["External functs.", "`malloc` `free` `write` `va_start` `va_arg` `va_copy` `va_end`"],
        ["Libft authorized", "**Yes** — keep a libft folder in the repo and have your Makefile build it first"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "Handle the conversions `%c` `%s` `%p` `%d` `%i` `%u` `%x` `%X` `%%`",
        "**`%p`** prints an address as `0x` followed by lowercase hexadecimal",
        "**`%x` / `%X`** are unsigned hexadecimal, lower and upper case",
        "**The return value must equal the number of characters actually printed** — this is what testers check before anything else",
        "Do not implement the buffer management real `printf()` does",
        "None of libc's `printf()` family may be used",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**No global variables**",
        "Must compile under `-Wall -Wextra -Werror` and be archived with `ar` (never `libtool`)",
        "`libftprintf.a` must sit at the repository root",
        "If you use libft: copy the whole `libft/` folder in with its own Makefile, have the main Makefile build it first, and make sure `libft.a`'s objects end up inside `libftprintf.a`",
      ]},
      { h: "Bonus (you need not do all of it)" },
      { ul: [
        "Any combination of the `-` `0` `.` flags plus the **minimum field width**, on every conversion",
        "The `#`, `+` and **space** flags (yes, one of them is a space character)",
        "The subject warns you itself: if you intend to do the bonus, **design for it from the start** or a naive approach will trap you",
        "The bonus is graded only if the mandatory part is **perfect**",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Compare against real `printf()` in the same program, checking **both the bytes written and the return value**",
        "Edge cases that must pass: `%s` with NULL, `%p` with NULL, `%d` with INT_MIN, `%u` with -1, `%c` with the value 0, an empty format, and a trailing `%`",
        "`make` twice with no relink; norminette clean; valgrind clean",
      ]},
      { note: "Source: ft_printf subject" },
    ],
  };

  /* =========================== get_next_line =========================== */
  S.get_next_line = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียนฟังก์ชันที่คืน **หนึ่งบรรทัด** จาก file descriptor ต่อการเรียกหนึ่งครั้ง เรียกซ้ำไปเรื่อย ๆ ก็ได้บรรทัดถัดไปจนหมดไฟล์. ความยากอยู่ที่ `read` ส่งข้อมูลมาเป็นก้อนขนาดคงที่ซึ่งไม่ตรงกับขอบบรรทัด และของที่อ่านเกินมาแล้วเอาคืนไม่ได้" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Function Name", "`get_next_line`"],
        ["Prototype", "`char *get_next_line(int fd);`"],
        ["Files to Submit", "`get_next_line.c`, `get_next_line_utils.c`, `get_next_line.h` — **ทั้งหมดอยู่ที่ root ของ repository**"],
        ["Parameters", "`fd`: file descriptor ที่จะอ่าน"],
        ["Return Value", "บรรทัดที่อ่านได้ / `NULL` เมื่อไม่มีอะไรให้อ่านแล้ว หรือเกิด error"],
        ["External Function", "`read` `malloc` `free` — **เท่านี้**"],
        ["Libft authorized", "**No** — ต้อง self-contained"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "เรียกซ้ำ ๆ (เช่นในลูป) แล้วต้องอ่านไฟล์ได้ทีละบรรทัดจนจบ",
        "บรรทัดที่คืนต้อง **รวม `\\n` ท้ายบรรทัดด้วย** ยกเว้นบรรทัดสุดท้ายที่ไฟล์ไม่ได้ลงท้ายด้วย newline",
        "ต้องทำงานถูกต้องไม่ว่าคอมไพล์ด้วย `BUFFER_SIZE` เท่าไร — ค่ามาจากบรรทัดคอมไพล์ `-D BUFFER_SIZE=n`",
        "ต้องทำงานได้ทั้งกับไฟล์และกับ standard input",
        "ห้ามใช้ `lseek()` และ**ห้ามอ่านทั้งไฟล์เข้ามาทีเดียว**",
        "ห้ามใช้ตัวแปร global — ใช้ `static` ในฟังก์ชันแทน",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "ไฟล์ทั้งสามต้องอยู่ที่ **root ของ repository**",
        "`-Wall -Wextra -Werror` ต้องผ่าน และต้องคอมไพล์ได้แม้ไม่ได้ส่ง `-D BUFFER_SIZE` มา (header ต้องมี `#ifndef` fallback)",
        "พฤติกรรมเมื่อไฟล์ถูกแก้ระหว่างอ่าน หรือเมื่ออ่านไฟล์ binary ถือเป็น **undefined** — ไม่ต้องรับผิดชอบ",
        "ต้องไม่มี memory leak",
      ]},
      { h: "Bonus" },
      { ul: [
        "ใช้ตัวแปร `static` **เพียงตัวเดียว**",
        "จัดการหลาย file descriptor พร้อมกันได้ — อ่าน fd 3, 4, 5 สลับกันแล้วข้อมูลต้องไม่ปนกัน",
        "ไฟล์ bonus แยกต่างหาก: `get_next_line_bonus.c`, `get_next_line_utils_bonus.c`, `get_next_line_bonus.h`",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "คอมไพล์และรันครบทุกค่า `BUFFER_SIZE` = 1, 2, 5, 42, 1000, 9999 ในลูปเดียว",
        "เคสที่ต้องผ่าน: ไฟล์ว่าง, ไฟล์ที่ไม่ลงท้ายด้วย newline, ไฟล์ที่มีแต่ newline, บรรทัดยาวมาก, `fd` ติดลบ, fd ที่ปิดแล้ว, `BUFFER_SIZE` เป็น 0, และ stdin ผ่าน pipe",
        "เทียบผลกับ `getline(3)` ทีละบรรทัด แม่นกว่าการ diff ทั้งไฟล์",
        "valgrind สะอาดทุกค่า BUFFER_SIZE ที่ทดสอบ",
      ]},
      { note: "อ้างอิง: get_next_line subject v14.3" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Write a function that returns **one line** from a file descriptor per call, so that calling it repeatedly walks the whole file. The difficulty is that `read` delivers fixed-size blocks that do not align with line boundaries, and whatever it read past the newline cannot be un-read." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Function Name", "`get_next_line`"],
        ["Prototype", "`char *get_next_line(int fd);`"],
        ["Files to Submit", "`get_next_line.c`, `get_next_line_utils.c`, `get_next_line.h` — **all at the repository root**"],
        ["Parameters", "`fd`: the file descriptor to read from"],
        ["Return Value", "the line read, or `NULL` when there is nothing left or an error occurred"],
        ["External Function", "`read` `malloc` `free` — **that is all**"],
        ["Libft authorized", "**No** — it must be self-contained"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "Repeated calls (in a loop, say) must walk the file one line at a time to the end",
        "The returned line must **include its trailing `\\n`**, except on a final line where the file does not end in one",
        "It must work for any `BUFFER_SIZE` it is compiled with — the value comes from the compile line, `-D BUFFER_SIZE=n`",
        "It must work on files and on standard input alike",
        "`lseek()` is forbidden, and so is **reading the whole file at once**",
        "No global variables — a `static` inside the function is what the subject permits",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "All three files must be at the **repository root**",
        "`-Wall -Wextra -Werror` must pass, and it must still compile with no `-D BUFFER_SIZE` given (the header needs an `#ifndef` fallback)",
        "Behaviour when the file changes while reading, or when reading a binary file, is **undefined** — not your problem",
        "No memory leaks",
      ]},
      { h: "Bonus" },
      { ul: [
        "Use **only one** `static` variable",
        "Handle several file descriptors at once — reading fd 3, 4 and 5 in turn must not mix their data",
        "Separate bonus files: `get_next_line_bonus.c`, `get_next_line_utils_bonus.c`, `get_next_line_bonus.h`",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Compile and run at `BUFFER_SIZE` = 1, 2, 5, 42, 1000 and 9999 in one loop",
        "Cases that must pass: an empty file, a file with no trailing newline, a file of only newlines, very long lines, a negative `fd`, a closed fd, `BUFFER_SIZE` of 0, and stdin through a pipe",
        "Compare against `getline(3)` line by line — sharper than diffing the whole file",
        "Valgrind clean at every BUFFER_SIZE you tested",
      ]},
      { note: "Source: get_next_line subject v14.3" },
    ],
  };

  /* ============================= push_swap ============================= */
  S.push_swap = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เรียงจำนวนเต็มบน stack a โดยใช้ stack b ช่วย ผ่านชุดคำสั่งจำกัด 11 ตัว แล้ว **พิมพ์ลำดับคำสั่งที่สั้นที่สุดเท่าที่ทำได้** ออก stdout. subject v1.1 เปลี่ยนเป็น **งานกลุ่ม 2 คนพอดี** และบังคับให้มี 4 strategy กับตัววัด disorder" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`push_swap`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (+ `bonus`)"],
        ["Arguments", "รายการจำนวนเต็ม (arg แรก = ยอด stack) + selector ที่ไม่บังคับ"],
        ["External Function", "`read` `write` `malloc` `free` `exit` และ `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
        ["Group", "**2 คนพอดี** — README บรรทัดแรกต้องมี login ทั้งสองคน"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "**11 operations**: `sa` `sb` `ss` `pa` `pb` `ra` `rb` `rr` `rra` `rrb` `rrr` คั่นด้วย `\\n` และห้ามมีอะไรอื่น",
        "**วัด disorder ก่อนขยับ stack** — จำนวนคู่ที่สลับกันหารด้วยจำนวนคู่ทั้งหมด ได้ค่า 0 ถึง 1",
        "**4 strategy ในไฟล์เดียว** เลือกได้ตอนรัน: `--simple` O(n²), `--medium` O(n·√n), `--complex` O(n log n), `--adaptive` (ค่าปริยาย) เลือกตาม disorder",
        "adaptive ต้องเคารพเพดานต่อช่วง: disorder < 0.2 ใช้ O(n²), 0.2-0.5 ใช้ O(n·√n), ≥ 0.5 ใช้ O(n log n)",
        "**complexity class ที่เคลมต้องจริงในหน่วย operation ที่ generate** ไม่ใช่ Big-O ของ algorithm แบบ array",
        "`--bench` (ไม่บังคับ) พิมพ์ออก **stderr**: disorder เป็น % ทศนิยม 2 ตำแหน่ง, ชื่อ strategy + class, จำนวน op รวม, และจำนวนแยกของ op ทั้ง 11 ชนิด",
        "ไม่มี argument → ไม่พิมพ์อะไรแล้วคืน prompt; error → พิมพ์ `Error` + `\\n` ออก **stderr**",
      ]},
      { h: "เกณฑ์ผลงาน (วัดจาก strategy ปริยาย)" },
      { table: { head: ["จำนวน", "excellent", "good", "ผ่านขั้นต่ำ"], rows: [
        ["100 ตัว", "< 700 ops", "< 1500", "< 2000"],
        ["500 ตัว", "< 5500 ops", "< 8000", "< 12000"],
      ]}},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้าม global variable**",
        "Makefile ต้องไม่ relink",
        "error ที่ต้องจับ: argument ที่ไม่ใช่จำนวนเต็ม, ค่าเกินช่วง int, ค่าซ้ำ",
        "README ต้องอธิบายเหตุผลของ threshold, เทคนิคที่ใช้ในแต่ละช่วง และข้อโต้แย้งเรื่อง complexity (ขอบบน) ทั้งเวลาและพื้นที่",
      ]},
      { h: "Bonus — checker" },
      { ul: [
        "โปรแกรม `checker` รับ stack a เป็น argument แล้วอ่าน operation จาก stdin ทีละบรรทัด",
        "อ่านจบแล้วนำไป apply: ถ้า a เรียงและ b ว่าง → พิมพ์ `OK` ไม่งั้น `KO`",
        "operation ที่ไม่รู้จักหรือรูปแบบผิด → `Error` ออก stderr",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ ซึ่งในโปรเจกต์นี้แปลว่า **ผ่าน benchmark ครบทุกข้อ**",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "ทุก strategy × n = 2, 3, 5, 100, 200, 500 ต่อเข้า checker แล้วได้ `OK` ทุกครั้ง",
        "input สุ่มทดสอบได้แค่ช่วง disorder ≈ 0.5 — ต้องสร้าง input อีกสองช่วงเองแล้วยืนยันจาก `--bench`",
        "valgrind สะอาดทั้งตอนรันปกติและตอน argument ผิด",
      ]},
      { note: "อ้างอิง: Push_swap subject v1.1" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Sort integers on stack a using stack b and a fixed set of 11 instructions, printing **the shortest sequence you can find** to stdout. Subject v1.1 makes this a **two-person group project** and requires four strategies plus a disorder metric." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`push_swap`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (plus `bonus`)"],
        ["Arguments", "a list of integers (the first is the top of the stack) plus an optional selector"],
        ["External Function", "`read` `write` `malloc` `free` `exit`, and `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
        ["Group", "**exactly 2 learners** — both logins on the README's first line"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "**11 operations**: `sa` `sb` `ss` `pa` `pb` `ra` `rb` `rr` `rra` `rrb` `rrr`, separated by `\\n` and nothing else",
        "**Measure disorder before moving anything** — inverted pairs over total pairs, a number from 0 to 1",
        "**Four strategies in one binary**, selectable at runtime: `--simple` O(n²), `--medium` O(n·√n), `--complex` O(n log n), and `--adaptive` (the default) which routes on disorder",
        "Adaptive must respect the ceiling per regime: disorder < 0.2 is O(n²), 0.2-0.5 is O(n·√n), ≥ 0.5 is O(n log n)",
        "**The complexity class you claim must hold in the operation model** — the instructions generated, not the Big-O of an array-based version",
        "`--bench` (optional) prints to **stderr**: disorder as a percentage with two decimals, the strategy name and class, the total operation count, and the count of each of the 11 operations",
        "No arguments means print nothing and return the prompt; errors print `Error` and `\\n` to **stderr**",
      ]},
      { h: "Performance targets (measured on the default strategy)" },
      { table: { head: ["Size", "excellent", "good", "minimum to pass"], rows: [
        ["100 numbers", "< 700 ops", "< 1500", "< 2000"],
        ["500 numbers", "< 5500 ops", "< 8000", "< 12000"],
      ]}},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**No global variables**",
        "The Makefile must not relink",
        "Errors to catch: arguments that are not integers, values outside int range, duplicates",
        "The README must document the rationale for your thresholds, the technique used in each regime, and a complexity argument (upper bounds) for time and space",
      ]},
      { h: "Bonus — the checker" },
      { ul: [
        "A `checker` program takes stack a as arguments and reads operations from stdin, one per line",
        "After reading them all it applies them: if a is sorted and b is empty it prints `OK`, otherwise `KO`",
        "An unknown or malformed operation prints `Error` to stderr",
        "The bonus is graded only if the mandatory part is perfect — which here means **every benchmark validated without exception**",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Every strategy at n = 2, 3, 5, 100, 200 and 500, piped through the checker, returning `OK` every time",
        "Random input only exercises disorder ≈ 0.5 — build inputs for the other two regimes and confirm from `--bench` which strategy ran",
        "Valgrind clean on normal runs and on invalid arguments",
      ]},
      { note: "Source: Push_swap subject v1.1" },
    ],
  };

  /* ============================== pipex =============================== */
  S.pipex = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียนโปรแกรมที่ทำงานเทียบเท่ากับคำสั่ง shell `< file1 cmd1 | cmd2 > file2` เป๊ะ ๆ. หัวใจคือ `pipe` `fork` `dup2` และ `execve` — คือการต่อ stdout ของโปรเซสหนึ่งเข้ากับ stdin ของอีกโปรเซส" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`pipex`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "`file1 cmd1 cmd2 file2`"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `access` `dup` `dup2` `execve` `exit` `fork` `pipe` `unlink` `wait` `waitpid` + `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "รับ 4 argument: `file1` และ `file2` เป็นชื่อไฟล์, `cmd1` และ `cmd2` เป็นคำสั่ง shell พร้อมพารามิเตอร์",
        "พฤติกรรมต้องเหมือน `< file1 cmd1 | cmd2 > file2` ทุกประการ",
        "ตัวอย่าง: `./pipex infile \"ls -l\" \"wc -l\" outfile` ต้องได้ผลเท่ากับ `< infile ls -l | wc -l > outfile`",
        "ต้องหา executable จาก `PATH` เอง เพราะ `execve` ไม่ค้นให้",
        "**ถ้าไม่แน่ใจว่า error ไหนควรทำยังไง ให้ทำเหมือนที่ shell ทำกับคำสั่งเดียวกัน** — นี่คือกฎที่ subject บอกตรง ๆ",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "Makefile ต้องไม่ relink",
        "**โปรแกรมต้องไม่จบแบบผิดปกติเด็ดขาด** — segfault, bus error, double free ถือว่าพัง",
        "ต้องไม่มี memory leak",
      ]},
      { h: "Bonus" },
      { ul: [
        "รองรับ **คำสั่งกี่ตัวก็ได้**: `./pipex file1 cmd1 cmd2 cmd3 ... cmdn file2`",
        "รองรับ `<<` และ `>>` เมื่อ argument แรกคือ `here_doc` — `./pipex here_doc LIMITER cmd cmd1 file`",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "เทียบกับ shell จริงทุกเคส รวมถึง exit status",
        "เคสที่ต้องผ่าน: infile ไม่มีอยู่, infile ไม่มีสิทธิ์อ่าน, คำสั่งไม่มีอยู่, คำสั่งมีอยู่แต่ไม่มีสิทธิ์รัน, `PATH` ไม่ถูกตั้งไว้เลย, argument ว่าง",
        "ตรวจว่าปิด fd ครบทั้งฝั่งพ่อและลูก ไม่งั้นโปรแกรมจะค้างรอ EOF ที่ไม่มีวันมา",
        "valgrind ที่ตัวพ่อ และตรวจว่าไม่มี zombie process ค้าง",
      ]},
      { note: "อ้างอิง: Pipex subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Write a program that behaves exactly like the shell command `< file1 cmd1 | cmd2 > file2`. The core of it is `pipe`, `fork`, `dup2` and `execve` — wiring one process's stdout into another's stdin." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`pipex`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "`file1 cmd1 cmd2 file2`"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `access` `dup` `dup2` `execve` `exit` `fork` `pipe` `unlink` `wait` `waitpid`, plus `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "Take four arguments: `file1` and `file2` are file names, `cmd1` and `cmd2` are shell commands with their parameters",
        "Behave exactly like `< file1 cmd1 | cmd2 > file2`",
        "Example: `./pipex infile \"ls -l\" \"wc -l\" outfile` must equal `< infile ls -l | wc -l > outfile`",
        "Resolve executables from `PATH` yourself — `execve` does not search",
        "**When unsure how to handle an error, do what the shell does for the same command** — the subject says so directly",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "The Makefile must not relink",
        "**The program must never terminate unexpectedly** — a segfault, bus error or double free counts as broken",
        "No memory leaks",
      ]},
      { h: "Bonus" },
      { ul: [
        "Handle **any number of commands**: `./pipex file1 cmd1 cmd2 cmd3 ... cmdn file2`",
        "Handle `<<` and `>>` when the first argument is `here_doc` — `./pipex here_doc LIMITER cmd cmd1 file`",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Compare against the real shell in every case, including exit status",
        "Cases that must pass: a missing infile, an unreadable infile, a nonexistent command, a command that exists but is not executable, an unset `PATH`, and empty arguments",
        "Check every fd is closed in both parent and children, or the program hangs waiting for an EOF that never comes",
        "Valgrind on the parent, and confirm no zombie processes are left behind",
      ]},
      { note: "Source: Pipex subject" },
    ],
  };

  /* ============================= minitalk ============================= */
  S.minitalk = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียนโปรแกรมสื่อสารระหว่าง client กับ server โดยส่งข้อความผ่าน **UNIX signal เท่านั้น** และใช้ได้แค่สองสัญญาณคือ `SIGUSR1` กับ `SIGUSR2` — แปลว่าต้องเข้ารหัสข้อความทีละบิต" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`client` และ `server`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (+ `bonus`)"],
        ["Arguments", "server: ไม่มี · client: `<PID ของ server>` `<ข้อความ>`"],
        ["External Function", "`write` `signal` `sigemptyset` `sigaddset` `sigaction` `kill` `getpid` `malloc` `free` `pause` `sleep` `usleep` `exit` + `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "**server ต้องถูกรันก่อน** และตอนเริ่มต้องพิมพ์ PID ของตัวเองออกมา",
        "client รับสองพารามิเตอร์: PID ของ server และข้อความที่จะส่ง",
        "client ส่งข้อความไปให้ server แล้ว server พิมพ์ออกมา",
        "**server ต้องแสดงผลโดยไม่หน่วง** — โจทย์ระบุเกณฑ์ไว้ชัด: ถ้าแสดง 100 ตัวอักษรใช้เวลา 1 วินาที ถือว่าช้าเกินไป",
        "server ต้องรับข้อความจาก client หลายตัวติดกันได้โดยไม่ต้องรีสตาร์ต",
        "สื่อสารกันด้วย signal เท่านั้น และใช้ได้แค่ `SIGUSR1` กับ `SIGUSR2`",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "Makefile ต้องไม่ relink",
        "**อนุญาต global variable ได้ 1 ตัวต่อโปรแกรม** (client หนึ่ง server หนึ่ง) แต่ต้องอธิบายเหตุผลได้ตอนตรวจ",
        "ต้องจัดการ error ให้ครบ โปรแกรมห้ามจบแบบผิดปกติ",
        "ต้องไม่มี memory leak",
      ]},
      { h: "กับดักที่โจทย์เตือนไว้เอง" },
      { note: "Linux **ไม่เข้าคิว signal** เมื่อมีสัญญาณชนิดเดียวกันค้างอยู่ — ส่งเร็วเกินไปแล้วบิตจะหายไปเงียบ ๆ. นี่คือเหตุผลที่ต้องมีการ handshake หรือ delay และเป็นหัวใจของโปรเจกต์นี้จริง ๆ" },
      { h: "Bonus" },
      { ul: [
        "server ต้องตอบรับข้อความที่ได้รับ โดยส่ง signal กลับไปหา client",
        "รองรับอักขระ Unicode",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "ส่งข้อความยาว (หลายพันตัวอักษร) แล้วต้องไม่มีบิตหาย",
        "ส่งจาก client หลายตัวติดกันโดยไม่รีสตาร์ต server",
        "PID ผิด / ไม่ใช่ตัวเลข / ข้อความว่าง ต้องไม่ทำให้โปรแกรมพัง",
        "ใช้ `sigaction` แทน `signal` เพราะพฤติกรรมนิ่งกว่าและตั้ง `SA_SIGINFO` เพื่ออ่าน PID ของผู้ส่งได้",
      ]},
      { note: "อ้างอิง: Minitalk subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Write a client and a server that talk to each other using **UNIX signals only**, and only two of them: `SIGUSR1` and `SIGUSR2` — which means encoding the message one bit at a time." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`client` and `server`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re` (plus `bonus`)"],
        ["Arguments", "server: none · client: `<server PID>` `<message>`"],
        ["External Function", "`write` `signal` `sigemptyset` `sigaddset` `sigaction` `kill` `getpid` `malloc` `free` `pause` `sleep` `usleep` `exit`, plus `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "**The server starts first** and prints its own PID on launch",
        "The client takes two parameters: the server's PID and the string to send",
        "The client sends the string; the server prints it once received",
        "**The server must display without delay** — the subject is explicit: if printing 100 characters takes one second, it is too slow",
        "The server must accept strings from several clients in a row without restarting",
        "Communication is signals only, and only `SIGUSR1` and `SIGUSR2`",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "The Makefile must not relink",
        "**One global variable per program is allowed** (one for the client, one for the server), but you must justify it at evaluation",
        "Errors must be handled thoroughly; the program may never quit unexpectedly",
        "No memory leaks",
      ]},
      { h: "The trap the subject warns about itself" },
      { note: "Linux **does not queue signals** when one of that type is already pending — send too fast and bits vanish silently. That is why a handshake or a delay is needed, and it is the real substance of this project." },
      { h: "Bonus" },
      { ul: [
        "The server acknowledges each received message by signalling the client back",
        "Unicode character support",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Send a long message (thousands of characters) with no bits lost",
        "Send from several clients in a row without restarting the server",
        "A wrong PID, a non-numeric PID and an empty message must not break anything",
        "Prefer `sigaction` over `signal` — its behaviour is better defined, and `SA_SIGINFO` lets you read the sender's PID",
      ]},
      { note: "Source: Minitalk subject" },
    ],
  };

  /* ============================== so_long ============================= */
  S.so_long = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "สร้างเกม 2D พื้นฐานด้วย MiniLibX: ตัวละครเก็บของสะสมให้ครบแล้วออกทางประตู โดยเดินให้สั้นที่สุด. เป็นโปรเจกต์แรกที่ได้จับหน้าต่าง, event, texture และ game loop" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`so_long`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`, maps, textures"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "แผนที่นามสกุล `*.ber`"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · ทุกฟังก์ชันของ math (`-lm`) · ทุกฟังก์ชันของ MiniLibX · `gettimeofday()` · `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — เกม" },
      { ul: [
        "เป้าหมายคือเก็บของสะสมให้ครบแล้วออกทางประตู โดยใช้เส้นทางที่สั้นที่สุดเท่าที่ทำได้",
        "ต้องใช้ปุ่ม **W A S D** ในการเดิน 4 ทิศ: ขึ้น ลง ซ้าย ขวา",
        "เดินทะลุกำแพงไม่ได้",
        "**ทุกครั้งที่ขยับ ต้องพิมพ์จำนวนก้าวปัจจุบันออก shell**",
        "ต้องเป็นมุมมอง 2D (บนลงล่าง หรือด้านข้าง)",
      ]},
      { h: "Mandatory — หน้าต่างและแผนที่" },
      { ul: [
        "ต้องใช้ MiniLibX (เวอร์ชันบนเครื่องโรงเรียน หรือคอมไพล์จาก source)",
        "กด `ESC` ต้องปิดหน้าต่างและออกโปรแกรมอย่างสะอาด และกดกากบาทที่หน้าต่างก็ต้องออกได้เช่นกัน",
        "แผนที่ประกอบด้วย `0` พื้นว่าง, `1` กำแพง, `C` ของสะสม, `E` ทางออก, `P` จุดเริ่มของผู้เล่น",
        "แผนที่ต้องเป็นสี่เหลี่ยมผืนผ้าและ **ล้อมรอบด้วยกำแพงทุกด้าน**",
        "ต้องมีทางออกอย่างน้อย 1, ของสะสมอย่างน้อย 1 และผู้เล่น **1 คนเท่านั้น**",
        "ต้องตรวจว่ามีเส้นทางที่ไปเก็บของครบและถึงทางออกได้จริง",
        "แผนที่ผิดรูปแบบใด ๆ ต้องออกโปรแกรมพร้อมพิมพ์ `Error\\n` ตามด้วยข้อความอธิบาย",
      ]},
      { h: "Bonus" },
      { ul: [
        "มีศัตรูเดินลาดตระเวน แตะแล้วแพ้",
        "ใส่ sprite animation",
        "แสดงจำนวนก้าวบนหน้าจอแทนการพิมพ์ลง shell",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "แผนที่พัง: ไม่ปิดล้อม, แถวยาวไม่เท่ากัน, ไม่มี `P`/`E`/`C`, มี `P` เกินหนึ่ง, มีตัวอักษรแปลกปลอม, ไฟล์ว่าง, นามสกุลไม่ใช่ `.ber`, เดินไปเก็บของไม่ครบ",
        "ปิดหน้าต่างทั้งสองทาง (ESC และกากบาท) แล้ว valgrind ต้องสะอาด",
        "Makefile ต้องไม่ relink และต้องคอมไพล์ MiniLibX ให้ด้วย",
      ]},
      { note: "อ้างอิง: So Long subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Build a basic 2D game with MiniLibX: a character collects every collectible and then escapes through the exit, taking the shortest route. It is the first project where you touch windows, events, textures and a game loop." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`so_long`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`, maps, textures"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "a map with the `*.ber` extension"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · all of the math library (`-lm`) · all of MiniLibX · `gettimeofday()` · `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — the game" },
      { ul: [
        "The goal is to collect every collectible and then escape, by the shortest route possible",
        "Movement uses **W A S D** in four directions: up, down, left, right",
        "The player must not be able to walk into walls",
        "**Every move must print the current move count to the shell**",
        "The view must be 2D (top-down or profile)",
      ]},
      { h: "Mandatory — window and map" },
      { ul: [
        "MiniLibX is required, either the school-machine version or built from source",
        "`ESC` must close the window and exit cleanly, and so must clicking the window's close button",
        "The map is built from `0` empty, `1` wall, `C` collectible, `E` exit and `P` the player's start",
        "The map must be rectangular and **closed by walls on every side**",
        "There must be at least one exit, at least one collectible, and **exactly one** player",
        "You must verify a valid path exists that collects everything and reaches the exit",
        "Any invalid map must exit after printing `Error\\n` followed by an explicit message",
      ]},
      { h: "Bonus" },
      { ul: [
        "Patrolling enemies that make the player lose on contact",
        "Sprite animation",
        "Show the move count on screen instead of writing it to the shell",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Broken maps: not closed, ragged rows, missing `P`/`E`/`C`, more than one `P`, stray characters, an empty file, the wrong extension, and an unreachable collectible",
        "Close the window both ways (ESC and the close button) with valgrind clean afterwards",
        "The Makefile must not relink and must build MiniLibX too",
      ]},
      { note: "Source: So Long subject" },
    ],
  };

  /* ================================ fdf ================================ */
  S.fdf = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "อ่านไฟล์ `.fdf` ที่เก็บความสูงเป็นตาราง แล้ววาดเป็นภูมิประเทศ 3D แบบโครงลวด โดยเชื่อมจุด (x, y, z) ด้วยเส้นตรง และฉายภาพแบบ isometric" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`fdf`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`, ไฟล์ `.fdf` ตัวอย่าง"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "ไฟล์นามสกุล `*.fdf`"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · ทุกฟังก์ชันของ math (`-lm`) · ทุกฟังก์ชันของ MiniLibX · `gettimeofday()` · `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "ต้องแสดงโมเดลด้วย **isometric projection**",
        "พิกัดของภูมิประเทศมาจากไฟล์ `.fdf` ที่ส่งเข้าทาง command line",
        "ตัวเลขแต่ละตัวในตารางคือค่า `z` ของจุดนั้น ส่วน `x` และ `y` มาจากตำแหน่งในตาราง",
        "ต้องเชื่อมจุดที่ติดกันด้วยเส้นตรง (edge) ให้เป็นโครงลวด",
        "กด `ESC` ต้องปิดหน้าต่างและออกอย่างสะอาด และกดกากบาทก็ต้องออกได้",
        "ต้องใช้ MiniLibX (เวอร์ชันบนเครื่องโรงเรียน หรือคอมไพล์จาก source)",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้าม global variable**",
        "Makefile ต้องไม่ relink",
        "ต้องจัดการ error ของไฟล์: ไม่มีไฟล์, เปิดไม่ได้, รูปแบบผิด — ห้ามพังแบบไม่ควบคุม",
      ]},
      { h: "Bonus" },
      { ul: [
        "เพิ่ม projection อีกแบบ (เช่น parallel หรือ conic)",
        "ซูมเข้า-ออก",
        "เลื่อนโมเดล (translate)",
        "หมุนโมเดล (rotate)",
        "เพิ่ม bonus อื่นตามใจอีกหนึ่งอย่าง",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "แผนที่ที่แถวยาวไม่เท่ากัน, ไฟล์ว่าง, มีอักขระที่ไม่ใช่ตัวเลข ต้องขึ้น error ไม่ใช่ crash",
        "รูปแบบ `z,0xRRGGBB` (สีต่อจุด) ต้องอ่านได้ ไม่ใช่แค่ `z` เปล่า",
        "แผนที่ใหญ่มากและแผนที่ที่ค่าความสูงต่างกันสุดขั้ว ต้องยังอยู่ในกรอบหน้าต่าง",
        "valgrind สะอาดหลังปิดหน้าต่าง",
      ]},
      { note: "อ้างอิง: FdF subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Read a `.fdf` file holding a grid of altitudes and draw it as a 3D wireframe landscape, joining points (x, y, z) with line segments and projecting them isometrically." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`fdf`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`, demo `.fdf` files"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "a file with the `*.fdf` extension"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · all of the math library (`-lm`) · all of MiniLibX · `gettimeofday()` · `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "The model must be rendered in **isometric projection**",
        "The landscape's coordinates come from the `.fdf` file passed on the command line",
        "Each number in the grid is that point's `z`; `x` and `y` come from its position in the grid",
        "Neighbouring points must be joined by line segments into a wireframe",
        "`ESC` must close the window and exit cleanly, and so must the window's close button",
        "MiniLibX is required, either the school-machine version or built from source",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**Global variables are forbidden**",
        "The Makefile must not relink",
        "File errors must be handled: missing file, unreadable file, malformed content — never an uncontrolled crash",
      ]},
      { h: "Bonus" },
      { ul: [
        "One extra projection (parallel or conic, for instance)",
        "Zoom in and out",
        "Translate the model",
        "Rotate the model",
        "One more bonus of your own choosing",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Ragged rows, an empty file and non-numeric characters must produce an error, not a crash",
        "The `z,0xRRGGBB` form (per-point colour) must parse, not only the bare `z`",
        "Very large maps and maps with extreme altitude ranges must still fit inside the window",
        "Valgrind clean after closing the window",
      ]},
      { note: "Source: FdF subject" },
    ],
  };

  /* ============================== fractol ============================== */
  S.fractol = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "เขียนโปรแกรมสำรวจแฟร็กทัลด้วย MiniLibX — เรนเดอร์ Mandelbrot กับ Julia แล้วซูมเข้าไปได้แทบไม่จำกัด ซึ่งเป็นแก่นของสิ่งที่เรียกว่าแฟร็กทัล" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`fractol`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "ชนิดของแฟร็กทัลที่จะแสดง และตัวเลือกอื่นที่รองรับ"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · ทุกฟังก์ชันของ math (`-lm`) · ทุกฟังก์ชันของ MiniLibX · `gettimeofday()` · `ft_printf` หรือเทียบเท่าที่เขียนเอง"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — ต้องทำอะไรบ้าง" },
      { ul: [
        "ต้องมีทั้งชุด **Julia** และ **Mandelbrot**",
        "**ล้อเมาส์ซูมเข้า-ออกได้แทบไม่จำกัด** (เท่าที่ความละเอียดของเครื่องจะไหว)",
        "ต้องสร้าง Julia set แบบต่าง ๆ ได้ด้วยการส่งพารามิเตอร์ที่ต่างกันเข้าโปรแกรม",
        "พารามิเตอร์บน command line เป็นตัวกำหนดว่าจะแสดงแฟร็กทัลชนิดไหน",
        "**ถ้าไม่ส่งพารามิเตอร์ หรือส่งพารามิเตอร์ที่ไม่ถูกต้อง ต้องแสดงรายการตัวเลือกที่ใช้ได้แล้วจบโปรแกรม**",
        "กด `ESC` ต้องปิดหน้าต่างและออกอย่างสะอาด และกดกากบาทก็ต้องออกได้",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้าม global variable**",
        "Makefile ต้องไม่ relink",
        "ต้องใช้ MiniLibX",
      ]},
      { h: "Bonus" },
      { ul: [
        "เพิ่มแฟร็กทัลอีกหนึ่งชนิด (บนเน็ตมีให้เลือกเป็นร้อยแบบ)",
        "ซูมตามตำแหน่งเมาส์จริง ๆ ไม่ใช่ซูมที่กลางจอ",
        "เลื่อนมุมมองด้วยปุ่มลูกศรเพิ่มจากการซูม",
        "ทำให้ช่วงสีเลื่อนไปมาได้",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "รันโดยไม่ใส่พารามิเตอร์ และใส่พารามิเตอร์มั่ว ต้องได้รายการตัวเลือกแล้วจบ ไม่ใช่ crash",
        "ซูมลึกมาก ๆ แล้วภาพต้องไม่แตกเป็นบล็อกก่อนถึงขีดจำกัดของ `double`",
        "ตรวจว่าเขียนภาพลง buffer ของ image แล้วส่งเข้าหน้าต่างครั้งเดียว ไม่ใช่วาดทีละ pixel",
        "valgrind สะอาดหลังปิดหน้าต่าง",
      ]},
      { note: "อ้างอิง: fract-ol subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Write a small fractal explorer with MiniLibX — render the Mandelbrot and Julia sets and zoom into them almost without limit, which is the whole point of a fractal." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`fractol`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "the type of fractal to display, plus any other option you support"],
        ["External Function", "`open` `close` `read` `write` `malloc` `free` `perror` `strerror` `exit` · all of the math library (`-lm`) · all of MiniLibX · `gettimeofday()` · `ft_printf` or an equivalent you wrote"],
        ["Libft authorized", "**Yes**"],
      ]}},
      { h: "Mandatory — what has to exist" },
      { ul: [
        "Both the **Julia** and **Mandelbrot** sets",
        "**The mouse wheel zooms in and out almost infinitely**, within the machine's limits",
        "Different Julia sets must be reachable by passing different parameters",
        "A command-line parameter decides which fractal is displayed",
        "**With no parameter, or an invalid one, the program must show the available options and exit**",
        "`ESC` must close the window and exit cleanly, and so must the window's close button",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**Global variables are forbidden**",
        "The Makefile must not relink",
        "MiniLibX is required",
      ]},
      { h: "Bonus" },
      { ul: [
        "One more fractal (there are well over a hundred documented)",
        "Zoom that follows the actual mouse position rather than the screen centre",
        "Move the view with the arrow keys in addition to zooming",
        "Make the colour range shift",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "Running with no parameter and with nonsense parameters must list the options and exit, not crash",
        "Deep zooms must not break into blocks before `double` actually runs out of precision",
        "Confirm you draw into an image buffer and push it to the window once, rather than per pixel",
        "Valgrind clean after closing the window",
      ]},
      { note: "Source: fract-ol subject" },
    ],
  };

  /* ============================ philosophers =========================== */
  S.philosophers = {
    th: [
      { h: "โจทย์นี้สั่งอะไร" },
      { p: "จำลองปัญหา dining philosophers: นักปรัชญานั่งล้อมโต๊ะ สลับกันกิน นอน คิด โดยมีส้อมอยู่ระหว่างแต่ละคู่ ต้องใช้ส้อมสองอันจึงจะกินได้ และห้ามใครอดตาย. mandatory ใช้ **thread + mutex** ส่วน bonus ใช้ **process + semaphore**" },
      { table: { head: ["หัวข้อ", "ข้อกำหนด"], rows: [
        ["Program Name", "`philo`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c` ในไดเรกทอรี `philo/`"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "`number_of_philosophers time_to_die time_to_eat time_to_sleep [number_of_times_each_philosopher_must_eat]`"],
        ["External Function", "`memset` `printf` `malloc` `free` `write` `usleep` `gettimeofday` `pthread_create` `pthread_detach` `pthread_join` `pthread_mutex_init` `pthread_mutex_destroy` `pthread_mutex_lock` `pthread_mutex_unlock`"],
        ["Libft authorized", "**No**"],
      ]}},
      { h: "Mandatory — กติกาของการจำลอง" },
      { ul: [
        "**นักปรัชญาแต่ละคนต้องเป็น thread แยกกัน**",
        "มีส้อมหนึ่งอันระหว่างนักปรัชญาแต่ละคู่ — ถ้ามีหลายคน แต่ละคนจะมีส้อมซ้ายและขวา; **ถ้ามีคนเดียวจะเข้าถึงส้อมได้แค่อันเดียว**",
        "ต้องป้องกันไม่ให้ส้อมถูกใช้ซ้ำซ้อน โดยครอบสถานะของส้อมแต่ละอันด้วย mutex",
        "นักปรัชญาต้องกินโดยใช้ส้อมสองอัน แล้วสลับไปนอนและคิด",
        "ถ้าใส่พารามิเตอร์ตัวที่ห้า การจำลองจะหยุดเมื่อทุกคนกินครบจำนวนนั้น ไม่งั้นหยุดเมื่อมีคนตาย",
      ]},
      { h: "Mandatory — รูปแบบ log" },
      { ul: [
        "ทุกการเปลี่ยนสถานะต้องพิมพ์ `timestamp_in_ms X <ข้อความ>` โดย X คือหมายเลขนักปรัชญา",
        "ข้อความที่ต้องมี: has taken a fork / is eating / is sleeping / is thinking / died",
        "**ข้อความว่าตายต้องพิมพ์ภายใน 10 มิลลิวินาทีนับจากเวลาที่ตายจริง**",
        "ข้อความต้องไม่ปนกัน — บรรทัดหนึ่งต้องไม่แทรกกลางอีกบรรทัด",
      ]},
      { h: "กฎที่พลาดแล้วศูนย์" },
      { ul: [
        "**ห้าม global variable**",
        "Makefile ต้องไม่ relink",
        "ต้องไม่มี data race — ผู้ตรวจอาจรันด้วย helgrind หรือ `-fsanitize=thread`",
        "ต้องไม่มี memory leak และต้อง destroy mutex ทุกตัวก่อนจบ",
      ]},
      { h: "Bonus — process กับ semaphore" },
      { ul: [
        "โปรแกรมชื่อ `philo_bonus` อยู่ในไดเรกทอรี `philo_bonus/` แยกต่างหาก",
        "**นักปรัชญาแต่ละคนเป็น process แยก** และ process หลักไม่ใช่นักปรัชญา",
        "ส้อมทั้งหมดอยู่กลางโต๊ะ แทนด้วย **semaphore ตัวเดียว** ที่นับจำนวนส้อมที่ว่าง",
        "ฟังก์ชันที่เพิ่มมา: `fork` `kill` `exit` `waitpid` `sem_open` `sem_close` `sem_post` `sem_wait` `sem_unlink`",
        "bonus ตรวจก็ต่อเมื่อ mandatory สมบูรณ์แบบ",
      ]},
      { h: "เช็กก่อนส่ง" },
      { ul: [
        "`1 800 200 200` — คนเดียวต้องไม่กินได้ และต้องตายพอดีที่ 800ms",
        "`4 410 200 200` — ต้องไม่มีใครตาย",
        "`5 800 200 200 7` — ต้องหยุดเมื่อทุกคนกินครบ 7 ครั้ง",
        "`2 310 200 100` และ `4 310 200 100` — ต้องไม่มีใครตาย",
        "รันทิ้งไว้นาน ๆ แล้วต้องไม่ deadlock และ timestamp ต้องไม่กระโดด",
      ]},
      { note: "อ้างอิง: Philosophers subject" },
    ],
    en: [
      { h: "What the subject asks for" },
      { p: "Simulate the dining philosophers: philosophers around a table alternating between eating, sleeping and thinking, with one fork between each pair, needing two forks to eat, and nobody allowed to starve. The mandatory part uses **threads and mutexes**; the bonus uses **processes and semaphores**." },
      { table: { head: ["Item", "Requirement"], rows: [
        ["Program Name", "`philo`"],
        ["Files to Submit", "`Makefile`, `*.h`, `*.c` in a `philo/` directory"],
        ["Makefile", "`NAME` `all` `clean` `fclean` `re`"],
        ["Arguments", "`number_of_philosophers time_to_die time_to_eat time_to_sleep [number_of_times_each_philosopher_must_eat]`"],
        ["External Function", "`memset` `printf` `malloc` `free` `write` `usleep` `gettimeofday` `pthread_create` `pthread_detach` `pthread_join` `pthread_mutex_init` `pthread_mutex_destroy` `pthread_mutex_lock` `pthread_mutex_unlock`"],
        ["Libft authorized", "**No**"],
      ]}},
      { h: "Mandatory — the simulation's rules" },
      { ul: [
        "**Each philosopher must be a separate thread**",
        "There is one fork between each pair — with several philosophers each has a fork on their left and right; **with only one philosopher there is only one fork to reach**",
        "Protect each fork's state with a mutex so forks cannot be duplicated",
        "A philosopher eats with two forks, then sleeps, then thinks",
        "With the fifth parameter given, the simulation stops once everyone has eaten that many times; otherwise it stops when a philosopher dies",
      ]},
      { h: "Mandatory — log format" },
      { ul: [
        "Every state change prints `timestamp_in_ms X <message>`, where X is the philosopher's number",
        "The required messages: has taken a fork / is eating / is sleeping / is thinking / died",
        "**A death must be printed within 10 milliseconds of the actual death**",
        "Messages must not interleave — one line must never be printed inside another",
      ]},
      { h: "Rules that score zero if broken" },
      { ul: [
        "**Global variables are forbidden**",
        "The Makefile must not relink",
        "No data races — an evaluator may run helgrind or `-fsanitize=thread`",
        "No memory leaks, and every mutex destroyed before exit",
      ]},
      { h: "Bonus — processes and semaphores" },
      { ul: [
        "A `philo_bonus` program in its own `philo_bonus/` directory",
        "**Each philosopher is a separate process**, and the main process is not a philosopher",
        "All forks sit in the middle of the table, represented by **a single semaphore** counting the forks available",
        "Additional functions: `fork` `kill` `exit` `waitpid` `sem_open` `sem_close` `sem_post` `sem_wait` `sem_unlink`",
        "The bonus is graded only if the mandatory part is perfect",
      ]},
      { h: "Before you submit" },
      { ul: [
        "`1 800 200 200` — one philosopher must not manage to eat, and must die at exactly 800ms",
        "`4 410 200 200` — nobody may die",
        "`5 800 200 200 7` — must stop once everyone has eaten seven times",
        "`2 310 200 100` and `4 310 200 100` — nobody may die",
        "Leave it running a long time: no deadlock, and timestamps that never jump",
      ]},
      { note: "Source: Philosophers subject" },
    ],
  };

  /* ---- merge into the pages that are already loaded ---- */
  window.TEACHING_DATA = window.TEACHING_DATA || [];
  window.TEACHING_EN = window.TEACHING_EN || {};
  window.TEACHING_DATA.forEach(function (p) {
    if (!S[p.id]) return;
    p.sections.subject = S[p.id].th;
    if (window.TEACHING_EN[p.id]) window.TEACHING_EN[p.id].subject = S[p.id].en;
  });
})();
