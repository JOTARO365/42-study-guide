/* ===================== ft_printf ===================== */
/* window.TEACHING_DATA — อิงจากโค้ดจริงใน push_swap/libft/ft_printf*.c ของ wiaon-in */
window.TEACHING_DATA.unshift({
  id: "ft_printf",
  name: "ft_printf",
  tag: { th: "เขียน printf ขึ้นมาใหม่ด้วย variadic function — va_list, การเลื่อน argument, การแปลงฐานแบบเรียกซ้ำ และค่าที่คืนที่ต้องตรงกับของจริงทุกตัว",
         en: "Rebuild printf with variadic functions — va_list, argument promotion, recursive base conversion, and a return count that must match the real thing" },
  accent: "#ff9f43",
  sections: {
    principle: [
      { h: "ft_printf สอนอะไร" },
      { p: "โจทย์คือเขียน `printf` ขึ้นมาใหม่ — แต่สิ่งที่โปรเจกต์นี้สอนจริง ๆ คือ **ฟังก์ชันที่รับ argument ได้ไม่จำกัดจำนวนและไม่จำกัดชนิด (variadic function)** ซึ่งเป็นกลไกที่ C ซ่อนไว้เบื้องหลัง `...`" },
      { code: String.raw`int ft_printf(const char *s, ...);
                          ^^^
     "หลังจากนี้จะมี argument อีกกี่ตัวก็ได้ ชนิดอะไรก็ได้"

คำถามที่ต้องตอบให้ได้:
    ฟังก์ชันรู้ได้ยังไงว่ามีกี่ตัว        →  ไม่รู้ ต้องอ่านจาก format string
    ฟังก์ชันรู้ได้ยังไงว่าแต่ละตัวชนิดอะไร  →  ไม่รู้ ต้องอ่านจาก specifier
    ★ ถ้า format ไม่ตรงกับ argument จริง = undefined behaviour
      นี่คือเหตุผลที่ printf ไม่ปลอดภัยโดยธรรมชาติ`, cap: "compiler ไม่ตรวจให้ (ยกเว้นใส่ attribute พิเศษ) — `printf(\"%s\", 42)` คอมไพล์ผ่านแล้วพังตอนรัน", lang: "c" },
      { h: "ขอบเขตของส่วนบังคับ" },
      { table: { head: ["specifier", "ชนิดที่อ่าน", "พิมพ์อะไร"], rows: [
        ["`%c`", "`int`", "ตัวอักษร 1 ตัว"],
        ["`%s`", "`char *`", "สตริง · NULL ต้องพิมพ์ `(null)`"],
        ["`%p`", "address", "`0x` ตามด้วยฐาน 16 ตัวเล็ก · NULL พิมพ์ `(nil)`"],
        ["`%d` `%i`", "`int`", "ฐาน 10 มีเครื่องหมาย"],
        ["`%u`", "`unsigned int`", "ฐาน 10 ไม่มีเครื่องหมาย"],
        ["`%x`", "`unsigned int`", "ฐาน 16 ตัวเล็ก"],
        ["`%X`", "`unsigned int`", "ฐาน 16 ตัวใหญ่"],
        ["`%%`", "—", "`%` 1 ตัว และนับรวมในค่าที่คืน"],
      ]}},
      { p: "**ไม่มี flag ไม่มีความกว้าง ไม่มีความละเอียด** ในส่วนบังคับ — `%-5.3d` เป็นเรื่องของ bonus. ส่วนบังคับจึงเป็นการกระจายงานล้วน ๆ ไม่ใช่การแปลไวยากรณ์" },
      { h: "กฎเหล็ก" },
      { ul: [
        "**ห้ามใช้ `printf` จริง** (ชัดเจน) · ฟังก์ชันภายนอกที่ใช้ได้: `malloc`, `free`, `write`, และตระกูล `va_*`",
        "**norminette ต้องผ่าน** — 25 บรรทัด/ฟังก์ชัน, 5 ฟังก์ชัน/ไฟล์ · นี่คือเหตุผลที่ต้องกระจายงานเป็นหลายชั้น",
        "ต้องส่งเป็น `libftprintf.a` (หรือรวมเข้า `libft.a` ถ้าโปรเจกต์ปลายทางต้องการ)",
        "**ค่าที่คืนต้องตรงกับ `printf` จริง** — คือจำนวนตัวอักษรทั้งหมดที่พิมพ์ออกไป",
      ]},
      { note: "ในเวิร์กสเปซนี้โค้ด ft_printf ถูกวางไว้ในโฟลเดอร์ `libft/` เดียวกันและประกาศไว้ท้าย `libft.h` — เพื่อให้โปรเจกต์ปลายทางลิงก์ไฟล์เดียวจบ. **แต่ตอนส่งเป็นคนละโปรเจกต์กับ libft**" },
    ],

    theory: [
      { h: "🔬 เจาะลึก A: `va_list` ทำงานยังไงจริง ๆ" },
      { p: "C ไม่มีเวทมนตร์ — `va_list` คือ **ตัวชี้ตำแหน่งที่เดินไปตามกองของ argument ที่ผู้เรียกวางไว้**. `va_arg` แค่อ่านค่าตรงตำแหน่งนั้นตามชนิดที่เราบอก แล้วเลื่อนตัวชี้ไปตามขนาดของชนิดนั้น" },
      { code: String.raw`ft_printf("%d %s", 42, "hi");

ผู้เรียกวาง argument ไว้ (แนวคิด — ของจริงบน x86-64 ใช้ทะเบียนก่อนแล้วค่อยลง stack):

    [ s = "%d %s" ]  [ 42 ]  [ "hi" ]
                        ↑
                      va_list ชี้ตรงนี้หลัง va_start

va_arg(args, int)        อ่าน 42 → เลื่อนไป sizeof(int)
va_arg(args, char *)     อ่าน "hi" → เลื่อนไป sizeof(char *)

★ ไม่มีการตรวจสอบใด ๆ ทั้งสิ้น
  บอกว่า int ก็อ่าน 4 ไบต์ตรงนั้นแล้วตีความเป็น int
  บอกผิด = ได้ขยะ หรืออ่านเลยกองไปเลย`, cap: "va_list คือตัวชี้ + กฎการเลื่อน ไม่ใช่โครงสร้างที่รู้ว่ามีอะไรอยู่ข้างใน", lang: "c" },
      { table: { head: ["มาโคร", "ทำอะไร", "ต้องเรียกเมื่อไหร่"], rows: [
        ["`va_start(args, last)`", "ตั้ง `args` ให้ชี้ argument ตัวถัดจาก `last`", "ก่อนใช้ `va_arg` ครั้งแรก"],
        ["`va_arg(args, T)`", "อ่านค่าเป็นชนิด `T` แล้วเลื่อนตัวชี้", "ทุกครั้งที่ต้องการ argument ตัวถัดไป"],
        ["`va_end(args)`", "เก็บกวาด (บางสถาปัตยกรรมไม่ทำอะไร แต่ต้องเรียก)", "ก่อน `return` ทุกทาง"],
        ["`va_copy(dst, src)`", "ก๊อปสถานะการเดิน", "เมื่อต้องเดินซ้ำจากจุดเดิม (ไม่ใช้ในโปรเจกต์นี้)"],
      ]}},
      { p: "**`last` ใน `va_start` ต้องเป็นพารามิเตอร์ตัวสุดท้ายที่มีชื่อ** — คือ `s` ในกรณีนี้. มาโครใช้มันเป็นจุดอ้างอิงว่า 'กองของ argument ที่ไม่มีชื่อเริ่มถัดจากตรงนี้'" },
      { note: "ทุกทางออกจากฟังก์ชันต้องผ่าน `va_end` — `return` กลางทางโดยไม่เรียกเป็น undefined behaviour ตามมาตรฐาน แม้บน Linux/x86-64 จะไม่เห็นผลก็ตาม" },
      { qa: [
        { q: "`va_list` คืออะไร?", a: "ตัวชี้ตำแหน่งในกองของ argument ที่ไม่มีชื่อ — `va_arg` อ่านค่าตรงตำแหน่งนั้นตามชนิดที่เราระบุแล้วเลื่อนตัวชี้ต่อ" },
        { q: "`va_start` ต้องรับพารามิเตอร์ตัวไหน?", a: "พารามิเตอร์ตัวสุดท้ายที่มีชื่อ (ในที่นี้คือ format string) — ใช้เป็นจุดอ้างอิงว่ากองของ argument ที่ไม่มีชื่อเริ่มถัดจากไหน" },
        { q: "ถ้าบอกชนิดผิดใน `va_arg` เกิดอะไรขึ้น?", a: "undefined behaviour — มันอ่านจำนวนไบต์ตามชนิดที่บอกแล้วตีความตามนั้น ไม่มีการตรวจสอบใด ๆ. ได้ขยะหรืออ่านเลยกองไปเลย" },
        { q: "ทำไม `printf` ถึงไม่ปลอดภัยโดยธรรมชาติ?", a: "เพราะ format string เป็นตัวบอกชนิดของ argument และไม่มีอะไรบังคับว่าต้องตรงกับที่ส่งมาจริง — `printf(\"%s\", 42)` คอมไพล์ผ่านแล้วพังตอนรัน" },
      ]},

      { h: "🔬 เจาะลึก B: default argument promotion — ทำไม `%c` อ่านเป็น `int`" },
      { p: "argument ที่ส่งผ่าน `...` จะถูก **ขยายชนิดโดยอัตโนมัติ** ก่อนถึงมือฟังก์ชัน. กฎนี้มาจากยุคที่ C ยังไม่มี prototype และยังอยู่จนวันนี้" },
      { code: String.raw`ชนิดที่ส่งเข้ามา       ถูกขยายเป็น        ต้องอ่านด้วย
  char, signed char       int             va_arg(args, int)
  unsigned char           int             va_arg(args, int)
  short                   int             va_arg(args, int)
  unsigned short          int/unsigned    va_arg(args, int)
  _Bool                   int             va_arg(args, int)
  float                   double          va_arg(args, double)

  int, long, pointer      ไม่เปลี่ยน       ตามชนิดเดิม

ดังนั้น:
    ft_printf("%c", 'A');
        'A' เป็น char → ถูกขยายเป็น int ตั้งแต่ฝั่งผู้เรียก
        va_arg(args, char)   ✗ undefined behaviour
        va_arg(args, int)    ✓ ถูกต้อง`, cap: "ไม่มีทางเลือก — ตัว char ไม่ได้อยู่ในกองแล้ว มีแต่ int ที่ขยายมาจากมัน", lang: "c" },
      { code: String.raw`int	ft_check(va_list *args, char c)
{
	if (c == 'c')
		return (ft_putchar(va_arg(*args, int)));    /* ★ int ไม่ใช่ char */
	else if (c == 's')
		return (ft_putstr(va_arg(*args, char *)));
	else if (c == '%')
		return (ft_putchar('%'));
	return (ft_check_num(args, c));
}`, cap: "โค้ดจริง — `va_arg(*args, int)` แล้วส่งต่อให้ `ft_putchar(char c)` ซึ่งแคบกลับเองตอนเรียก", lang: "c" },
      { note: "**คอมไพเลอร์บางตัวเตือนให้เลย** — `gcc` ขึ้น *'second argument to va_arg is of promotable type char'* ถ้าเขียนผิด. `-Wall -Wextra -Werror` จึงจับได้ตั้งแต่คอมไพล์" },
      { qa: [
        { q: "ทำไม `%c` ต้อง `va_arg(args, int)`?", a: "เพราะ `char` ที่ส่งผ่าน `...` ถูกขยายเป็น `int` ตั้งแต่ฝั่งผู้เรียกตามกฎ default argument promotion — ในกองไม่มี `char` เหลืออยู่แล้ว" },
        { q: "ชนิดไหนบ้างที่ถูกขยาย?", a: "`char`, `short`, `_Bool` ขยายเป็น `int` · `float` ขยายเป็น `double`. `int`, `long` และ pointer ไม่เปลี่ยน" },
        { q: "กฎนี้มีมาทำไม?", a: "มาจากยุคก่อนที่ C จะมี prototype — ตอนนั้นคอมไพเลอร์ไม่รู้ชนิดพารามิเตอร์ จึงใช้กฎการขยายชนิดเดียวกันหมดเพื่อให้ฝั่งเรียกกับฝั่งรับตกลงกันได้" },
      ]},

      { h: "🔬 เจาะลึก C: ทำไมต้องส่ง `va_list *` ไม่ส่ง `va_list`" },
      { p: "norminette บังคับให้แต่ละฟังก์ชันไม่เกิน 25 บรรทัด ทำให้ต้องแยกงานออกเป็นหลายชั้น. แต่พอส่ง `va_list` ข้ามฟังก์ชัน จะเจอปัญหาที่ไม่ชัดในทันที" },
      { code: String.raw`ปัญหา: va_list ถูกนิยามยังไงขึ้นกับสถาปัตยกรรม

  บน x86-64 System V (Linux, macOS Intel):
      typedef struct { ... } va_list[1];     ← ★ เป็นอาเรย์ 1 ช่อง!

  บน สถาปัตยกรรมอื่นอาจเป็น:
      typedef char *va_list;                ← เป็น pointer ธรรมดา

ผลของการส่ง by value:
  ถ้าเป็นอาเรย์  → ส่งไปเป็น pointer โดยปริยาย → ฝั่งรับเดินตัวเดียวกัน
                    → ผู้เรียกเห็นตัวชี้เลื่อนไปด้วย
  ถ้าเป็น pointer → ก๊อปค่า → ฝั่งรับเดินสำเนา
                    → ★ ผู้เรียกไม่เห็นตัวชี้เลื่อน → argument ตัวเดิมถูกอ่านซ้ำ

  โค้ดชุดเดียวกันจึงทำงานถูกบนเครื่องหนึ่งและผิดบนอีกเครื่องหนึ่ง`, cap: "มาตรฐานเขียนไว้ตรง ๆ ว่าการส่ง `va_list` ต่อไปให้ฟังก์ชันอื่นแล้วอ่านต่อ ผลไม่ถูกนิยาม", lang: "c" },
      { p: "**ทางแก้: ส่ง address ไปเสมอ** — `ft_check(&args, c)` แล้วในฟังก์ชันใช้ `va_arg(*args, T)`. แบบนี้ไม่ว่า `va_list` จะถูกนิยามเป็นอะไร ทุกชั้นก็เดินตัวเดียวกันแน่นอน" },
      { code: String.raw`int	ft_printf(const char *s, ...)
{
	va_list	args;
	...
	length += ft_check(&args, s[i]);       /* ★ ส่ง address */
}

int	ft_check(va_list *args, char c)        /* ★ รับเป็น pointer */
{
	... va_arg(*args, int) ...             /* ★ dereference ก่อนใช้ */
}`, cap: "โค้ดจริง — รูปแบบเดียวกันทั้ง 4 ชั้น", lang: "c" },
      { qa: [
        { q: "ทำไมส่ง `va_list *` ไม่ส่ง `va_list`?", a: "เพราะ `va_list` อาจถูกนิยามเป็นอาเรย์ (x86-64) หรือ pointer ธรรมดา (สถาปัตยกรรมอื่น) — ส่ง by value แล้วบางที่ฝั่งรับเดินสำเนา ทำให้ผู้เรียกอ่าน argument ตัวเดิมซ้ำ. ส่ง address แล้วทุกชั้นเดินตัวเดียวกันแน่นอน" },
        { q: "ถ้าส่ง by value แล้วบั๊กจะออกมาเป็นยังไง?", a: "บนเครื่องที่ `va_list` เป็น pointer ธรรมดา ตัวชี้ในผู้เรียกจะไม่เลื่อน — `ft_printf(\"%d %d\", 1, 2)` จะพิมพ์ `1 1`. บน x86-64 จะไม่เห็นบั๊กเลยเพราะมันเป็นอาเรย์" },
      ]},

      { h: "🔬 เจาะลึก D: แปลงเลขเป็นสตริงด้วยการเรียกซ้ำ" },
      { p: "การพิมพ์เลขมีปัญหาลำดับ: การหารเอาเศษให้ **หลักขวาสุดออกมาก่อน** แต่เราต้องพิมพ์จากซ้าย. ทางออกคลาสสิกคือเรียกตัวเองก่อนพิมพ์ — ทำให้พิมพ์ตอนถอนกลับซึ่งได้ลำดับกลับด้านพอดี" },
      { code: String.raw`int	ft_putnbr_base_len(long long nbr, char *base)
{
	int	len_base;
	int	count;

	count = 0;
	len_base = (int)ft_strlen(base);
	if (nbr < 0)
	{
		count += ft_putchar('-');
		nbr = -nbr;                 /* ★ ปลอดภัยเพราะ nbr เป็น long long */
	}
	if (nbr >= len_base)
		count += ft_putnbr_base_len(nbr / len_base, base);   /* ★ เรียกก่อน */
	count += ft_putchar(base[nbr % len_base]);               /* ★ พิมพ์ทีหลัง */
	return (count);
}`, cap: "โค้ดจริง — ฟังก์ชันเดียวรองรับทุกฐาน โดยใช้ความยาวของสตริง base เป็นเลขฐาน", lang: "c" },
      { code: String.raw`ft_putnbr_base_len(255, "0123456789abcdef")     len_base = 16

  เรียก(255)  255 >= 16  →  เรียก(15) ก่อน
    เรียก(15)   15 < 16   →  ไม่เรียกต่อ
                            พิมพ์ base[15 % 16] = base[15] = 'f'
  กลับมาที่ (255)          พิมพ์ base[255 % 16] = base[15] = 'f'

  ★ ได้ "ff"  — ตัวแรกถูกพิมพ์ตอนถอนกลับจากชั้นในสุด

ความลึกของการเรียกซ้ำ = จำนวนหลัก
    เลข 64 บิตในฐาน 2 ลึกสุด 64 ชั้น — ไม่มีทาง stack overflow`, cap: "เป็นการเรียกซ้ำที่มีขอบเขตชัดเจน ไม่ต้องกังวลเรื่องความลึก", lang: "txt" },
      { p: "**ทำไมพารามิเตอร์เป็น `long long`:** `%d` ส่ง `int` มา แต่ `-INT_MIN` ล้น `int`. ขยายเป็น `long long` ตั้งแต่ signature ทำให้ `nbr = -nbr` ปลอดภัยโดยไม่ต้องแยกเคส" },
      { p: "**ทำไมต้องมี `ft_putptr_base_len` แยกอีกตัว:** `%p` ต้องรับ `unsigned long long` ซึ่งค่าสูงสุดเกินช่วงของ `long long` — ใส่ตัวเดียวกันไม่ได้ ต้องมีเวอร์ชัน unsigned ที่ไม่มีโค้ดจัดการเครื่องหมาย" },
      { qa: [
        { q: "แปลงเลขเป็นสตริงด้วยการเรียกซ้ำทำงานยังไง?", a: "หารเอาเศษให้หลักขวาสุดก่อน จึงเรียกตัวเองด้วยผลหารก่อน แล้วค่อยพิมพ์เศษ — ทำให้หลักซ้ายสุดถูกพิมพ์ตอนถอนกลับจากชั้นในสุด ได้ลำดับถูกต้อง" },
        { q: "ทำไมรับ `long long` ไม่รับ `int`?", a: "เพราะ `-INT_MIN` ล้น `int` — ขยายชนิดตั้งแต่ signature ทำให้กลับเครื่องหมายได้ปลอดภัยโดยไม่ต้องแยกเคส `INT_MIN`" },
        { q: "ทำไม `%p` ต้องมีฟังก์ชันแยก?", a: "address ต้องอ่านเป็น `unsigned long long` ซึ่งค่าสูงสุดเกินช่วงของ `long long` — และไม่มีเครื่องหมายให้จัดการ" },
        { q: "การเรียกซ้ำนี้เสี่ยง stack overflow ไหม?", a: "ไม่ — ความลึกเท่ากับจำนวนหลัก ซึ่งอย่างมากคือ 64 ชั้น (เลข 64 บิตในฐาน 2)" },
      ]},

      { h: "🔬 เจาะลึก E: ค่าที่คืน — รายละเอียดที่ tester จับได้ทันที" },
      { p: "`printf` คืน **จำนวนตัวอักษรที่พิมพ์ออกไปทั้งหมด**. ฟังดูง่าย แต่ต้องนับให้ครบทุกทาง — และรูปแบบการเขียนที่ทำให้ไม่พลาดคือ **ให้ทุกฟังก์ชันพิมพ์คืนจำนวนที่ตัวเองเขียน**" },
      { code: String.raw`int	ft_putchar(char c)
{
	write(1, &c, 1);
	return (1);              /* ★ คืน 1 ไม่ใช่ void */
}

int	ft_putstr(char *str)
{
	size_t	len;

	if (str == NULL)
	{
		write(1, "(null)", 6);
		return (6);          /* ★ (null) นับ 6 ตัว */
	}
	len = ft_strlen(str);
	write(1, str, len);
	return ((int)len);
}`, cap: "โค้ดจริง — ออกแบบให้คืน `int` ตั้งแต่ตัวล่างสุด ชั้นบนจึงแค่บวกสะสม", lang: "c" },
      { table: { head: ["กรณี", "ต้องนับ", "เพราะ"], rows: [
        ["`%%`", "1", "`%` ที่พิมพ์ออกไปคือตัวอักษรจริง"],
        ["`%s` กับ NULL", "6", "`(null)` ยาว 6 ตัว"],
        ["`%p` กับ NULL", "5", "`(nil)` ยาว 5 ตัว"],
        ["`%p` ปกติ", "2 + จำนวนหลัก", "`0x` นับด้วย"],
        ["`%d` ติดลบ", "1 + จำนวนหลัก", "`-` นับด้วย"],
        ["ตัวอักษรธรรมดาใน format", "ทุกตัว", "มันถูกพิมพ์ออกไปเหมือนกัน"],
      ]}},
      { note: "**เทสค่าที่คืนแยกจากผลลัพธ์เสมอ** — `ft_printf` ที่พิมพ์ถูกทุกตัวอักษรแต่คืนเลขผิดจะผ่านสายตา 100% และไม่ผ่าน tester 100%" },
      { qa: [
        { q: "`ft_printf` คืนค่าอะไร?", a: "จำนวนตัวอักษรทั้งหมดที่พิมพ์ออกไป รวมทั้งที่มาจาก `%%`, จาก `(null)` และจากตัวอักษรธรรมดาใน format string — เหมือน `printf` จริง" },
        { q: "ออกแบบยังไงให้นับไม่พลาด?", a: "ให้ฟังก์ชันพิมพ์ระดับล่างสุด (`ft_putchar`, `ft_putstr`) คืน `int` เป็นจำนวนที่ตัวเองเขียน แล้วทุกชั้นบนแค่บวกสะสม — ไม่มีที่ไหนต้องนับเอง" },
        { q: "`%p` กับ NULL คืนกี่ตัว?", a: "5 — `(nil)` มี 5 ตัวอักษร" },
      ]},

      { h: "🔬 เจาะลึก F: `write` โดยตรง = ไม่มีบัฟเฟอร์" },
      { p: "`printf` จริงเก็บผลลัพธ์ไว้ในบัฟเฟอร์แล้วค่อยเขียนออกเป็นก้อน. `ft_printf` ที่เรียก `write` ทีละตัวอักษรไม่มีบัฟเฟอร์เลย — มีทั้งข้อดีและข้อเสีย" },
      { table: { head: ["", "`printf` จริง (มีบัฟเฟอร์)", "`ft_printf` (write ตรง)"], rows: [
        ["จำนวน syscall", "น้อย (รวมเป็นก้อน)", "**มาก** — 1 ครั้งต่อตัวอักษร"],
        ["ความเร็ว", "เร็วกว่ามากเมื่อพิมพ์เยอะ", "ช้ากว่า"],
        ["ผลลัพธ์โผล่ตอนไหน", "เมื่อบัฟเฟอร์เต็ม / เจอ `\\n` / `fflush`", "**ทันที**"],
        ["ตอนโปรแกรม crash", "อาจตกหล่นที่ยังค้างในบัฟเฟอร์", "สิ่งที่พิมพ์ไปแล้วออกครบแน่นอน"],
      ]}},
      { p: "**ผลที่เห็นได้จริง:** ตอน debug โปรแกรมที่ segfault, `ft_printf` แสดงบรรทัดสุดท้ายก่อนพังเสมอ ในขณะที่ `printf` จริงอาจกลืนหายไปกับบัฟเฟอร์ที่ยังไม่ถูกล้าง — เป็นเหตุผลที่หลายคนใช้ `ft_printf` แทนตอนไล่บั๊กในโปรเจกต์หลัง ๆ" },
      { note: "การรวมตัวอักษรไว้ในบัฟเฟอร์แล้วเขียนทีเดียวก็ทำได้และเร็วกว่ามาก — แต่ต้องจัดการเรื่องล้างบัฟเฟอร์ตอนจบเอง และ norm 25 บรรทัดทำให้โค้ดยาวขึ้นพอสมควร. ส่วนบังคับไม่ได้ต้องการความเร็ว" },
      { qa: [
        { q: "`ft_printf` ต่างจาก `printf` จริงตรงไหนในเชิงพฤติกรรม?", a: "`printf` จริงเก็บบัฟเฟอร์แล้วค่อยเขียนเป็นก้อน ส่วน `ft_printf` ที่เรียก `write` ตรงจะออกทันที — ช้ากว่าแต่เห็นผลก่อน crash เสมอ" },
        { q: "ทำไมเรียก `write` ทีละตัวถึงช้า?", a: "`write` เป็น syscall ซึ่งต้องสลับเข้าไปทำงานในเคอร์เนลทุกครั้ง — ค่าใช้จ่ายต่อครั้งสูงกว่าการเขียนลงบัฟเฟอร์ในหน่วยความจำมาก" },
      ]},

      { h: "📖 อ่านเพิ่มเติม" },
      { links: [
        { label: "man 3 printf", url: "https://man7.org/linux/man-pages/man3/printf.3.html", note: "ค่าที่คืนและความหมายของทุก specifier" },
        { label: "man 3 stdarg", url: "https://man7.org/linux/man-pages/man3/stdarg.3.html", note: "`va_start` / `va_arg` / `va_end` / `va_copy`" },
        { label: "cppreference — Variadic arguments", url: "https://en.cppreference.com/w/c/variadic", note: "กฎ default argument promotion" },
        { label: "System V ABI — x86-64", url: "https://gitlab.com/x86-psABIs/x86-64-ABI", note: "ที่มาของการที่ `va_list` เป็นอาเรย์บนสถาปัตยกรรมนี้" },
        { label: "man 2 write", url: "https://man7.org/linux/man-pages/man2/write.2.html", note: "ทำไม syscall ถึงแพงกว่าการเขียนลงบัฟเฟอร์" },
      ]},
    ],

    foundations: [
      { h: "โครง 4 ชั้นของการกระจายงาน" },
      { code: String.raw`ft_printf(s, ...)        เดินไปตาม format · เจอ '%' ส่งตัวถัดไปให้ ft_check
   └─ ft_check           %c %s %%  จบที่นี่ · ที่เหลือส่งลงล่าง
       └─ ft_check_num   %d %i %u  จบที่นี่ · ฐาน 16 ส่งลงล่าง
           └─ ft_check_hex   %x %X %p

ทำไมต้อง 4 ชั้น:
    norm บังคับ 25 บรรทัด/ฟังก์ชัน — เขียน if-else 9 ตัวในฟังก์ชันเดียวไม่พอ
    และ 5 ฟังก์ชัน/ไฟล์ — จึงต้องแยกเป็น ft_printf.c กับ ft_printf_utils.c

ทุกชั้นคืน int (จำนวนตัวอักษรที่เขียน) → ชั้นบนบวกสะสม`, cap: "ข้อจำกัดของ norm บังคับโครง แต่บังเอิญได้โครงที่อ่านง่ายด้วย", lang: "txt" },
      { code: String.raw`int	ft_printf(const char *s, ...)
{
	va_list	args;
	int		length;
	int		i;

	length = 0;
	i = 0;
	va_start(args, s);
	while (s[i])
	{
		if (s[i] == '%')
		{
			i++;
			length += ft_check(&args, s[i]);
		}
		else
			length += ft_putchar(s[i]);
		i++;
	}
	va_end(args);
	return (length);
}`, cap: "โค้ดจริง — `i++` 2 ที่: ตัวในกระโดดข้าม `%` ตัวนอกเดินต่อจาก specifier", lang: "c" },

      { h: "ชั้นล่างสุด — ฟังก์ชันพิมพ์" },
      { code: String.raw`int	ft_putpointer(unsigned long long p)
{
	int	len;

	len = 0;
	if (p == 0)
		len += ft_putstr("(nil)");            /* ★ NULL → (nil) */
	else
	{
		len = ft_putstr("0x");                /* ★ นำหน้าด้วย 0x */
		len += ft_putptr_base_len(p, "0123456789abcdef");
	}
	return (len);
}`, cap: "โค้ดจริง — `%p` เป็น specifier เดียวที่มีคำนำหน้าติดมาด้วย", lang: "c" },
      { code: String.raw`int	ft_check_hex(va_list *args, char c)
{
	char	*base;

	if (c == 'x')
	{
		base = "0123456789abcdef";
		return (ft_putnbr_base_len(va_arg(*args, unsigned int), base));
	}
	else if (c == 'X')
	{
		base = "0123456789ABCDEF";           /* ★ ต่างกันแค่สตริง base */
		return (ft_putnbr_base_len(va_arg(*args, unsigned int), base));
	}
	else if (c == 'p')
		return (ft_putpointer(va_arg(*args, unsigned long long)));
	return (0);
}`, cap: "โค้ดจริง — `%x` กับ `%X` ใช้ฟังก์ชันเดียวกัน ต่างกันแค่ชุดตัวอักษรที่ส่งเข้าไป", lang: "c" },
      { p: "**การส่ง `base` เป็นสตริงคือกุญแจ** — ฟังก์ชันแปลงเลขตัวเดียวรองรับได้ทุกฐานโดยใช้ `ft_strlen(base)` เป็นเลขฐาน และ `base[n % len]` เป็นตัวอักษรของแต่ละหลัก. เพิ่มฐาน 8 หรือฐาน 2 ก็แค่ส่งสตริงใหม่เข้าไป" },
      { note: "`%X` ไม่ได้แปลงเป็นตัวใหญ่ทีหลัง — มันใช้ชุดตัวอักษรที่เป็นตัวใหญ่ตั้งแต่แรก. เขียนแบบแปลงทีหลังก็ได้แต่ยาวกว่าและไม่จำเป็น" },
    ],

    architecture: [
      { h: "โครงไฟล์" },
      { code: String.raw`ft_printf.h            ประกาศทั้ง 7 ฟังก์ชัน · include libft.h และ <stdarg.h>
ft_printf.c            ft_printf · ft_check · ft_check_num · ft_check_hex   (4 ตัว)
ft_printf_utils.c      ft_putchar · ft_putstr · ft_putpointer
                       ft_putnbr_base_len · ft_putptr_base_len              (5 ตัว)

norm บังคับ 5 ฟังก์ชัน/ไฟล์ → 9 ฟังก์ชันจึงต้องแยก 2 ไฟล์พอดี`, cap: "การแบ่งไฟล์ไม่ได้เกิดจากรสนิยม แต่เกิดจากข้อจำกัดของ norm", lang: "txt" },
      { code: String.raw`#ifndef FT_PRINTF_H
# define FT_PRINTF_H

# include "libft.h"        /* ★ ใช้ ft_strlen ของ libft ไม่เขียนซ้ำ */
# include <stdarg.h>       /* ★ va_list และผองเพื่อน */

int	ft_printf(const char *s, ...);
int	ft_check(va_list *args, char c);
int	ft_putchar(char c);
int	ft_putstr(char *str);
int	ft_putnbr_base_len(long long nbr, char *base);
int	ft_putptr_base_len(unsigned long long nbr, char *base);
int	ft_putpointer(unsigned long long p);

#endif`, cap: "โค้ดจริง — `<stdarg.h>` เป็น header เดียวที่โปรเจกต์นี้ต้องการเพิ่มจาก libft", lang: "c" },
      { p: "**ทำไม include `libft.h`:** `ft_putstr` ต้องใช้ `ft_strlen` และ `ft_putnbr_base_len` ก็ต้องใช้เพื่อหาความยาวของ `base`. เขียน `ft_strlen` ซ้ำในโปรเจกต์นี้ก็ได้ แต่ในเวิร์กสเปซนี้เลือกลิงก์กับ libft ที่มีอยู่แล้ว" },

      { h: "รูปแบบการส่ง 2 แบบ" },
      { table: { head: ["", "แยกเป็น `libftprintf.a`", "รวมเข้า `libft.a` (แบบที่ใช้ที่นี่)"], rows: [
        ["ตอนส่งโปรเจกต์", "ตามที่ subject กำหนด", "ต้องมี Makefile ที่สร้าง `libftprintf.a` ได้ด้วย"],
        ["โปรเจกต์ปลายทางลิงก์", "`cc main.c libft.a libftprintf.a`", "`cc main.c libft.a`"],
        ["ft_strlen ที่ ft_printf ใช้", "เขียนซ้ำในโปรเจกต์ หรือลิงก์ libft ด้วย", "ใช้ของ libft ตรง ๆ"],
      ]}},
      { note: "ส่ง ft_printf ต้องส่งเป็น `libftprintf.a` ตาม subject — ในเวิร์กสเปซนี้เก็บโค้ดรวมไว้กับ libft เพื่อความสะดวกของโปรเจกต์ปลายทาง แต่ตอนส่งจริงต้องมีโฟลเดอร์ของโปรเจกต์เองที่สร้างไฟล์ตามชื่อที่กำหนด" },
    ],

    dataflow: [
      { h: "ไล่ `ft_printf(\"n=%d s=%s\\n\", 42, \"hi\")`" },
      { code: String.raw`i   s[i]   ทำอะไร                                   length   ตัวชี้ argument
 0   'n'   ft_putchar('n')                          1
 1   '='   ft_putchar('=')                          2
 2   '%'   i++ → ft_check(&args, 'd')
             ft_check_num → ft_putnbr_base_len(42)
               42 < 10? ไม่ → เรียก(4) ก่อน
                 4 < 10 → พิมพ์ '4'
               กลับมา → พิมพ์ '2'                    4       เลื่อนพ้น 42
 4   ' '   ft_putchar(' ')                          5
 5   's'   ft_putchar('s')                          6
 6   '='   ft_putchar('=')                          7
 7   '%'   i++ → ft_check(&args, 's')
             ft_putstr("hi") → write 2 ไบต์          9       เลื่อนพ้น "hi"
 9   '\n'  ft_putchar('\n')                         10

va_end(args)
return 10                    ★ ตรงกับ printf จริง`, cap: "argument ถูกอ่านตามลำดับที่ specifier ปรากฏ — ไม่มีการย้อนกลับหรือข้าม", lang: "txt" },

      { h: "เคสขอบที่ต้องเทสให้ครบ" },
      { table: { head: ["อินพุต", "ผลที่ถูก", "จับอะไร"], rows: [
        ["`ft_printf(\"%s\", NULL)`", "`(null)` · คืน 6", "ไม่ crash และนับถูก"],
        ["`ft_printf(\"%p\", NULL)`", "`(nil)` · คืน 5", "กรณีพิเศษของ `%p`"],
        ["`ft_printf(\"%d\", INT_MIN)`", "`-2147483648` · คืน 11", "การกลับเครื่องหมายไม่ล้น"],
        ["`ft_printf(\"%u\", -1)`", "`4294967295`", "อ่านเป็น unsigned ไม่ใช่ signed"],
        ["`ft_printf(\"%x\", UINT_MAX)`", "`ffffffff`", "ไม่มีเครื่องหมายลบโผล่มา"],
        ["`ft_printf(\"%%\")`", "`%` · คืน 1", "นับตัวที่พิมพ์ไม่ใช่ตัวใน format"],
        ["`ft_printf(\"\")`", "ไม่พิมพ์อะไร · คืน 0", "format ว่าง"],
        ["`ft_printf(\"%c\", 0)`", "พิมพ์ไบต์ 0 · คืน 1", "`\\0` เป็นตัวอักษรที่ถูกต้อง — `write` ส่งได้"],
        ["`ft_printf(\"abc\")`", "`abc` · คืน 3", "ไม่มี specifier เลย"],
      ]}},
      { note: "**`%c` กับค่า 0 คือเคสที่คนพลาดบ่อย** — ถ้าไปใช้ `ft_putstr` แทน `write` มันจะหยุดที่ `\\0` แล้วไม่พิมพ์อะไรเลย. ใช้ `write(1, &c, 1)` ถึงจะส่งไบต์ 0 ออกไปได้จริง" },
    ],

    implementation: [
      { h: "ลำดับการลงมือเขียน" },
      { ul: [
        "1. **`ft_putchar` / `ft_putstr` ที่คืน `int`** — ทำก่อนเพราะทุกอย่างวางบนมัน และถ้าทำเป็น `void` ไว้ก่อนจะต้องกลับมาแก้ทั้งชุด",
        "2. **`ft_printf` โครงกับ `%c` `%s` `%%`** — ทดสอบว่าเดิน format ถูกและนับถูกก่อนไปต่อ",
        "3. **`ft_putnbr_base_len`** — ทดสอบฐาน 10 ก่อนแล้วค่อยลองฐาน 16 ด้วยฟังก์ชันเดียวกัน",
        "4. **`%d` `%i` `%u`** — ระวังว่า `%u` ต้อง `va_arg(*args, unsigned int)`",
        "5. **`%x` `%X` `%p`** — `%p` ต้องมีฟังก์ชันแปลงแบบ unsigned แยก",
      ]},
      { h: "อาการพัง → สาเหตุ" },
      { table: { head: ["อาการ", "สาเหตุ", "แก้"], rows: [
        ["`%d %d` พิมพ์เลขตัวแรกซ้ำ 2 ครั้ง", "ส่ง `va_list` by value", "ส่ง `va_list *` แล้วใช้ `va_arg(*args, T)`"],
        ["คอมไพเลอร์เตือน *promotable type*", "`va_arg(args, char)`", "`va_arg(args, int)`"],
        ["`%d` กับ `INT_MIN` ได้ค่าผิด", "กลับเครื่องหมายบน `int`", "รับพารามิเตอร์เป็น `long long`"],
        ["`%u` กับ `-1` ได้ `-1`", "`va_arg(*args, int)`", "`va_arg(*args, unsigned int)`"],
        ["`%x` กับค่าใหญ่มีเครื่องหมายลบโผล่", "เหมือนกัน", "เหมือนกัน"],
        ["`%s` กับ NULL แล้ว crash", "ไม่เช็ค NULL", "พิมพ์ `(null)` แล้วคืน 6"],
        ["`%p` กับ NULL พิมพ์ `0x0`", "ไม่ได้แยกเคส", "พิมพ์ `(nil)` แล้วคืน 5"],
        ["`%c` กับค่า 0 ไม่พิมพ์อะไร", "ใช้ฟังก์ชันที่หยุดที่ `\\0`", "`write(1, &c, 1)` โดยตรง"],
        ["ค่าที่คืนน้อยกว่าจริง", "ฟังก์ชันพิมพ์บางตัวคืน `void` หรือลืมบวก", "ทุกตัวคืน `int` และทุกชั้นบวกสะสม"],
        ["norminette ฟ้องจำนวนฟังก์ชัน", "เกิน 5 ตัวใน 1 ไฟล์", "แยกเป็น `ft_printf.c` กับ `ft_printf_utils.c`"],
        ["`%` ตัวสุดท้ายของ format แล้วอ่านเลยขอบ", "`i++` แล้วเข้าถึง `s[i]` โดยไม่เช็ค", "เช็คว่ายังไม่ถึง `\\0` ก่อน dispatch"],
      ]}},
      { h: "build / test" },
      { code: String.raw`make re && make          # ครั้งที่ 2 ต้องขึ้น "Nothing to be done"
norminette *.c *.h        # 0 error

# ไดรเวอร์เทียบกับ printf จริง: ทั้ง output และค่าที่คืน
cat > cmp.c <<'EOF'
#include <stdio.h>
#include "ft_printf.h"
int main(void)
{
    int a = ft_printf("[%s] [%d] [%p]\n", "hi", -42, (void *)0);
    int b = printf("[%s] [%d] [%p]\n", "hi", -42, (void *)0);
    printf("ret: mine=%d real=%d\n", a, b);
    return (0);
}
EOF
cc -Wall -Wextra -Werror cmp.c libftprintf.a -o cmp && ./cmp

# แยก stream เพื่อ diff เฉพาะ output
./mine > mine.txt && ./ref > ref.txt && diff mine.txt ref.txt && echo "output ตรง"

# valgrind
valgrind --leak-check=full --error-exitcode=42 -q ./cmp && echo "ผ่าน"

# บน Windows ผ่าน WSL
wsl --exec bash -lc 'cd /mnt/d/Projects/42/push_swap/libft && norminette ft_printf*.c ft_printf.h'`, lang: "bash" },
      { note: "**เทียบกับ `printf` จริงในโปรแกรมเดียวกัน** — ไม่ใช่เทียบกับสิ่งที่เราคิดว่ามันควรพิมพ์. รูปแบบของ `%p` ต่างกันได้ระหว่างระบบปฏิบัติการ การเทียบสด ๆ จึงเชื่อถือได้กว่า" },
    ],

    tricks: [
      { h: "ทริค 1: ให้ทุกฟังก์ชันพิมพ์คืน `int` ตั้งแต่ตัวแรก" },
      { p: "ถ้าเริ่มด้วย `void ft_putchar(char c)` แล้วมารู้ทีหลังว่าต้องนับ จะต้องไล่แก้ทั้งชุดพร้อมทุกจุดที่เรียก. คืน `int` ตั้งแต่แรกไม่เสียอะไรเลย" },
      { h: "ทริค 2: ส่ง `va_list *` เสมอเมื่อข้ามฟังก์ชัน" },
      { p: "จำเป็นทางเทคนิคและยังเป็นคำถามที่กรรมการถามบ่อย — รู้เหตุผล (`va_list` อาจเป็นอาเรย์) แล้วตอบได้ในประโยคเดียว" },
      { h: "ทริค 3: ส่ง `base` เป็นสตริง แทนที่จะเขียนฟังก์ชันต่อฐาน" },
      { p: "`ft_putnbr_base_len(n, \"0123456789\")` กับ `(n, \"0123456789abcdef\")` ใช้โค้ดชุดเดียวกัน — และ `%X` ก็แค่เปลี่ยนสตริงเป็นตัวใหญ่" },
      { h: "ทริค 4: เทสค่าที่คืนแยกจากผลลัพธ์" },
      { p: "เขียนไดรเวอร์ที่พิมพ์ `ret: mine=%d real=%d` ไว้ท้ายทุกเคส — บั๊กเรื่องการนับมองไม่เห็นจาก output เลย" },
      { h: "ทริค 5: เทียบกับ `printf` จริงในโปรแกรมเดียวกัน" },
      { p: "รูปแบบของ `%p` ต่างกันได้ระหว่าง Linux กับ macOS — เทียบสดในโปรแกรมเดียวกันจึงเชื่อถือได้กว่าการจดผลที่คาดไว้" },
      { h: "ทริค 6: ระวัง `%` ที่อยู่ท้าย format" },
      { p: "`ft_printf(\"100%\")` ทำให้ `i++` แล้วอ่าน `s[i]` ที่เป็น `\\0` — ไม่ crash แต่ก็ไม่ควรปล่อย. เช็คก่อน dispatch แล้วจบเรื่อง" },
      { h: "ทริค 7: ใช้ ft_printf ตัวเองตอนไล่บั๊กโปรเจกต์หลัง" },
      { p: "ไม่มีบัฟเฟอร์ = บรรทัดสุดท้ายก่อน segfault ออกมาครบเสมอ ในขณะที่ `printf` จริงอาจกลืนหายไปกับบัฟเฟอร์ที่ยังไม่ถูกล้าง" },
    ],

    eval: [
      { qa: [
        { q: "variadic function คืออะไร?", a: "ฟังก์ชันที่รับ argument ได้ไม่จำกัดจำนวน ประกาศด้วย `...` — ตัวฟังก์ชันไม่รู้จำนวนหรือชนิดเอง ต้องอาศัยข้อมูลอื่น (ในที่นี้คือ format string)" },
        { q: "`va_start` / `va_arg` / `va_end` ทำอะไรตามลำดับ?", a: "`va_start` ตั้งตัวชี้ให้ชี้ argument ตัวถัดจากพารามิเตอร์ที่มีชื่อตัวสุดท้าย · `va_arg` อ่านค่าตามชนิดที่ระบุแล้วเลื่อนตัวชี้ · `va_end` เก็บกวาดก่อน return" },
        { q: "`va_arg` ตรวจสอบชนิดให้ไหม?", a: "ไม่เลย — มันอ่านจำนวนไบต์ตามชนิดที่เราบอกแล้วตีความตามนั้น. บอกผิดคือ undefined behaviour" },
        { q: "ทำไม `%c` ต้องอ่านเป็น `int`?", a: "เพราะ `char` ที่ส่งผ่าน `...` ถูกขยายเป็น `int` ตั้งแต่ฝั่งผู้เรียกตามกฎ default argument promotion" },
        { q: "ชนิดไหนบ้างที่ถูกขยายเมื่อส่งผ่าน `...`?", a: "`char`, `short`, `_Bool` ขยายเป็น `int` และ `float` ขยายเป็น `double`" },
        { q: "ทำไมส่ง `va_list *` ไม่ส่ง `va_list`?", a: "`va_list` อาจถูกนิยามเป็นอาเรย์หรือ pointer แล้วแต่สถาปัตยกรรม — ส่ง by value แล้วบางที่ฝั่งรับเดินสำเนา ทำให้ argument ตัวเดิมถูกอ่านซ้ำ. ส่ง address แล้วทุกชั้นเดินตัวเดียวกันแน่นอน" },
        { q: "ทำไมต้องแบ่งเป็น 4 ชั้น?", a: "เพราะ norm บังคับ 25 บรรทัด/ฟังก์ชัน และ 5 ฟังก์ชัน/ไฟล์ — if-else 9 กรณีใส่ฟังก์ชันเดียวไม่ได้" },
        { q: "แปลงเลขเป็นสตริงยังไง?", a: "เรียกซ้ำด้วยผลหารก่อนแล้วค่อยพิมพ์เศษ — เพราะการหารเอาเศษให้หลักขวาสุดออกมาก่อน การพิมพ์ตอนถอนกลับจึงได้ลำดับถูก" },
        { q: "ทำไมฟังก์ชันแปลงเลขรับ `long long`?", a: "เพื่อให้ `-INT_MIN` ไม่ล้นตอนกลับเครื่องหมาย — ถ้ารับ `int` จะต้องแยกเคส `INT_MIN` ออกมาต่างหาก" },
        { q: "`%x` กับ `%X` ต่างกันยังไงในโค้ด?", a: "ใช้ฟังก์ชันแปลงตัวเดียวกัน ต่างกันแค่สตริง `base` ที่ส่งเข้าไป — ตัวเล็กกับตัวใหญ่" },
        { q: "`ft_printf` คืนค่าอะไร?", a: "จำนวนตัวอักษรทั้งหมดที่พิมพ์ออกไป รวมทั้งตัวอักษรธรรมดาใน format, `%` จาก `%%`, และ `(null)`/`(nil)`" },
        { q: "`%s` กับ NULL ต้องทำอะไร?", a: "พิมพ์ `(null)` และนับ 6 — ไม่ใช่ crash และไม่ใช่ไม่พิมพ์อะไร" },
        { q: "`%p` กับ NULL ต้องทำอะไร?", a: "พิมพ์ `(nil)` และนับ 5" },
        { q: "`%c` กับค่า 0 ต้องได้อะไร?", a: "ต้องเขียนไบต์ 0 ออกไปจริงและนับ 1 — ใช้ `write(1, &c, 1)` เพราะฟังก์ชันที่ทำงานกับสตริงจะหยุดที่ `\\0`" },
        { q: "`ft_printf` ต่างจาก `printf` จริงตรงไหน?", a: "`printf` จริงมีบัฟเฟอร์แล้วเขียนเป็นก้อน; `ft_printf` เรียก `write` ตรงจึงออกทันที — ช้ากว่าแต่ไม่ตกหล่นตอนโปรแกรม crash" },
        { q: "ทำไมถึงบอกว่า `printf` ไม่ปลอดภัย?", a: "เพราะ format string เป็นตัวกำหนดว่าจะอ่าน argument เป็นชนิดอะไร โดยไม่มีอะไรบังคับให้ตรงกับที่ส่งมาจริง — และ format string ที่มาจากผู้ใช้เปิดช่องให้อ่านหน่วยความจำนอกขอบเขตได้" },
      ]},
      { h: "เช็กลิสต์ก่อนส่ง" },
      { code: String.raw`# 1. build + norm
make re && make          # ครั้งที่ 2 ต้อง "Nothing to be done"
norminette *.c *.h       # 0 error

# 2. ไม่มี printf จริงหลุดอยู่ในโค้ด
grep -n 'printf' *.c | grep -v ft_printf

# 3. เทียบกับ printf จริง ทั้ง output และค่าที่คืน ทุก specifier
#    %c %s %p %d %i %u %x %X %%

# 4. เคสขอบ
#    %s NULL → (null) คืน 6
#    %p NULL → (nil)  คืน 5
#    %d INT_MIN → -2147483648 คืน 11
#    %u -1 → 4294967295
#    %x UINT_MAX → ffffffff
#    %c 0 → เขียนไบต์ 0 คืน 1
#    "" → คืน 0
#    "%" ท้าย format → ไม่อ่านเลยขอบ

# 5. valgrind สะอาด
valgrind --leak-check=full --error-exitcode=42 -q ./cmp && echo "ผ่าน"

# 6. โฟลเดอร์สะอาด
make fclean`, lang: "bash" },
    ],
  },
});
